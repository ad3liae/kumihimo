# Task 022: 台の上で組む（Jolt）

- 状態: **022-1 実施中。** 精査（この文書）は承認済み（`e6e16aa`）。**作者から台の寸法・錘・4点の答えを受け取り、反映した**（2026-09-07）。残りは Jolt の取得・build・harness の骨組みと種の静止。product code は変更していない
- 優先度: **最高。Task 020 の本体の続きである**（深さと上下を Task 021 から引き取る）
- 作成日: 2026-09-07
- 前提: **新しいセッションで始めてよいように書いてある。** ただし次を先に読むこと
  1. `CLAUDE.md` と `AGENTS.md`
  2. `docs/architecture.md` の「組み台の力学（作者の前提）」と「正本の読み方（作者の指示）」
  3. `docs/tasks/020-general-braid-simulator.md` の「これまでの誤り」
  4. `docs/tasks/021-depth-by-relaxation.md` の末尾「引き継ぎ」

## 作者の判断（2026-09-07）

> 渡りをどの高さに置くかは実際の台では誰も決めていない。糸の端を持ち上げて向こうのノッチに
> 置けば、途中の糸の上を通るので交差が生まれ、後から置いた方が上になる。**順番が上下を
> 決める。**

したがって——

- **「置く高さの模型」（021b〜021f）は不要になる。** 高さを勘定で与えるのではなく、**台の上で
  実際に運ぶ**。上下は運ぶ順序の帰結として出る。
- **積み重ね模型と占有履歴は捨てない。** どちらも **022 の結果と突き合わせる予言**として残す
  （下記「記録（合否を付けない）」）。
- Task 021 の状態は **保留**。**確認した。直す必要は無かった**（`021-depth-by-relaxation.md`
  の見出し行、および `docs/README.md` の項目が、どちらも「保留」と書いている）。

---

## 1. Task 016 の再読

### 016 の記録がどこにあるか（**重要。この枝でなければ読めない**）

**Task 016 の段階2・2b の記録は、未マージのブランチにしかない。**

  ブランチ  `claude/task-016-braid-physics-feasibility`
  commit    `d0e69c1`（段階2: 2つの定式化、どちらも解けない）
            `9b4d789`（段階2b: 2つの誤りを直した。それでも組み上がらない。打ち切り）
  試作      `prototypes/braid-physics/braid.c`・`tight.c`・`README.md`（同ブランチ）

**現在の枝の `docs/tasks/016-braid-physics-feasibility.md` は状態が「未着手」のままで、段階2 の
結果を持っていない。** `docs/README.md` の 016 の項目も同じである。**この食い違いは 022 では
直さない**（作者の指示の範囲外。報告に挙げる）。以下の引用はすべて上記ブランチからのものである。

`prototypes/braid-physics/` に未追跡で残っている `braid` と `tight` の2つの arm64 実行ファイルは、
この試作をビルドしたものである。**触らない。**

### 過程の模擬が組まなかった原因（016 の記録からの引用）

**試作1（丸台をそのまま回す。段階1 で設計したもの）**

> **組み上がらない。** 糸は中心へ集まるが、束にならずに毬になる。
>
> 1. **組み始めが持たない。** 結んだ端はまだ交差していないので、糸の張力を受け止められない。
>    鏡の上の水平部分（張力 = 玉1個）と、穴を下る垂直部分（張力 = 吊りおもり ÷ 16）が
>    角で繋がっているだけなので、糸は角を滑って引き出されてしまう。
> 2. **糸の送りが暴走する。** 鏡の縁の球が内側へ寄るたびに糸を1個送り出す実装にしたが、
>    消費されていないのに送ってしまうため、中心に糸が溜まる。

**試作2（完成した紐を締め上げる）**

> **この定式化は原理的に答えを持たない。** 3つ試して、いずれも退化した。
> **理由は退化ではなく物理である。** 完成した紐は、糸の長さと胞高の比がいくつであっても
> 釣り合っている。**1手あたりに紐がどれだけ伸びるかは、組み点でその手が糸をどれだけ食うかで
> 決まる。** つまり**過程の量であって、完成品の平衡からは出てこない。**

**段階2b（上の2つの誤りを直した1回限りの再挑戦）**

> **直ったこと**: 種は毬にならず、束として持ちこたえる。鏡の下に紐状の柱ができ、平均半径
> 2.8〜3.1 d で保たれる。**「組み始めが持たない」は解けた。**
>
> **直らなかったこと**
> - **新しい紐ができない。** 組み点は種の上端（約 −11.8 d）に留まったまま上がらない。糸は
>   送られてくる（1手あたり1本 2.8 d）が、紐にならず鏡の上に溜まる。
> - **1手あたりの送りが一定値へ落ち着かない。** 12手のあいだ 0 付近で揺れるだけで、収束の
>   兆しがない。平均半径はむしろ 2.86 → 3.08 と緩んでいく。
> - **種の寸法が結果に残っている。** 種のピッチ 1.2 d では組み点が −11.8 d で停止し、
>   2.4 d では**組み点がそもそも見つからない**。**出てくる数字は種を読み返しているだけで
>   あり、採用できない。**

**打ち切り判定**（016 自身の言葉）

> 指示書の打ち切り条件（組み上がらない、または送りが一定値へ落ち着かない）に両方あたる。
> **これ以上直さない。**

### 張力の前提は 016 に入っていたか — **入っていない**（3つのうち2つ）

作者の前提（`docs/architecture.md`「組み台の力学」）は3つある。016 の試作1／2b と突き合わせる。

| 前提 | 016 に入っていたか | 証拠 |
|---|---|---|
| 玉が鏡の縁を越えて垂れ、**外向き（下方）へ引き続ける** | **入っていない** | 玉の位置は**鏡の面の上**に運動学的に固定されていた |
| 紐の下端の錘が**下へ引き続ける** | **入っていた** | 尾の先端の粒に `TAKEUP / 16` を毎ステップ加えていた |
| **ゆるんだ糸は無い。どの区間も張った直線** | **入っていない。破れていた** | 「糸は…紐にならず**鏡の上に溜まる**」 |

1つ目の証拠は `braid.c` の `applyForces()` である。

```c
// The tama's position is imposed, carried or not: the hand puts it where
// the rule says. The tension is not a force on this bead — it is the
// elastic first segment below, and the tama pays out yarn when that
// segment's tension reaches its weight. See feed().
double a = slotAngle[y];
pos[i] = v3((RIM + 1.0) * cos(a), (RIM + 1.0) * sin(a), R);
```

**玉は鏡の面の高さ（`z = R`、糸の半径）に、縁のすぐ外（`RIM + 1.0`）で固定されている。**
垂れていない。**外向きの引きは、力としてはどこにも入っていない。** 玉が果たしていた役割は
「糸を繰り出す定荷重の供給源」（`feed()`）だけであって、**紐を外へ張る役ではない。**

そのため、016 で糸が鏡の上を走る区間は**張られていない**。016 が特定した第1の原因
（「鏡の上の水平部分と穴を下る垂直部分が角で繋がっているだけなので、糸は角を滑って引き出される」）
は、まさに**この区間に外向きの張力が無いことの言い換え**である。

**時系列も一致する。** 016 は 2026-09-06、作者が張力の前提を置いたのは 2026-09-07
（Task 021b-1）である。**016 は前提が存在しない時点の試みである。**

### したがって 022 は 016 と別の試みである（別である点）

1. **玉が垂れる。** 玉は鏡の縁を越えて外へ垂れ、**外向き下方の張力を生む**。鏡の上を走る区間は
   両端（組み点と縁）で引かれた**張った区間**になる。016 では玉が面の上に固定され、この張力が
   無かった。
2. **糸を繰り出さない。** 016 の `feed()`（張力で糸を継ぎ足す実装）は 022 には無い。糸は最初から
   十分な長さがあり、**玉が下がるぶんだけ組み点へ入る。** 016 の第2の原因（送りの暴走）は、
   **仕組みごと消える。**
3. **engine が違う。** 自作の XPBD（球の鎖・位置ベース）ではなく、**剛体カプセルの連鎖を Jolt に
   解かせる。** 摩擦・接触・スリープは engine のものを使い、値を記録する。

**016 の結論のうち、そのまま立つもの**（022 でも再挑戦しない）:
**完成した紐の平衡からは1手あたりの送りは出ない**（試作2、原理的）。**この道は二度と試さない。**

### 016 から引き継ぐ危険（022 が同じ所で死ぬかもしれない箇所）

- **種の寸法が結果に残る。** 016 の決定的な失敗はこれだった。**022 も種（組み始め）を要する。**
  022-2 では **016 自身の判定をそのまま使う**——ピッチの違う種を2つ用意し、十分に離れた先での
  1手あたりの送りが**種によらず同じ値へ落ち着く**こと。**示せなければ、出てきた数字は採用しない。**
  （**新しい測り方を作らない。** `docs/measurement-procedures.md`）
- **組み点が上がらない。** 016 は糸が鏡の上に溜まって組み点が停止した。022 で同じことが起きたら、
  **直しにいく前に止めて報告する。** 前提を足して押し切らない。

---

## 2. 台の幾何

### 値は作者の決定である（本には無い）

**丸台に厳密な規格は無い。** `docs/architecture.md`「**丸台の寸法と錘（作者の決定）**」が正本で
ある。**本から測った値ではないので、「本の値」として引かない。** 参考写真は
`.build/task022-references/`（git の管理外）。

| 量 | 実寸 | d = 2 mm のとき |
|---|---|---|
| 台の高さ | 40 cm | 200 d |
| 鏡（円板）の直径 | 25 cm | 半径 **62.5 d** |
| 中央の穴の直径 | 3 cm | 半径 **7.5 d** |
| 台板 | 30 cm 四方 × 厚さ 2 cm、脚 4 本 | **物理には要らない**（糸に触れない） |

- **鏡の上面は平面として扱う。** 実物は穴の周りが皿状に窪むが、省く（作者の決定）。
- **穴の縁と鏡の外縁は丸める。丸めの半径は解法の設定として記録する**（模型の定数ではない）。
- **糸の直径 d の実寸だけは本に無い。仮置き 2 mm。** **d を変えても模様と上下が変わらないことを、
  感度の確認に含める。**
- **Task 016 の試作の 14 d / 6 d は持ち越さない**（実物の 1/5 以下だった）。

### 位置（32 の角度）

- **丸台にノッチは無い。** 玉の重さで糸が鏡の縁に掛かることで位置が保たれる。
  **位置は 32 の角度（bookC の座標）として鏡の縁に取る**（11.25°ごと）。
- **番号は bookC のディスクのもの**（1〜32）。`Scripts/task021/braid_geometry.py` の
  `DISK_TO_STAND` が、休むノッチ16個を台の位置1〜16へ写している。
- **休むノッチは8組（隣り合う2ノッチ）、組の間は3ノッチ。この8組は丸源氏の閉じの対と一致する**
  （021 の確定事項）。**これは仮置きではない。表から出ている。**
- **角度が増える向き（時計回りか反時計回りか）は決めない。** これは **丸源氏の E/W の列順の
  未決事項そのもの**である。**022 は両方を回し、鏡像として扱う。今回の作業で決まったように
  書かない。**

### 寸法は合わせる対象ではない

**作者の方針**（021c の判定）により、**太さとピッチを物理で追い込まない。** 台の寸法は
**結果（模様・上下）がそれに鈍感であることを確かめる対象**であって、合わせる対象ではない。
判定は合格条件 (d) にある。

---

## 3. 糸

### 糸

- **直径 d の剛体カプセルを関節で繋いだ鎖。** 長さは、玉が鏡の縁から垂れ、組み点を通り、
  穴を下って結び目に届くまで**十分に長く**取る。**途中で継ぎ足さない**（016 の `feed()` は無い）。
- **摩擦と反発は engine の既定値を使う。** **値を読み取って記録し、模型の定数ではないと書き分ける。**
  **合わせるために動かさない。**

### 張力は質量と重力で与える（**作者の決定**）

**玉と吊りおもりを質量のある剛体としてぶら下げる。力を直接加える方式は取らない。**
**玉が鏡の縁を越えて垂れれば、外向き下方の張力は幾何の帰結になる。** 向きを手で与えない。

  吊りおもり（中央）  板おもり 190 g／枚
  玉（糸の末端）      240 g と 100 g がある

**このプロジェクトの既定と感度の確認**（`docs/architecture.md`「丸台の寸法と錘」）

  16 本組（既定）      玉 100 g × 16、吊りおもり 190 g × 3   比 **2.8:1**
  16 本組（感度）      玉 100 g × 16、吊りおもり 190 g × 4   比 **2.1:1**
  8 本組              玉 240 g × 8、 吊りおもり 190 g × 4

**玉の合計と吊りおもりの比 2:1〜3:1 が標準である。** 長さの単位は d ひとつ。

### 組み始め（種）は実際の組み始めと同じにする（**作者の決定**）

**糸の端を一点に結び**（結び目は小さな剛体で代用してよい）、**そこに吊りおもりを掛けて穴の下へ
垂らし、糸を 32 の角度へ放射状に置いて玉を縁に掛ける。** **実際の組み始めと同じであり、
こちらが決める寸法は結び目以外に無い。**

- **組み始めの数サイクルは実物でも乱れる。端の除外区間とする。**
- **これで組めなければ、別の種に黙って切り替えない。組めなかった様子を報告して止まる。**
- **016 の「種2つで同じ値へ落ち着くか」の判定はそのまま使う。**
  **種は、結び目の位置と、糸の初期のたるみを変えた2通り。**

---

## 4. 手の動き

- **bookC の1手ごとに**、指定の糸の玉を持ち上げ、鏡の上を運び、目的のノッチへ置く。
  **1手＝1本**（`docs/architecture.md`「正本の読み方」）。**行を左から右へ、行を上から下へ。**
- **運ぶ高さは他の糸の上**（鏡の面より上）。**これが上下を決める唯一の仕組みである。**
  後から置いた糸が、途中の糸の上を通る。**別の上下規則を置かない。**
- **閉じ移動（詰め直し）も同じ**に運ぶ。1周期は平源氏 24手（組み12＋閉じ12）、
  丸源氏 24手（組み8＋閉じ16）。
- **1手ごとに落ち着くまで待ってから次へ。** 待ち方（運ぶ時間、待つ時間、静止と見なす閾値）は
  **解法の設定**であって模型の定数ではない。**固定して記録する。**
- 玉の運びは運動学的（手が動かす）。**運んでいるあいだも糸は張力を受けている。**

---

## 5. Engine

- **Jolt Physics。** C++ の小さな harness を **`Scripts/task022/` に CMake で置く**
  （追跡ファイル。`prototypes/` は未追跡のまま触らない）。
- **版を固定して記録する。** CMake の `FetchContent` で**タグとコミットハッシュを固定**し、
  この文書にも書く。**022-1 で取得したときに追記する。**
- **決定性**: 単一スレッドのジョブシステム、固定の時間刻み、固定のシード。**同じ入力で同じ
  出力になることを確かめて記録する。**
- **出力**: **各手の後に全カプセルの中心の位置を書き出す。** 形式は 016 の `.xyz` にならい、
  1行 `hand thread bead x y z` の平文＋設定を書いた見出し。
- **判定は 021 の Python の道具で行う**（`Scripts/task021/`：占有履歴、面の見え、反転の検出、
  図）。道具は `(p, thread_of, laid_in)` の配列を読むので、**`Scripts/task022/` に読み替えだけの
  薄い adaptor を置く。** **021 の道具の意味を書き換えない。**
- **座標系**: 軸を z、鏡を z = 0、紐は −z へ伸びる。**021 の道具が軸 = z を仮定しているので、
  adaptor 側で合わせる**（道具を直さない）。
- **後で Swift から C++ interop で呼ぶ前提**: harness は**中核（台の幾何・糸・手・落ち着かせ）を
  1つの薄い C++ の入口**にまとめ、CLI と書き出しはその外に置く。**大域状態を持たない。**
  Objective-C や Swift 固有のものを混ぜない。**022-4 はこの入口を Swift から呼ぶだけになること。**
- **build の手順はこの文書に書く**（022-1 で実際に通してから追記する。**今は書いていない**）。
- **組み方の名前で分岐しない。** 名前は入力の選択（`hira` / `maru`）としてだけ現れ、
  **物理と手の運びは表だけを読む。** `Kumihimo/Domain/Braiding/` には何も足さないので
  番人 script の対象は変わらないが、**同じ規律を harness にも課す。**

### 022-1 で決めて記録すること（Jolt の呼び方。今は決めていない）

- 鎖の関節の種類（点拘束か、円錐＋捻りか）。**曲げ剛性を入れるかどうかと、その理由。**
- 鏡（穴のある平面と丸めた縁）をどの形状で表すか。
- 内部エッジの扱い、スリープ、接触の許容量。
- 摩擦・反発の既定値（**ソースから読み取った値をそのまま書く**）。

---

## 6. 合格条件（**前もって書く。後から緩めない**）

- **(a) 組める。** 束が締まり、糸がほどけず、貫入が無い。
  **組み点が上がり続けること**（016 が到達できなかった点）。
- **(b) 上下が順序と一致する。** 各交差について「後から置いた方が上」と一致する。
  **021 の反転検出を流用し、反転は 0。**
- **(c) 面の見えが占有履歴と一致する。**
  - **丸源氏 32/32**（`Scripts/task021/occupancy.py` と同じ突き合わせ。
    **鏡像は一致扱い。E/W の列順は未決のまま**）。
  - **平源氏 bookA p97 の3実験**（横糸だけに色→本体に色0升／矢絣／はしごが表裏で反転）。
- **(d) 鈍感である。** 台の寸法（鏡の半径・穴の半径・縁の丸め）と張力（玉・吊りおもり）を
  常識的な範囲で振っても **(a)〜(c) が変わらない。**

**不合格なら止まる。定数を足して合わせにいかない。前提を足さない。**

### 記録（**合否を付けない。予言との突き合わせ**）

- **1サイクルの長さ。** 積み重ね模型の **3d**、平源氏の実測 **ピッチ÷紐幅 0.3665**、
  丸源氏の**山形の密度 1.8〜2.15** と並べる。**合わせにいかない。**
- **傾きの向き**と **Task 004 の周期2**（菱形の傾き）。021 の予言——
  **列の偶奇で交互になる**——と並べる。
- **平源氏の膨らみ**と **畝の高さ 0.45**。
- **断面図**（両方の組）。

---

## 7. 段（**各段の終わりで止まって報告する**）

  **022-1**       この指示書。016 の再読。Jolt の取得と build が通ること。harness の骨組み。
                  **種の状態（結び目に吊りおもり、糸が放射状、玉が縁に掛かる）が静止すること
                  まで。組むのは 022-2 から。** 静止の様子を図にして作者が見る
                  （`.build/task022-figures/`）。
  **022-2**        **平源氏を1サイクル組んで図を出す**（作者が見る）。種2つの試験も行う。
  **022-3**        **合格条件の判定**（平源氏・丸源氏）。
  **022-4**        **Swift への接続**（Task 020 段階3 に相当）。

**一度に複数の段をやらない。承認された設計が破綻したら、代替を作らずに止めて報告する。**

## 8. 引き継ぐもの・捨てるもの

**引き継ぐ**

- **32ノッチ**（休むノッチ8組、組の間は3ノッチ、丸源氏の閉じの対と一致）。
- **bookC の1手1本の順序**（行を左から右へ、行を上から下へ）と `BraidMethodCatalog`。
- **張力の前提**（`docs/architecture.md`「組み台の力学」）。
- **カプセル**（線分どうしの最短距離で当てる。球だけだと隣の糸のくぼみにはまる。021 で
  R=3d の1ケースが 2.9e-1 対 1.8e-3 と決定的に違った）。
- **判定の道具**（`Scripts/task021/`：占有履歴、面の見え、Task 004・p97 との升単位の突き合わせ、
  反転の検出、図）。
- **予言**（積み重ね模型 k=3・pitch 0.375・到着位相、占有履歴の面、傾きの周期2）。

**捨てる**

- **置く高さの模型（021b〜021f）。** 高さを勘定で決める道。**作者の判断により不要。**
- **R を長さとして課すこと**（021d）と **R の二分探索**（境目が無い）。
- **完成した紐の平衡から送りを出す道**（016 試作2。原理的に答えが無い）。
- **糸を張力で継ぎ足す実装**（016 `feed()`）。

## 守ること

- **規則を足さない。** 上下は運ぶ順序だけで決まる。別の上下規則を置かない。
- **見た目に合わせて定数を決めない。** 測った値は比較対象であって入力ではない。
- **承認された設計が破綻したら、代替を作らずに止めて報告する。**
- `git add -A` を使わない。**docs / code / Scripts のコミットを分ける。**
- **product code は変更しない**（022-4 まで）。`Kumihimo/Domain/Braiding/` に組み方の名前を
  入れない（`sh Scripts/check-braiding-is-general.sh`）。
- **積3**（`BraidShape.pitchPerStep` の `.derived` 化）は**保留のまま。**
- **凍結中の2生成器**（`MaruGenjiSurfaceMeshGenerator` / `HiraGenjiSurfaceMeshGenerator`）は
  **022-4 が描けるようになった時点で退役。** それまで1頂点も変えない。
- テストは `AGENTS.md`「テストの回し方」に従う。**Task 018 の打ち切りが起きたら明記し、
  「テストは通った」と書かない。**

## 作者の回答（2026-09-07）— 4点は決まった

1. **張力は質量と重力で与える。** 玉と吊りおもりを剛体としてぶら下げる。**力を直接加える方式は
   取らない。** → 上記 3.
2. **種は (a)。** 実際の組み始めと同じにする。**(a) で組めなければ (b) に黙って切り替えず、
   組めなかった様子を報告して止まる。** → 上記 3.
3. **Task 016 の枝はマージしない。** 現在の枝の `docs/tasks/016-*.md` の**状態行だけ**を直す。
   → 実施した（下記）。
4. **022-1 の残りへ進んでよい。** 止まったのは指示の食い違いであり、読みは正しかった。

