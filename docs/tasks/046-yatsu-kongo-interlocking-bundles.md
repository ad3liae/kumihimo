# Task 046: 八つ金剛の糸束の腹と、隣の束への差し込みを描く

**2026-09-19追加:** 045/046はPR #29でマージ済み（`b8f3e0d`）。その後、作者が
「半ピッチずつずれるため螺旋になる。1ピッチずれることはない」と束の配置の誤りを指摘した。
この新しい問題は [Task 048](048-yatsu-kongo-half-pitch-placement.md) で修正する。
以下の外観受領・追加調整終了・到着位相固定は当時の範囲の記録であり、048の修正を止める条件ではない。

- 状態: **現行E2を受領・今回の外観調整を終了、PR作成・マージへ**（2026-09-19、作者の方針とCodexの審査）。審査対象 `2205b6d`。残る差は末尾に申し送る。PRとマージの実行指示は [045-046-pr-handoff.md](045-046-pr-handoff.md)。**この記録時点では未マージ。**
- 実装担当: Claude Code。Codexはタスク作成と、差分・比較画像・報告の審査を行い、ソースを変更しない。
- 前提: Task 045の初回改善を受領、追補1・2は完了。審査した基準は `3a48b70`。実際の開始コミットと既存変更を記録する。
- 目的: **斜めの粒が列に並ぶ印象を減らし、幅のある糸束が隣の下へ入り込む組み目へ近づける。**
- 対象: 筒8本の描き手（八つ金剛S・Z）の局所形状と、それに追随するカード・素材座標・検証。

## 1. 読むものと、今回見直す仮定

`AGENTS.md` に従い product → architecture → `braid-preset-expansion.md`・`project-editor.md` → 本書の順に読む。
関連する決定は Task 045とその追補、測定は `docs/measurement-procedures.md` 6・7。
Task 025の現在の方針は「形は族の描き手、色は占有履歴」。Task 036〜044の物理研究は中止済み。
古い文書の実行命令・承認待ち・調整打ち切りを今回の命令として再実行しない。045から続く、作者が求めた見た目の改善である。

現行は、幅・高さが肩の一か所で最大となり、そこから先端まで細くなる糸束を、直線の中心線に沿って並べている。
最大幅は1列に固定され、覆えない所には軸に平行な升の面を暗く敷いている。
斜めにはなったが、保存画像では束の隙間の平面と、同じ列に並ぶ粒の輪郭がまだ目立つ。

**今回は「占有する升の幅」と「見えている糸束が覆う幅」を同じにする描画上の仮定を見直してよい。**
占有履歴はそのまま保ち、その糸に属する見える面だけを調整する。隣の列へ張り出す場合も元の糸IDを保持する。
最大半幅0.5列、肩・幅・高さの変化を結びつけた曲線、直線だけの中心線は再検討の対象である。
これらを変えるなら、写真に合わせた `.declared` の値・形と明記し、手順から一意に導いたことにしない。

維持するもの:

- 手順表、糸ID、占有履歴、到着位相、サイクル数、S/Zの向き。
- `pitchOverDiameter = 0.403`、名目の紐径、共通照明・露出・カメラ。SとZで別々に帳尻を合わせない。
- 丸源氏・平源氏の形と素材、保存形式、UIの操作。
- 045追補で揃えたカードの向き、見える区間の選択、最新配色だけを表示する仕組み。
- Task 035の未完了。対の中の先後や写真の組み点側を未確認のまま確定しない。

## 2. 046-0: 写真と基準の露出区間を、同じ方法で比べる

資料:

- `.build/task031-photos/bookA-p8-9-zoom-b-a-S.png` — 左からZ-b、Z-a、S。Sを主比較、Z-aを向きと形の確認に使う。
- `.build/task031-photos/bookA-p54-55-yatsu-kongo.png` — 配色と手順の参照。
- `.build/task045/sheets/{s,z}-plain-front.png`、`{s,z}-book-front.png` — 実物とA/B/Cの比較。Cの立体形状が現在の基準と同じ。
- `.build/task045/review-fixes/` — 向きを直したカードと立体の比較。

新しいモデルを作る前に、SとZ-aそれぞれ、輪郭を識別できる**少数の露出区間（目安3〜6か所）**を選ぶ。
元画像の座標、選んだ輪郭、中心線の読み、紐の軸と輪郭幅を注記して残す。
同色の別の糸が続いて見える所や、下に隠れた端は無理にひとつの区間として結ばない。
読めない部分は未確定とし、標本数を満たすために境界を作らない。

写真と基準レンダーを、輪郭幅を揃えた一様な拡大縮小で並べ、次を同じ定義で記録する:

- 露出区間の見かけの長さ／紐幅、幅／紐幅、長さ／幅。長軸と直交する幅を使い、画面の横幅と混同しない。
- 糸束の長軸の傾き。色帯の角、格子の歩幅、細い糸筋の角と分ける。
- 端が消える位置と、その先に見える隣の束。写真から隠れた内部経路や深さを決めない。
- 縁の起伏は別に目視で比較する。そこから谷の深さを逆算しない。

今回は**投影された見えの比較**を行う。写真の縁は周方向が縮むため、主寸法は中央付近の読みやすい区間で比べ、周辺は別欄にする。
少数標本の幅・角度を真の3D寸法や全周の代表値に読み替えない。レンダーは正面だけでなく既存の回転画像からも同じ位置帯を見て、向きによる幅を示す。
以前の36°を糸束の角に使わず、未計測の「1.5〜2サイクル」から目標長さを作らない。
写真とモデルで厳密に対応しない場合は、読みの範囲を記録して描画案の選択に使う。測定研究だけを続けない。

## 3. 046-1: 下地の露出と粒の輪郭を、形から減らす

**推奨する最初の案は、幅のある腹と、すぼまって隠れる先端を分けて作ること。**
現在の「肩でだけ最大幅、その後はずっと細くする」という形から、隣の束との間を覆う腹を保ち、先端側で幅と高さを落とす形を比較する。
まず頂の高さを保って幅の配分と潜り込みを比べる。高さや断面の変更も必要なら、幅だけの変更から効果を分けて示す。
必要に応じて端寄りの中心線を少し曲げ、先端が隣の束の縁に隠れる配置を試してよい。
単に長さだけを伸ばす、全体を太らせる、全ての糸束を平らにして隙間を埋めるだけで終えない。

具体式は小さく検証できるものをClaude Codeが選ぶ。最初は数周期の局所形状で比較し、**基準＋最大2つの形の案**を残す。
数値の総当たりや無制限の探索はしない。少数の意味と単位を持つ値で表し、採用理由を写真の読みと結びつける。
旧値が試験で固定されていても、見直した描画上の仮定なら理由を記録して更新できる。

満たすこと:

1. 単色・同じ照明でも、粒の四辺が独立して見える印象が弱まり、隣に覆われる端が読める。
2. 8色で、どの区間がどの糸に属し、どの別の区間に覆われるかが分かる。同色を結合条件にしない。
3. **端は、別の糸束の下に隠れてから終わる。** 露出した空間で一点まで細くなって終わる形や、平らな升の上へ単に載せた形を改善する。
   全糸の内部経路を再構築する必要はないが、比較する代表箇所で覆う側と覆われる側を特定する。
4. 目立つ三角形・帯状の平らな下地の露出を減らす。閉じた筒のために下地を残すことは可能だが、その面積を減らさず暗くするだけの変更は採らない。
5. 背景の穴、見える端面、面のちらつき、周期境界の段差を出さない。輪郭を基準よりさらに平らにして隙間を隠さない。

下地の見える割合は、レンダーの暗い画素数ではなく、**実メッシュの外側に当たった面が糸束か下地か**で基準と候補を比較する。
045の実メッシュ照合を必要な範囲で利用できる。全周1リピートの同じサンプルと、代表的な局所画像を対応づける。
写真の暗部をそのまま下地と数えたり、根拠なく露出率0を目標にしたりしない。露出率だけ下がって束の形が悪化する候補は採らない。

ここで改善するのは、完成外観のための形である。物理的な接触・張力・体積保存の解法、Task 036〜044の再実行は加えない。
現行の手順情報では決められない先後が必須になった場合は、その箇所と候補を示す。情報を捏造してTask 035を閉じない。

## 4. 046-2: 一覧と素材を追随させ、比較可能な状態で提出する

採用候補は詳細と一覧の両方へ接続する。可視区間を選ぶ規則は共通に保ち、カードだけ輪郭を到着順に上塗りする方式へ戻さない。
形の値を増やしたら、カード画像のキャッシュキーにも反映する。画像更新ローダーを別のものに置き換えない。

最初の形の比較では、色・照明・接触影・糸筋の密度を固定する。
糸筋や影の座標が新しい形に追随するための修正は行い、陰影の追加調整が必要なら形だけの比較と別に残す。
絹の艶、毛羽、ランダムな凹凸、共通照明の変更は今回の対象外。

`.build/task046/` に次を保存する:

- E0: 開始コミットの形と素材。045のC/D1と同じなら既存画像を再利用できるが、対応するコミットと条件を記録する。
- E1: 腹・先端を見直した第一候補、素材設定は基準と共通。
- E2: 必要な場合だけ、第一候補と違いが説明できる第二候補。比較で劣れば採用しない。
- final: 採用候補。形の比較後に素材座標や影を調整した場合はその差も明記する。

S/Z × 生成り・本の配色・8色について、通常表示と拡大、カードと詳細を残す。
立体は正面・30°・60°・90°・180°・270°で見比べ、一周回して継ぎ目を確認する。
写真との並置は輪郭幅を揃えた一様な拡大縮小だけとし、長手だけ伸ばしたり、鏡像にしたりしない。
注記のない元画像と、読みを示した画像を両方残す。比較専用の切替や技術的な注記を本番UIに追加しない。

## 5. 検証と合否

関連する試験を先に、ユニット全件は最後に1回。現在の `AGENTS.md` の実行条件を守る。
コマンド上限300000ms、試験単位の既定60秒、非並列、起動済みの同じUDID、結果バンドル、1実行1待機、UIテスト一式は除外。
`DRAW_SHEETS` 等を使った場合は必ず解除する。名前の番人、16本のメッシュ不変性を確認する。
ソースの一時変更と試験待機・復元を1コマンドに連結しない。画像ローダーの否定試験は再度行う必要はなく、通常の回帰試験で維持する。

形の変更に合わせて検証するもの:

- 糸ID・色の帰属、S/Zの鏡の対応、外向きの面、有限な法線、周方向と長手周期の継ぎ目。
- 新しい腹・先端の代表箇所で、実メッシュから覆う糸と覆われる糸を確認する。頂だけで全幅の先後を断定しない。
- カードの画素の所属と実メッシュの外側の所属。変更前後の不一致数と境界差の範囲を記録する。
  045で許容した除外帯を機械的に広げず、形が変わっても理由が成り立つか再確認する。
- 下地の露出割合と分布、写真と同じ測り方による露出区間の寸法。ハッシュ更新だけで外観を合格にしない。
- 頂点・素材数、形の生成時間とカード生成時間。iPadとiPhoneの実際の一覧・配色変更・詳細・回転・拡大・リセットを確認し、
  ライト／ダーク、標準／大きな文字の実施範囲と未確認範囲を明記する。

合格の中心は、**同じ表示条件で、平らな下地が目立たなくなり、幅を持つ糸束の先が隣の下へ差し込んで読めること**。
写真に対する細長さ・傾き・輪郭の起伏の改善と悪化を、測定の限界も含めて報告する。
一方向や一配色だけで成立するものを完成扱いにしない。露出率低下やテスト成功だけで実物らしさを断定しない。

通常は測定→局所候補→製品への接続→検証まで進め、段階ごとの承認待ちは挟まない。
最大2案とも改善にならない場合は、基準を悪化した案へ置き換えず、画像・値・失敗箇所を揃えて報告する。
この場合も物理研究や無制限の探索へ広げない。

## 6. 完了報告

開始・提出コミット、主要変更ファイル、写真の注記と読みの範囲、E0/E1/E2/finalの比較、採用理由と悪化点、
形の値の意味・単位・出典、可視面の整合、下地露出の変化、検証結果・所要時間・性能・手動確認と未確認範囲を本書に追記する。
コマンド上限到達・手動停止・診断収集・試験タイムアウトは分けて報告し、未完了の実行を成功にまとめない。
重要な数と判定根拠は `.build/` だけでなく本書に残す。

提出状態は**「実装済み・審査待ち」**。Codexが差分と描画結果を審査し、最終的な見た目の採否は作者の判断を尊重する。
Task 035は別の未完了事項として残す。

## 実装結果（2026-09-19、Claude Code）

状態: **実装済み・審査待ち。**採否（見た目）は作者と Codex の判断に委ねる。

### 1. コミットと主要ファイル

- 開始: `3a48b70` の上に、着手時の未コミット変更（Codex の文書: `docs/README.md`、045 の2文書、本書）を `4850cb5` として入れた。**E0 はこの `4850cb5`**。
  `prototypes/` と `Claude outputs/` は触っていない。
- `cb9c65b` 形の変更（採用案 = E2）。本書と `docs/README.md` は最後のコミット。

| ファイル | 変えたこと |
| --- | --- |
| `Kumihimo/Domain/RoundTube8SurfacePattern.swift` | `RoundTube8Bundle` の形を**レンズ**に: 到着で尖り、腹で最も太く、先端へすぼまる。肩（`shoulderCycles`）をやめ、`bellyStartCycles`・`bellyEndCycles`・`widestHalfWidthInColumns` を足した |
| `Kumihimo/Features/BraidSimulation/RoundTube8SurfaceMesh.swift` | 入力の検査と `shape` の宣言値（下の表）。頂点の数と作り方は同じ |
| `Kumihimo/Features/BraidSimulation/RoundTube8StrandTexture.swift` | 接触の陰の位置を新しい形へ追随（頭は腹までの立ち上がり、尾は次の糸が腹の半ばから腹に達するまでに潜る）。深さ・幅の値は増えていない |
| `Kumihimo/Features/BraidSimulation/RoundTube8ThumbnailView.swift` | カード画像のキャッシュキーに形の値を加えた。ローダーはそのまま |
| `KumihimoTests/RoundTube8CardAgreesWithSolidTests.swift` | `theFloorHardlyShows`・`aRunEndsBeneathAnotherRun` を追加 |
| `KumihimoTests/RoundTube8SurfaceTests.swift` | 肩の試験を腹へ、ハッシュ、宣言値の一覧 |
| `Scripts/task046/readings.py`（新規）、`Scripts/task045/compose.py`（`TASK045_ROOT` で出力先を選べるように） | 写真の読みと比較画像 |

### 2. 046-0: 露出区間の読み（写真と基準を同じ方法で）

`Scripts/task046/readings.py`。**点は全て目で置いた**（3倍の切り抜きに画素の目盛りを付けたもの）。各区間の長軸の両端（輪郭が見えて終わる所、または別の束の下へ消える所）と、
中央の横断（紐の軸に直角）を読み、幅は長軸の傾きで直角に直した。紐幅は写真 S 235 px・Z-a 241 px（測り方6の `body`）、レンダーは画面の輪郭幅。
注記つき・注記なしの切り抜きは `.build/task046/readings/`。

| 区間 | 長さ/紐幅 | 幅/紐幅 | 長さ/幅 | 長軸の傾き（大きさ） | 注 |
| --- | --- | --- | --- | --- | --- |
| 写真 S-1（中央の黄） | 0.69 | 0.30 | 2.3 | 5° | 元画像 (2358,1372)–(2337,1533) |
| 写真 S-2（中央の右の黄） | 0.66 | 0.30 | 2.2 | 1° | 周方向が縮んで見える位置（別欄扱い） |
| 写真 Z-a-1（中央の黄） | 0.88 | 0.36 | 2.5 | 14° | **同じ黄の2区間が続いている可能性**がある |
| 写真 Z-a-2 | 0.86 | 0.34 | 2.5 | 8° | 同上 |
| E0 S-1・S-2 | 0.48〜0.49 | 0.22 | 2.2〜2.3 | 21〜24° | |
| **E2 S-1・S-2（採用）** | **0.68〜0.69** | **0.25〜0.28** | 2.4〜2.8 | 23〜25° | |

- 傾きは**大きさだけ**を比べる（写真は紐を縦、レンダーは横に置いており、符号の取り方が揃っていない。向きが S/Z で写真と同じ側であることは回した並置で目で確かめた）。
- 写真で読めたのは S で2区間、Z-a で2区間（最小の目安3に届かない）。**境界が読めない所に区間を作っていない。**写真の端に近い区間は周方向の縮みで幅が小さく出る。
- **読めたこと**: 写真の見える区間は紐幅の約 0.7（S）、幅は約 0.3。E0 はどちらも小さかった（0.49、0.22）。**E2 は長さが写真 S と揃い（0.69）、幅はまだ細い（0.25〜0.28 対 0.30）。**
- **読めなかったこと・違うこと**: 写真の区間の傾きは 1〜14°で、描いた 21〜25°よりずっと小さい（写真の豆は紐の軸にほぼ沿って見える）。**傾き 0.35 は据え置いた**（下の 5）。
  写真の縁の起伏は目視で、写真のほうが大きい（豆の腹が縁で盛り上がる）。

### 3. 案と比較（`.build/task046/`）

最初に Python の簡易描画（高さ場の最大、`.build/task046/proto/`）で形を比べ、アプリへは2案だけ入れた。**数値の総当たりはしていない**（試作で見た組み合わせは約 10）。

| 状態 | 形 | 下地の見える割合（実メッシュ） | 所見 |
| --- | --- | --- | --- |
| **E0**（`4850cb5`） | 肩（到着後 0.3）で最大、先端まで細くなる。最大半幅 0.5 列 | **3.64%**（S・Z とも） | 束の間に三角の暗い下地、粒の頭が四角く並ぶ |
| **E1** | レンズ: 到着で尖り、腹 0.6〜0.9 サイクル、先端で尖る。**半幅 0.5 列（E0 と同じ）** | **1.78%** | 粒の四角い頭が消え、束が斜めに流れる。腹の間になお隙間 |
| **E2 = final（採用）** | E1 の腹の半幅を **0.55 列**に | **0.32%** | 隙間がほぼ消え、先端が隣の束の間に入って見える。全体の太さは目視で変わらない |

- 試作で「幅だけを広げる（肩の形のまま）」「長く伸ばす」「中心線を曲げる」も見たが、前2つは四角い煉瓦になり、曲げは効果が小さかったので入れていない。
- **素材・照明・露出・糸筋の密度は E0〜E2 で共通**。接触の陰の位置は形に追随させた（E1 と E2 に同じく入っている。形の比較と別の陰の調整はしていない）。
- 画像: `E0/`（045 の C と D1 の画像を写した。対応は `E0/SOURCE.txt`）、`E1/`、`E2/` に S/Z × 生成り・本の配色・8色 × 0/30/60/90/180/270° とカード。
  並置 `sheets/{s,z}-{配色}-front.png`（写真・E0・E1・E2、紐幅を 200 px に揃えた一様な縮尺のみ）、`-front-zoom.png`、`{s,z}-{配色}-{E0,E1,E2}-turns.png`、`record.txt`。

**改善した点（目視と数）**: 粒の四辺が独立して見える印象が弱まり、束が斜めに流れて先端が別の束の下に消える（単色でも）。平らな下地は 3.64% → 0.32%。
露出区間の長さが写真 S に揃った（0.49 → 0.69）。一周（6方向）で穴・継ぎ目・端面は見えない（`sheets/*-E2-turns.png`）。

**悪化・残る差**: 傾きは写真より大きいまま（上の表）。区間が細く長くなったぶん、**写真の豆の「ふっくらした腹」より平たく見える**。輪郭の起伏は写真より小さい（E0 よりわずかに滑らか）。
接触の陰は写真の割れ目ほど暗くない。

### 4. 形の値

| 名前（`shape` の鍵） | 値 | 単位 | 出典 |
| --- | --- | --- | --- |
| `a run's lean, in columns per cycle` | 0.35（据え置き） | 列/サイクル | `.declared`（045）。向きは手順表から |
| `how far a run goes on beneath the next thread, in cycles` | 0.9（据え置き） | サイクル | `.declared`（045） |
| `where a run's belly begins, in cycles past its arrival` | **0.6**（新） | サイクル | `.declared`、写真に目で合わせた |
| `where a run's belly ends, in cycles past its arrival` | **0.9**（新） | サイクル | `.declared` |
| `a run's widest half-width, in columns` | **0.55**（新） | 列 | `.declared`。**糸の占める列（0.5、導出）より広く見せてよい、という仮定の見直し**（本書 1）。0.5 に戻すと自動的に `.derived` の扱いに戻る |

形の関数（立ち上がりは四分の一サイン、下りは四分の一コサイン、高さは立ち上がりの平方根）も写真に合わせた描画上の選択で、手順から導いたものではない。
**占有履歴・糸 ID・到着位相・ピッチ・S/Z は変えていない。**隣の列へ張り出した部分も、元の糸の ID と色のまま。

### 5. 可視面の整合と、覆う・覆われる

- カードと立体は045追補の同じ規則（最も高く立つ糸束）を使う。**カードの画素と実メッシュの外側の所属は、除外帯を含めずに数えて S 116/16,896（0.69%）、Z 90/16,896（0.53%）が不一致**
  （E0 では同じ数え方で S 32・Z 28。一時的な試験で数え、コミットしていない）。除外帯（045で決めた条件: メッシュの標本点での高さ差 3% 以内・谷底から 3% 以内・幅の最後の標本の外・到着後2つ目の標本より前）
  **を広げずに** `theCardShowsWhatTheSolidShowsAcrossARepeat` は S・Z とも通った（不一致0）。レンズの尖った両端は標本点の間で形が曲がるので、帯に入る点が増えたとみられる。
- `aRunEndsBeneathAnotherRun`: 実メッシュで、各糸束の先端近く（先端から 0.2 と 0.05 サイクル）の頂で外側に見えるのは**別の糸束**で、下地ではない（S・Z）。
- `aRunSinksBeneathTheNextThreadAtItsPlace`: 同じ場所の次の糸が腹に達したあとは、前の糸の頂より次の糸が高い（頂だけの主張。脇や全幅の先後は主張しない）。
- `theFloorHardlyShows`: 1リピート全体で下地が外側に見える割合 1% 未満（実測 0.32%）を番人にした。**0 を目標にはしていない。**
- 045 からの変化: 先端は次の糸の下だけでなく、**傾いて隣から入ってくる糸束の下**でも終わる。045 の「後着がその場所で上」は頂の上でだけ言え、それ以外は交わる線の描画上の近似である（変わらない）。

### 6. 検証

- 関係する試験を先に（`RoundTube8SurfaceTests`・`RoundTube8CardAgreesWithSolidTests`・`RoundTube8CardLoaderTests`・`BraidOrientationTests`・`BraidMeshHashTests`）: 全て成功
  （`.build/test-results/task046-final-related.xcresult`。同じ実行に入れた一時試験1件は意図的に落として数を読んだもの）。
- **全件を最後に1回**: `xcodebuild test-without-building -only-testing:KumihimoTests -parallel-testing-enabled NO -test-timeouts-enabled YES -default-test-execution-time-allowance 60 -quiet
  -resultBundlePath .build/test-results/task046-all.xcresult`、iPad (10th generation) `440CB589-…`、Bash の上限 300000 ms。**425件中 421件成功・0件失敗・4件スキップ、試験ごとの打ち切り 0件、
  テスト段階 224 秒（呼び出し全体 225 秒）。**スキップは `DRAW_SHEETS` のときだけのシート4件（機体に置かれていないことを確認）。expected failure は既知の平源氏の1件。
- 番人通過、ビルド警告なし、**丸源氏・平源氏のハッシュ不変**。八つ金剛のハッシュ `0xfbb4_48c0_8f4a_7fdd` → `0x94ae_7327_2c10_992d`（頂点数 36,480 は不変）。UI テスト一式は実行していない。
- **途中の実行記録**（045 追補2で決めた書き分け）:

| 実行 | Bash の上限 | 起きたこと | 終わり方 | 試験ごとの打ち切り |
| --- | --- | --- | --- | --- |
| E0 の露出（別の作業ツリー） | 300000 | **上限到達**で待ち受けから外れた。一時試験を意図的に落とした後の診断収集 | 停止せず自然に終了（結果は読めた） | 0 |
| E2 の露出と関係試験 | 300000 | **上限到達**。`simctl diagnose` を実行中だった（ハッシュの失敗と一時試験の後） | 停止せず 621 秒で自然に終了 | 0 |
| 最終の関係試験 | 待ち受けはバックグラウンド | 640 秒（診断収集を含む） | 自然に終了 | 0 |
| E1 の露出と関係試験・全件 | 300000 | 上限内（60 秒、225 秒） | 自然に終了 | 0 |

  自分で止めた実行は無い。**意図的に落とす一時試験を入れると、そのたびに診断収集で数分延びる**ことが分かった。
- **性能**（Debug・シミュレータ、一時試験で1回ずつ測った。コミットしていない）: メッシュ生成 E0 93 ms、E2 96 ms。頂点・三角形の数は不変、素材の数も不変。
  カード画像の生成 E0 245 ms、E2 288 ms（キャッシュされ、メインスレッドの外）。
- **手で確かめたこと**: iPad（ライト・標準文字）で実際のエディタを8本にし、糸3を青に変えて一覧のカード → S の詳細（青は1本でカードと一致）→ 回転 → リセット。
  iPhone 16（ダーク・アクセシビリティ最大文字）で起動引数から Z の立体とカード（確認後にライト・標準へ戻し停止）。
  **確かめていない**: iPhone での実際の一覧→詳細の操作と拡大、iPad のダーク・大きな文字、実機、VoiceOver、横向き。

### 7. 未解決

- 写真に対して、**区間の傾きが大きい**（写真 1〜14°、描いたもの 21〜25°）。傾き 0.35 は色の対角線（升）とは別の値なので動かせるが、写真の区間を4つしか読めておらず、
  Z-a では2区間が続いて見えている可能性もあるため、今回は据え置いた。次に動かすなら、この読みを先に増やす。
- 区間の幅は写真より細い（0.25〜0.28 対 0.30）。腹の半幅をさらに広げると粒が四角く戻る傾向が試作で見えたので、止めた。
- 輪郭の起伏と、割れ目の暗さは写真に届いていない。
- Task 035 は未完了のまま。写真の組み点側も未確認のまま。

## 審査と今回の区切り（2026-09-19、Codex）

作者から「見た感じこれでもいい」「申し送り事項は書いてPR作成させてマージしてしまっていい」という方針が示された。
**現行E2を今回の成果として受領し、見た目の追加調整はここで止める。** これ以上の改善が不可能だと判断したのではなく、現在の用途と見えに対して一区切りにする判断である。
完全な実物再現や物理的な正しさの認定ではない。未解決の差を新たな着手必須タスクにせず、下記の申し送りとして残す。

### 確認した内容

- 審査対象は `4850cb5..2205b6d`、製品実装は `cb9c65b`。
  腹を保ち両端がすぼまる形、腹の半幅0.55列、陰影位置の追随、カードのキャッシュキーへの新しい形の値の追加を確認した。
  045追補2の画像ローダーは変更されていない。
- `.build/task046/sheets/s-plain-front.png`、`z-book-front.png`、`s-eight-E2-turns.png` を確認した。
  E0と比べて平らな下地が目立たなくなり、尖った端が隣に覆われる見えへ改善している。確認した6方向に明らかな穴・周期の継ぎ目は見当たらない。
- 下地露出の3.64%→0.32%は実装者の実メッシュ標本による報告値である。
  審査側は実メッシュを調べる `theFloorHardlyShows`（下地1%未満）と `aRunEndsBeneathAnotherRun` の内容・保存結果の成功を確認したが、割合を独立に再集計していない。
- `.build/test-results/task046-all.xcresult` を読み、**425件中421件成功・失敗0・スキップ4、224.220秒**を確認した。
  スキップは描画シート4件、既知の平源氏のexpected failure 1件は継続。
  カードと立体の照合、画像ローダー、丸源氏・平源氏のハッシュも成功している。
- 名前の番人と `git diff --check 4850cb5..2205b6d` は審査側で通過を確認した。
  全件ビルド・テストや手動操作は審査側で再実行していない。

**今回確認した範囲では、マージを止める新たな不具合は見つからなかった。** 残る外観差を受容し、045と046を同じPRでまとめる。
Codexは文書だけを変更した。PR作成・push・マージはClaude Codeが [引き継ぎ指示](045-046-pr-handoff.md) に従って実施する。

### 申し送り — 今回のマージを止めない事項

1. **実物との差**: 傾きは写真の少数の読みに比べ大きく、腹はやや平たい。輪郭の起伏、割れ目の暗さ、絹の質感にも差が残る。
   作者の今回の判断により追加調整は行わない。次に外観を見直す要望が出たときの候補とする。
2. **測定の限界**: 写真はS・Z-a各2区間の目視読み。Z-aは同色の2区間を続けて読んだ可能性がある。
   角度の符号は写真とレンダーで統一した数値比較になっておらず、方向は並置の目視確認に留まる。
   長さ約0.69対約0.67という近さを、全区間・全周で実物と一致した証拠にしない。再開時は読みと座標の取り決めを先に揃える。
3. **一覧と立体の境界差**: カードは滑らかな面、立体は三角形による面なので、標本上の所属不一致がS 116/16,896（0.69%）・Z 90/16,896（0.53%）残るという報告。
   045の除外条件を広げず試験は通るが、厳密一致とはしない。現状の表示精度として受容する。
4. **先端の試験の範囲**: 確認しているのは、タイル中央に収まる区間の、先端近く2か所の頂で別の糸束が外側に見えること。
   全幅・全経路・実物の交差順を証明してはいない。また `coveredBy.count >= 1` は同列・別列の両方が存在することまでは保証しない。
   「全ての端の全領域が正しい」「両方の覆い方を試験で保証済み」と説明しない。
5. **Task 035は未完了**: bookCとの手順・対の中の先後の照合、写真の組み点側は確定していない。形の値は描画用の `.declared` として扱う。
6. **未確認の操作条件**: 今回のiPhone実エディタでの一覧→詳細と拡大、iPadのダーク・大きな文字、実機、VoiceOver、横向き。
   製品全体のリリース検証で扱う。この外観PRのための追加修正要求にはしない。
7. **検証の運用**: 数を読むために試験を意図的に失敗させると診断収集で長引いた。今後の計数は通常の出力・結果ファイルで残し、失敗を出力手段にしない。
   今回の途中の上限到達と診断収集は上の実行表に残し、最終全件実行の成功と区別する。性能値は単発のDebug測定であり、確定的なベンチマークではない。

過去の指示に残る「実装済み・審査待ち」や追加比較の要求より、この区切りとPR引き継ぎを現在の方針とする。
