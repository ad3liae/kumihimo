# Task 045・046: 現行案を採用し、PR作成からマージまで完了する

- 状態: **Claude Codeへの実行指示**（2026-09-19）。作成時点ではPR作成・マージは未実行。
- 作者の方針: 現在の見た目で一区切りとし、申し送りを残してPR作成・マージへ進む。
- 実行担当: Claude Code。Codexは審査と文書を担当し、アプリ・試験・スクリプトのソースを変更していない。
- 対象リポジトリ: `ad3liae/kumihimo`。現在の作業ブランチは `claude/task-045-yatsu-kongo-appearance`。
- 審査済みの提出: `2205b6d`、最終の形の実装 `cb9c65b`。
- 審査時のローカル `main` と `origin/main` はともに `4c5fe71`。リモートの最新状態は実行時に確認する。

## 今回の判断

**現行E2を採用し、外観の追加調整は行わない。Task 045と046を1つのPRにまとめ、通常の確認を通してmainへマージする。**
PR作成とマージについて、段階ごとの追加承認待ちは挟まない。作者はこの方向を示している。
新たな重大な不具合、解消できない競合、リポジトリの必須承認・権限不足で進めない場合は、具体的な状態と必要事項を報告する。
保護ルールや失敗した必須チェックを回避してマージしない。

残る差と確認範囲は [Task 046の末尾](046-yatsu-kongo-interlocking-bundles.md) が申し送りの正本。
傾き・腹・輪郭・質感をさらに調整する新しいタスクは起こさない。Task 035や物理研究も今回の完了条件にしない。

## 実施手順

1. **文書を含めて提出を整える。** この引き継ぎ、Task 046の審査・申し送り、READMEの状態更新を確認してコミットする。
   作業ツリーに残る `prototypes/` と `Claude outputs/` は今回の対象外。`.build/` の画像・アプリ・結果バンドルを一括でコミットしない。
2. リモートの最新main、現在のブランチとの差分、同じブランチの既存PRの有無を確認する。
   差分は045・046とその文書・検証用の変更に収める。既存PRがあれば更新し、重複して作らない。
   現行ブランチをpushし、baseをmainにしたレビュー可能なPRを用意する。
3. 下のタイトル・本文を最終差分に合わせて使う。リポジトリのテンプレートがあればその項目も満たす。
   比較を添える場合はアプリ生成の変更前後画像を少数に絞る。ローカル画像のパスだけを、第三者が見られる画像として扱わない。
4. **必要な検証を確認する。** コードが審査済み状態のままで追記が文書だけなら、保存済みの全件結果を採用し、全件を理由なく再実行しない。
   main取り込みや競合解消で製品・試験コードが変わった場合は、その影響に応じた関連試験と最終の全件検証を実施する。
   実行条件は現在の `AGENTS.md`。通常の計数のために意図的な試験失敗を作らない。
5. PRの差分とチェック結果を確認する。必須チェック・必須レビューがある場合はその条件を満たす。
   チェックが設定されていない場合は、その事実と保存済み検証を区別して扱い、「CI成功」とは書かない。
6. **マージする。** 既存mainの履歴に合わせ、リポジトリで許可されていれば通常のmerge commitを使う。
   merge完了の状態とコミットを確認し、ローカルmainも他の未保存作業を壊さない方法で更新する。
   ブランチ削除や未追跡ディレクトリの掃除はこの指示に含めない。
7. 最後に、PRのURL、マージコミット、最終チェック結果、残る申し送り、実行できなかった事項を簡潔に報告する。
   作成だけ・自動マージ待ちを「マージ完了」と報告しない。リモートの実際の状態を確認する。

## PRタイトル案

八つ金剛の糸束形状と一覧・立体表示の整合を改善

## PR本文案

8本の八つ金剛S/Zが角丸の粒の列に見えていたため、幅のある腹とすぼまる端を持つ斜めの糸束へ変更しました。
Task 045の形からさらに調整し、実メッシュの標本で下地が見える割合を3.64%から0.32%へ減らしました。
一覧も立体と対応する可視面・向きで描き、配色変更時に遅れて完成した古い画像が最新表示を上書きしないようにしました。

手順表・占有履歴・到着位相・ピッチは維持しています。形の調整値は描画上の近似として記録しました。
作者の判断により現行案を採用し、今回の外観調整を終了します。

### 検証

- 最終ユニット結果: 425件中421件成功・失敗0・スキップ4、試験段階約224秒。スキップは任意の比較シート4件で、既知の平源氏のexpected failure 1件は継続しています。
- 実メッシュによる下地露出と先端付近の被覆、カードと立体の照合、画像の完成順が逆転する場合の更新を確認しました。
- 丸源氏・平源氏のメッシュハッシュ、一般導出に組み方名を入れない検査を通過しています。
- iPadの実エディタで配色変更・一覧→詳細・回転・リセットを確認。iPhoneではダーク・最大文字で比較用画面を確認しました。UIテスト一式は実行していません。

### 申し送り

写真に対する束の傾き・腹のふくらみ・輪郭・質感には差が残ります。写真の読みは少数の目視標本で、実物との厳密一致を主張しません。
カードと三角形メッシュには境界付近の小差が残ります（標本上S 0.69%／Z 0.53%）。
Task 035の資料照合、実機・VoiceOver・一部の画面条件は未確認のままです。
途中の検証でコマンド上限到達と診断収集による延長があり、最終成功と分けてタスク文書に記録しています。

実装・比較・検証の詳細: `docs/tasks/045-yatsu-kongo-appearance.md`、`docs/tasks/045-review-fixes.md`、`docs/tasks/046-yatsu-kongo-interlocking-bundles.md`。
