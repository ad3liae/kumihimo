# Task 042: 乗った区間を動かさない拘束は、詰め直しを妨げているか（held／still の比較。送りは共通）

- 状態: **閉じた（2026-09-17）。同僚の 14 回目の指摘で 042 を受領——041 の保留解除、`REST=still` を今後の比較の基準に（040 の現行仕様 2 を
  書き換えた。理由は「完全固定なしで 48 手が通る」）。次は Task 043（③ 固定を遅らせる。`tasks/043-delayed-fixing.md`。指示書の再承認待ちは
  挟まない）。同時に集計を保存データで訂正した（末尾「集計の訂正」）: (1) 走りは 24 手 × 2 サイクル。ピッチは held 4.02／3.04 d、still
  3.63／4.12 d、固めた球は 62／103、80／117（合計 165・197 は変わらない）。「3 サイクル」は RESUME で分けた走りごとの `cycle done` 行を
  写した誤り。(2) still の 9 段目 21 球のうち 16 は種の結び目（held では 8 段目にある）——「根元が詰まった」「拘束の影響を支持」は仮説に
  格下げ。(3) 「settle が 1/7」は回数が最大の手（5 手目）だけの比で、全体の比ではない。** それまで: **042 を判定した（審査側。末尾
  「審査側の判定」）。合格。042-0 で 041 の保留は解けた——縁側の角も残した検査で cap48・040 の基準結果とも通り抜け 0。042-2 の読みは
  「密になる」（訂正前。「集計の訂正」で改めた）。審査側の推し: `REST=still` を現行仕様に、次は③。** それまで: **042 完了（042-0 → 042-1 → 042-2 の 48 手まで走り、止まる条件 (a)〜(d) のどれにも当たらなかった）**。検査器の縁側の
  角落としを直し、cap48 と 040 の基準結果の 48 手を再検査して**通り抜け 0・未確定 0**。`REST=still` も 48 手を完走した。結果は末尾
  「042 の結果」に。**次（still の採用・押し付け・固定を遅らせる）を選ぶのは報告を受けた審査側と同僚（042-3）。worker はここで止まる。**
  それまで: **worker に渡す。追加の承認待ちは挟まない**（同僚の 13 回目の指摘、2026-09-16: 予定どおりの結果なら 042-0 → 042-1 → 042-2 の
  48 手まで続ける。段ごとの報告は進行を止める承認待ちにしない。異常が無ければ、途中の文言確認や指示書の再承認だけを理由に止まらない）。
  順序: 042-0 検査器の縁側の角落としを直し、元の走りを再検査 → 042-1 現行の上限つき 48 手（`cap48`）を基準に断面・密度 → 042-2 送りを
  共通にした held／still → 042-3 は報告を受けて審査側と同僚が決める。枝は `claude/task-041-crossing-order` の続き。
- 優先度: **編み出せる版の本線。初期リリース（Task 025 系）には触れない**
- 作成日: 2026-09-16
- 前提: **Task 040 の「現行仕様」（1〜10 項）**が模型と解法。実装は `Scripts/task040/probe.py` の既定と `Scripts/task041/checked_run.py`
  （検査つきの走り。上限 d/4、出し入れ後の settle、全遷移の連続衝突検査と停止条件）。**検査つきの走りが正本の走り。** 041 の道具
  （`follow.py`・`replay_check.py`・`crossing_audit.py`・`ccd_check.py`・`step_compare.py`）をそのまま使う。040・041 と同じもの（台、糸、拘束、
  種、乗られ判定、送り、記録、止まる条件）は書き直さない。

## 何を確かめるか

040 の `REST=held` は、乗った区間（自由部の固定端側で固定球に触れている区間）を settle でも動かさない。これは 040 の最初の意味
「縮めないが押されれば動く（`REST=still`）」からの**模型の変更**で、041-2 の 9 手目の詰まり（根元が頭に引きずられ、裏返った交差の下で
解けなくなった）を避けるために入れた。同僚（4 回目・12 回目の指摘）: 完全に動かさない拘束が**詰め直しを妨げている**可能性がある。
現行のピッチは 4.02／3.04 d で、旧結果の 4.8 d を理由に押し付け不足とは言えない。**この実験は「動かさない拘束が詰め直しを妨げて
いるか」を 1 条件の差で確かめる**——押し付け（A1）や固定を遅らせる（③）を入れるかは、結果を見て決める。

**1 条件にするために**: `probe.py` の送りは `REST` で分岐している（`REST=still` は固定部と芯だけを下げる旧方式に戻る。`send` の
`if not REST_HELD(): return super().send()`）。**両条件とも「組み上がり全体を一緒に下げる」現行の送りを使う**（送りの分岐を外す）。変えるのは
**乗った区間を射影（間隔・押し離し・台の押し出し）で動かせるか**だけ。縮めと置き直しを受けないのは両条件で同じ。

## 042-0. 検査器の縁側の角落としを直し、041 の結論への影響を確認する（最初に。これが済むまで 1 以降に進まない）

同僚の 12 回目の指摘: `follow.py` の `common()` は前後の**短い方の全長**で分割点を切っている（`hi = min(pre.u[-1], post.u[-1])`、
`U[U < hi]`）。その先にある長い側の角は落ち、最後の縁の点へ直結される。人工例（同じ球数・同じ両端・最大変位 0.2 d）:
更新前 `[(0,0,0),(1,0,0),(2,0,0),(2.01,0,0)]`、更新後 `[(0,0,0),(1,0.2,0),(2,−0.01,0),(2.01,0,0)]`、相手 `[(2,−0.01,−1),(2,−0.01,1)]`——元の折れ線は接触
（0 d）、`common()` 後は 0.00971 d、CCD は「安全」。**審査側も現行の `follow.py` で再現した**（更新後の角 u = 2.0416 が hi = 2.01 の先で落ちる）。

- **直し**（検査上の対応の定義）: 分割点は**両状態の全折れ点と両状態の縁の端点**を含む（切らない）。短い方の全長を超えた位置は、
  その状態の縁の点に留める（繰り出す前／取り込んだ後の材料は縁の点にある。`np.interp` の端の値そのまま）。**両端状態の折れ線の形が
  保たれる**ことを確かめる。審査側が人工例で確かめた: この作りで更新後の距離は 0 d、CCD は「検出」。取り込み（後が短い）の向きも同じ。
- 直したら `ccd_check.py` にこの例を**繰り出し・取り込みの両向き**で足し、既存の例が全部通ることを確かめる。
- **元の走りを再検査する。再生する解法と設定を明示する**: 040 の基準結果は**当時の上限なし・当時の処理順**、cap48 は**上限 d/4・
  出し入れ後の settle を含む処理順**。各手の再生結果は保存状態と**球数・座標が一致**すること。一致しない走りを元の走りの再検査として
  扱わない。検査のために刻みを変えた新しい走りと、元の走りの再検査は区別する。
- 順: (a) 局所の 3・6・7 手目と保存した 31 手目（`start-h31.pkl`）→ (b) cap48 の 48 手 → (c) 040 の基準結果 48 手（(b)(c) は各 30〜45 分）。
- **31 手目の上限なしは、既知の通り抜けを検出するための対照**。検出されることが期待結果であって停止理由ではない。検出できなければ
  止まって原因を確認する。同じ開始状態の d/4・d/8 も確認し、件数の変化を記録する。
- **上限つき（cap48）や基準結果で新しい接触・未確定が出たら、前後の状態と検出箇所を保存して報告し、042-1 以降に進まない。** 出なければ
  「縁側の角も残した検査で通り抜け無し」と初めて言え、042-1 へ続ける（承認待ちはしない）。

## 042-1. 現行の上限つき 48 手（`cap48`）を基準に、断面と密度を測る

042-2 の比較の基準は 040 の基準結果ではなく、**現行仕様の走り（上限 d/4・出し入れ後の settle・検査つき）**。`.build/task041/cap48/h48.pkl`
（無ければ 48 手目の pickle）から、糸の固定部だけ（人工の芯を除く）で:

- 固定部の球数（結び目・固めた球）、外径（2 max r + d）、r の 50／90 百分位。
- 断面: 固定部の天辺から 2・4・6・8 d 下の 1 d の厚さの切り口の**固定球**の数、幅、厚み、比（`Scripts/task036/measure.py` の `section`）。
  **同じ切り口の自由球も別欄で数える**（固定球が増えただけかを区別するため）。
- 1 d の高さに入る糸の固定球（天辺から 9 段）。
- 乗られて固まらない球（`Scripts/task040/left.py`）と、その内訳（扇の空中の交差／自由な糸の上の糸）。
- 交差の数（`crossing_audit.py`、48 手目）。
- ピッチ（サイクル単位）: 4.02／3.04 d（041-3b の実測。書き写す）。

同じ測り方を 042-2 の両条件に当てる。測り方の script は `Scripts/task042/measure.py` に置く（036・040 の関数を呼ぶだけ）。

## 042-2. 送りを共通にした held／still の比較

1. **`REST=still` の送りを現行の送り（全体を一緒に下げる）にする**（`probe.py` の `send` の分岐を外す）。変えるのは乗った区間を射影で
   動かせるかだけ——**縮め・置き直し・固定判定・上限・検査条件は共通**。分岐を外したあと、**`REST=held` の 1〜4 手目が cap48 と球ごとに
   一致する**ことを確かめる。不一致なら原因を調べて報告する。
2. **`REST=still` を検査つき・上限 d/4 で、まず 1〜12 手**。040 の実験②（`REST=held` を入れる原因になった 9 手目の詰まり: 送りの中の張りで
   残差 0.33／0.40、0.4 d の貫入）が今回の条件でどうなるかを見る——**041-2 の 31 手目の 1.7 d の跳びとは別の現象で、「上限があるので
   引きずりは消える」とは断定しない。** 詰まり（settle の上限到達が続く、(a) > 0.02 d、止まる条件）が出たらそこで止めて報告し、以前と
   同じ原因かは保存状態で確認する。
3. 通れば**その保存状態から 48 手まで続ける**（検査つき、上限 d/4、1 手ずつ前で、`PICKLE`）。held の 48 手は、再検査済みの cap48 を対照に使う。
4. **両条件を並べる**（042-1 の測り方で。ピッチはサイクル単位、実測値と変更内容をそのまま。1 件から誤差幅を出さない）: ピッチ、固めた球、
   乗られて固まらない球、外径、断面（固定球・自由球を別欄）、1 d の高さの球、交差の数と反転（追跡の未確定は別欄）、(a)、**残差、収束
   （settle の回数と上限到達）、接触・未確定**、上限が働いた手と球数、時間。
5. **乗った区間の移動量は settle の前後で測る**（各手、乗った区間の球の変位の最大と中央値。held では 0）。**送りの一括下降や球の追加・
   削除と混ぜない。** 「詰め直し」が起きたなら、どこで（天辺／脇／根元）起きたかを 1 d の高さの球数の変化で示す。

## 042-3. 結果の読み方（今回の条件で判断する。次を選ぶのは報告のあと、審査側と同僚）

- **変わらない**（still が通り、断面・密度が held と変わらない）→ 拘束を外す効果を確認できなかった。
- **詰まる**（still が序盤で詰まる）→ 現行解法では still が成立しなかった。以前（040 の実験②の 9 手目）と同じ原因かは保存状態で確認する。
- **密になる**（1 d の球数が増え、断面が縮む）→ 今回の条件では拘束の影響を支持する。
- **still の採用、押し付け（A1）の追加、固定を遅らせる（③）変更は、この比較結果を報告してから決める。** worker はここで止まる。

## 予言（回す前に書く。合わせにいかない。合わない結果も未確定もそのまま報告する）

- 042-0: 縁側の角を残しても、3・6・7 手目・cap48・040 の基準結果に通り抜けは出ない（出たら、それが答え）。31 手目の上限なしは既知の
  通り抜けを検出する（検出が期待結果）。
- 042-2: still は序盤（9 手目前後）で詰まらない。~~上限 d/4 が 1.7 d の引きずりを止めるので~~（同僚の 13 回目の指摘: 040 の実験②の詰まりと
  041-2 の跳びは別の現象。理由は書かない）。48 手は通る。
- still の断面・密度は held と大きく変わらない（1 d の球数の差は数球）。ピッチは書かない。

## 合格条件・止まる条件

**合格**: (1) 042-0 の直しが人工例で検出になり、041 の再検査の結果（通り抜けの有無）が報告されている。(2) cap48 の断面・密度の表。
(3) held／still の表（両条件とも検査つき・上限 d/4・送り共通）。still が詰まったなら、詰まった手と状態。(4) 乗った区間の変位の記録。

**止まる**: (a) 042-0 の再検査で通り抜けか未確定が出る（報告して止まる。041 の結論を保留に戻す）。(b) still が序盤で詰まる（報告。
042-2 の 3 には進まない）。(c) 1 手が 5 分を超える。(d) `REST=held` の 1〜4 手目が cap48 と一致しない（送りの分岐を外した直しが held を
変えている）。

## 段と報告

- 042-0 → 042-1 → 042-2 を、予定どおりの結果なら**止まらずに続ける**（各段の報告は進行を止める承認待ちにしない）。止まるのは止まる条件だけ。
- **完了報告にまとめるもの**: 検査器の確認結果（人工例）、再生一致（解法と設定を明示）、再検査結果、held／still の比較表、保存先、
  未解決事項。打ち切り（時間上限）は成功と区別して書く。

## 守ること

- 模型を変えるのは `REST` の 1 条件だけ（送りの分岐を外すのは「両条件を現行の送りに揃える」ため）。閾値・縮め・乗られ判定・上限は変えない。
- 検査つきの走りが正本。検査なしの数字は出さない。数字の名前を分ける（標本の最小距離／球の最大変位／検査用の対応点の移動量）。
- 文書は現行仕様・観測結果・原因の仮説を分けて書く。数字は糸と人工の芯を分ける。
- product code に触れない。テストは走らせない。`.build/` の外に走りを置かない。docs と Scripts のコミットは分ける。
- 前で回す（10 分の上限、`PICKLE`/`RESUME`）。重い走りは 1 つずつ。

## 042 の結果（2026-09-17。worker。止まる条件 (a)〜(d) のどれにも当たらなかった）

### 042-0. 検査器の縁側の角落としを直した

**直し**（`Scripts/task041/follow.py` の `common()`）: 分割点は**両状態の全折れ点と両状態の縁の端点**（`np.unique(np.concatenate([pre.u,
post.u]))`。短い方の全長で切る `hi` は消した）。短い側の全長を超えた位置は `np.interp` が最後の値を返す——**その状態の縁の点に留まる**。
繰り出し（後が長い）と取り込み（後が短い）の両向きが同じ式で覆われる。

**人工例**（`ccd_check.py`、6 例すべて通る）:

| 例 | 中身 | 結果 |
| --- | --- | --- |
| 1 | 交差する 2 線分を**安全と言わない**（速さ 2〜300 d、非二分の交差時刻 100 通り、直交と斜め） | 1 件も安全にならない |
| 2 | 明らかに離れた対は安全と判定できる | 判定できる |
| 3 | 上限まで決まらない対は**未確定**になる | 未確定 |
| 4 | 太さの閾値の谷 | 検出 |
| 5 | 同僚の 10 回目の角（統合すると 5e−6 d で安全） | **検出**（折れ点を残すので 0 d） |
| 6 | 同僚の 12 回目の縁側の角、**繰り出し・取り込みの両向き** | **両向きとも検出**（距離 0 d）。両端状態の折れ線の形が保たれることも確認（最大差 0 d） |

**再検査**（再生した解法と設定を明示する。各手の再生は保存状態と**球数・座標が一致**した）:

| 走り | 解法・設定 | 再生の一致 | 通り抜け | 未確定 | 遷移／対 | 標本の最小距離 | 1 回の射影で 1 d を超えた検査点 | 時間 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 局所 3・6・7 手目 | `checked`、上限 d/4 | 一致 | 0 | 0 | — | — | — | — |
| 31 手目（`start-h31.pkl`） | `checked`、**上限なし**（対照） | 一致 | **29 件（既知の通り抜けを検出）** | 0 | — | 2.36e−07 d | — | — |
| 同上 | 上限 d/4 | 一致 | 0 | 0 | — | — | — | — |
| 同上 | 上限 d/8 | 一致 | 0 | 0 | — | — | — | — |
| cap48 の 48 手 | `checked`、上限 d/4、出し入れ後の settle | **48 手すべて一致**（最大差 0.0e+00） | **0** | **0** | 857,260／293.4 M | 0.846 d（31 手目） | 1,198（すべて送りの一括下降） | 43.8 分 |
| 040 の基準結果 48 手 | `probe`、**当時の上限なし・当時の処理順** | **48 手すべて一致** | **0** | **0** | 652,138／217.2 M | 0.0646 d（5 手目） | 1,028（`push_apart` 63・`space_out` 40・`push_out` 6、最大 3.68 d） | 31.1 分 |

31 手目の上限なしで検出した箇所: 糸 4 の線分 130 × 糸 11 の線分 315、t = 0.7826、標本の最小距離 2.36e−07 d（送りの中の張り）。**期待どおり
検出された**ので対照として働いている。

→ **縁側の角も残した検査で、cap48 と 040 の基準結果に通り抜けは無い。** 041-3b で「保留」と書いた 040 の非交差は、この再検査で
（この検査器の範囲で）保たれた。止まる条件 (a) に当たらない。

### 042-1. cap48 の断面と密度

`.build/task041/cap48/h48.pkl`、糸だけ（人工の芯 148 球は数えない）。測り方は `Scripts/task042/measure.py`。

- 固定部の球数 181、自由部 185→（still と並べた表は次節）、外径 12.0 d、r の 50／90 百分位 3.2／4.3 d。
- 断面（天辺から下、1 d 厚）: 2 d 固定 23（比 1.32）・自由 13／4 d 固定 25（1.37）・自由 7／6 d 固定 30（1.07）・自由 0／8 d 固定 20（1.23）・自由 0。
- 1 d ごとの固定球（天辺から 9 段）: 6 18 19 28 24 25 28 33 0。
- 乗られて固まらない球 6（すべて「下に何もない」＝扇の空中の交差）。
- 交差 193。
- ピッチ（サイクル単位、実測。各手の行の `sent` を 1〜24 手・25〜48 手で集計）: **4.02／3.04 d、合計 7.06 d**（指示書 042-1 と同じ。
  **訂正**（2026-09-17、審査側が保存データで集計し直した）: 先の報告の「4.02／2.79／0.25、3 サイクル」は RESUME で分けた走りごとに出る
  `cycle done` 行を写したもので、集計の誤り。走りは 24 手 × 2 サイクル）。

### 042-2. held／still の比較（送り共通、上限 d/4、検査つき）

**送りの分岐を外した**（`Scripts/task040/probe.py`: `REST=still` が固定部と芯だけを下げる旧方式に戻る分岐を削除。両条件とも全体を一緒に
下げる）。

**止まる条件 (d) の確認**: `REST=held` の 1〜4 手目は **cap48 と球ごとに一致**（球数も一致、最大差 0.000e+00 d）。乗った区間の移動量も
0.000／0.000 d で、分岐を外した直しが held を変えていないことを確かめた。

**still は 1〜12 手を通り、そのまま 48 手まで完走した**（打ち切りではない。48 手目まで保存済み）。**040 の実験②の 9 手目の詰まり
（残差 0.33／0.40、0.4 d の貫入）は再現しなかった。** ただし今回は送りの共通化・上限 d/4・検査つきと**条件が複数違う**ので、
何が効いたかは切り分けていない（「上限で消える」とは書かない）。

| | **cap48（`REST=held`）** | **still（`REST=still`）** |
| --- | --- | --- |
| ピッチ（サイクル 1／2、合計。各手の `sent` の集計。訂正済み） | 4.02／3.04、**7.06 d** | 3.63／4.12、**7.75 d** |
| 固めた球（糸、サイクル 1／2、合計。各手の `fixed` の集計。訂正済み） | 62／103、**165** | 80／117、**197** |
| 48 手目の固定球／自由球（芯を除く） | 181／182 | **213**／185 |
| 乗られて固まらない球 | 6（すべて下に何もない） | **2**（すべて下に何もない） |
| 外径 | 12.0 d | **11.7 d** |
| r の 50／90 百分位 | 3.2／4.3 d | **2.6**／4.1 d |
| 断面 2 d: 固定（比）・自由 | 23（1.32）・13 | 20（1.16）・16 |
| 断面 4 d: 固定（比）・自由 | 25（1.37）・7 | 30（1.74）・9 |
| 断面 6 d: 固定（比）・自由 | 30（1.07）・0 | 35（1.05）・6 |
| 断面 8 d: 固定（比）・自由 | 20（1.23）・0 | 26（1.08）・0 |
| 1 d ごとの固定球（天辺から 9 段） | 6 18 19 28 24 25 28 33 0 | 9 17 23 22 28 32 34 27 **21** |
| 同、種の結び目 16 球を除く（訂正で追加） | 6 18 19 28 24 25 28 17 0（結び目は 8 段目） | 9 17 23 22 28 32 34 27 5（結び目は 9 段目） |
| 交差（48 手目） | 193 | 209 |
| 交差の反転／追跡の未確定 | 0／**1**（6 手目 t8-t9） | **1**（6 手目 t8-t9）／0 |
| (a) 対の数・最深 | 0 対、0.008 d | 0 対、0.007 d |
| 残差（tighten／send の最大） | 1.0e−02／1.0e−02 | 1.0e−02／1.0e−02 |
| 収束（settle の回数の最大） | 20,033（5 手目）／送り 6,669（4 手目） | 2,792（5 手目）／送り 2,439（6 手目） |
| settle の上限到達 | 0 | 0 |
| 接触／未確定 | **0／0** | **0／0** |
| 標本の最小距離 | 0.767 d（35 手目） | 0.763 d（26 手目） |
| 球の最大変位（射影前後の実際の球） | 0.36 d（3 手目） | 0.37 d（5 手目） |
| 上限が働いた手／抑えた球 | 7 手／1,714 | **25 手**／510 |
| 遷移／検査した対 | 857,673／289.4 M | 680,009／358.9 M |
| 時間 | 2,549 s | **2,437 s**（機械が眠った 2 手を除く。壁時計は 19,438 s。下記） |

**乗った区間の移動量（settle の前後、送りの一括下降や球の増減と混ぜていない）**: held は全手 0.000／0.000 d（定義どおり）。still は
最大 1.534 d（7 手目）、1.422（5 手目）、1.263（3 手目）、1.236（6 手目）、1.21（34 手目）で、**14 手目あたりから最大 0.1 d 前後・中央値
0.000 d に静まる**。つまり動かせる拘束が実際に動かしたのは**序盤**である。

**どこで詰め直しが起きたか**（1 d ごとの球数の変化）: 先の報告は「still は根元側が違う。9 段目が 0 → 21 球」と書いたが、**訂正**
（2026-09-17、審査側が保存状態で数え直した）: **still の 9 段目 21 球のうち 16 は種の結び目**で、held では同じ結び目が 8 段目にある
（33 ＝ 16 ＋ 17）。結び目を除いた固めた球は held 6 18 19 28 24 25 28 17 0、still 9 17 23 22 28 32 34 27 5——段ごとの差は数球で、根元だけが
違うとは言えない。固定部の天辺の z は −1.03 → −1.36、結び目の z は −8.71 → −9.41（送りの合計 7.06 → 7.75 d の分だけ全体が下がっている）。
「根元が詰まった」は**仮説**として残す（結び目の段の境界の効果と分けられない）。

### 予言と結果

- 042-0 の予言（通り抜けは出ない、31 手目の上限なしは検出する）は**当たった**。
- 042-2 の「still は序盤で詰まらない」「48 手は通る」も**当たった**。
- 「**still の断面・密度は held と大きく変わらない（1 d の球数の差は数球）**」——先の報告は「外れた（根元で 0 → 21）」としたが、**訂正**:
  9 段目の 21 球の 16 は種の結び目で、結び目を除けば段ごとの差は数球。動いたのは固めた球 165 → 197、外径 12.0 → 11.7 d、r の 50 百分位
  3.2 → 2.6 d。予言は「1 d の球数」では当たり、固めた球・外径・r では外れた。**そのまま報告する。**

### 交差の反転 1 件（still の 6 手目、t8-t9）——追跡の判定であって、衝突の判定ではない

`crossing_audit.py --track` は still の 6 手目に **REVERSED を 1 件**出した（t8-t9、3.63/2.16（前は 2.59/3.68）、−1.17 d → +1.12 d、r 5.00 → 2.17）。
**同じ手の連続衝突検査は接触 0・未確定 0、標本の最小距離 0.775 d**。静的にも糸 8 と糸 9 は 5 手目・6 手目とも**最接近 1.000 d**である。
**貫通は起きていない。**

起きたことは対応付けの側にある: cap48 の 6 手目には t8-t9 の交差が**2 つ**あり（古い −1.18 d／新しい +1.34 d）、だから 1 対 1 の照合が
**未確定**にした。still の 6 手目には**1 つしかない**（古い方が消えている）ので、残った新しい交差が古い交差に対応付けられ、符号が
反転して見えた。**どちらの交差が「同じ」交差かは決められない**（041-1 で誤った反転を出したのと同じ族の問題で、あのときは 1 対 1 に
して直した。今回は 1 対 1 でも残る）。**「反転」は追跡の判定、「接触 0」は検査の判定**として、両方そのまま表に残した。

### 時間について（打ち切りではない）

still の 36 手目と 37 手目は壁時計で 10,927 s と 6,074 s だった。**仕事量は前後の手と同等**（16,234／12,418 遷移、settle 997／849 回。
隣の 35 手目は 15,889 遷移で 85 s）。`pmset -g log` に 35 手目が終わった 5 秒後の **01:28:46 `Entering Sleep state due to 'Clamshell Sleep'`**
とその後の maintenance sleep／dark wake が並んでおり、**機械が眠っていた時間**である（38 手目以降は 65 s 前後に戻った）。
**止まる条件 (c)「1 手が 5 分を超える」は、壁時計では超え、計算としては超えていない。**両方書いておく。打ち切りは無い。

### 保存先（`.build/` は git の外。判断に効く数字はこの文書に書いた）

- 042-0 の再検査: `.build/task042/recheck-local/`（3・6・7 手目）、`recheck-cap48/`、`recheck-base/`（各 `hands.csv` と `run-*.log`）
- 31 手目の対照: `.build/task042/cap31-none/`（`touch-h31-*.npz` 20 件）、`cap31-d4/`、`cap31-d8/`
- held の 1〜4 手目: `.build/task042/held-1-4/`
- still: `.build/task042/still-1-12/`、`.build/task042/still-13-48/`（`h48.pkl`、`h48.txt`、`run-*.log`）
- 測り方: `Scripts/task042/measure.py`

### 未解決事項

1. **still の 6 手目の反転 1 件**（上記）。衝突検査は接触 0 なので貫通ではないが、**どちらの交差が同じかは未確定**のまま。
2. **断面の比較が交絡している**: still は固めた球が 197 対 165 と多い。断面の固定球が増えたのは「太った」からか「固めた球が増えた」
   からか、この 1 件では分けられない（自由球を別欄にしても、両条件で固定・自由の割り振りが違う）。
3. **040 の実験②の 9 手目の詰まりが出なかった理由は切り分けていない**（送りの共通化・上限 d/4・検査つきが同時に違う）。
4. **1 件から誤差幅は出していない**。各条件 1 走りである。
5. 上限（d/4）は still で **25 手**にわたって球を抑えた（held は 7 手）。抑えなければどうなるかは試していない。

---

# 審査側の判定（2026-09-17、042 を受けて）— **合格。041 の保留は解けた。still は完全固定なしで 48 手を通る（「根元を詰める」は訂正後は仮説——末尾「集計の訂正」）**

## 受け取ったこと（`.build/task042/` の `hands.csv` で確かめた: `recheck-cap48` 48 手 857,260 遷移・通り抜け 0・未確定 0・差 0、
`recheck-base` 48 手 652,138 遷移・0・0・差 0、`recheck-local` 3 手・0・0）

- **042-0**: `common()` は両状態の全折れ点と縁の端点を残す（短い方は縁の点に留める）。人工例 6 つ通過、同僚の 12 回目の例は両向きで検出。
  3・6・7 手目、31 手目（上限なしは既知の通り抜けを同じ時刻・深さで再現＝対照の期待結果。d/4・d/8 は 0）、**cap48 の 48 手（現行解法で
  再生、全手一致）と 040 の基準結果 48 手（当時の解法で再生、全手一致）とも通り抜け 0・未確定 0**。**041 の「形を保った再検査で非交差」の
  保留は、この検査器の届く範囲で解けた。**
- **042-1**: cap48 の断面・密度（糸だけ、自由球は別欄）。
- **042-2**: 送りを共通にし、`REST=held` の 1〜4 手目が cap48 と球ごとに一致（止まる条件 (d) 不該当）。**still は 48 手完走**、040 の実験②の
  9 手目の詰まりは再現しなかった（条件が複数違うので原因は切り分けていない——そのとおり）。表は上。
- 予言の外れ（断面・密度は大きく変わらない → 根元で 0 → 21 球）はそのまま報告された。
- 6 手目の反転 1 件は追跡の判定で、接触 0・未確定 0・2 本は 1.000 d より近づいていない。追跡の未確定として扱う（反転とは数えない）。
- 36・37 手目の壁時計は蓋を閉じた眠りで、計算ではない（`pmset` の記録）。止まる条件 (c) は計算では該当しない。

## 読み（042-3 の規則で。今回の条件で判断する）

**「密になる」——訂正後の読み**（訂正前の文は「集計の訂正」に写した）: still で確かに動いたのは、固めた球 165 → 197、r の中央値 3.2 → 2.6 d、
外径 12.0 → 11.7 d、固定部の天辺 −1.03 → −1.36。1 d ごとの固定球は、種の結び目 16 球を除けば段ごとの差は数球で、「根元が詰まった」
「動かさない拘束が詰め直しを妨げていた」は**仮説**（結び目の段の境界の効果と分けられない）。乗った区間が settle で動いたのは序盤
（3〜7 手目に最大 1.2〜1.5 d）で、14 手目あたりから静まる。交絡はそのまま: 「詰まった」と「固めた球が増えた」を 1 本ずつの走りでは
分けられない。断面の幅／厚みの比は 1.05〜1.74 で、平組の形（≥ 2）にはどちらもなっていない。settle の回数は**回数が最大の手（5 手目）で
20,033 → 2,792**（送りは 4 手目 6,669 → 6 手目 2,439）——最大の手だけの比で、全手の比ではない（「1/7」と一般化しない）。記録に残す
（読みは書かない）。

## 判断と推し → 同僚の答え（14 回目、2026-09-17）で決まった

1. **`REST=still` を今後の比較の基準にする（決定）。** 理由は「**完全固定（held）なしで 48 手が通る**」——`held` は 040 の実験②の 9 手目の
   詰まりの回避策で、その詰まりは現行解法（送り共通・上限 d/4・出し入れ後の settle・検査つき）では起きない。040 の現行仕様 2 を書き換えた。
   基準の状態は still の 48 手（`.build/task042/still-1-12/`・`still-13-48/`、各手の pickle）。「根元が詰まるから」は理由にしない（仮説）。
2. **次は Task 043（③ 固定を遅らせる）**（`tasks/043-delayed-fixing.md`）。まず「即時固定」（still の 48 手をそのまま対照に）と「1 手遅らせる
   固定」の比較。押し付け A1 や球ごとの手番号変更は同時に入れない。指示書の再承認待ちは挟まない。
3. 交絡（固めた球の差）は**固める数を人為的に揃えて分けない**（同僚の 14 回目）。数は記録し、断面は組み点から同じ深さで比べ、自由部を含む
   糸の形も測る。1 本ずつの走りに誤差幅は付けない。

## 041・040 への反映

- 041: 状態の行を「保留は解けた」に。040 の現行仕様 10: 「基準結果 48 手はこの検査で通り抜け 0（縁側の角も残した再検査済み）」に。
- 040 の現行仕様 2 を `REST=still` に書き換えた（2026-09-17、同僚の 14 回目の答えを受けて）。

---

# 集計の訂正（2026-09-17、同僚の 14 回目の指摘。審査側が保存データで数え直した）

1. **走りは 24 手 × 2 サイクル**（表は 24 手＝1 サイクル、48 手＝2 サイクル）。先の報告と判定の「サイクル 1／2／3」「3 サイクル目」は、
   RESUME で分けた走り（1〜4、5〜8、9〜20、…）ごとに `checked_run.py` が出す `cycle N done` 行（その走りの中だけの集計）を写したもの。
   各手の行の `sent`・`fixed` を 1〜24 手・25〜48 手で集計し直すと: **held（cap48）ピッチ 4.02／3.04 d（合計 7.06）、固めた球 62／103（165）；
   still ピッチ 3.63／4.12 d（合計 7.75）、固めた球 80／117（197）**。合計は変わらない。指示書 042-1 の「4.02／3.04」は正しかった。
   （`cycle N done` 行は RESUME で分けた走りでは走りの中の集計になる——記録の注意。script は変えず、数字は各手の行から集計する。）
2. **still の 9 段目の 21 球のうち 16 は種の結び目**（各糸の `made[t][0]`、z −9.41 d。held では z −8.71 d で 8 段目にあり、33 ＝ 16 ＋ 17）。
   結び目を除いた 1 d ごとの固めた球は held 6 18 19 28 24 25 28 17 0、still 9 17 23 22 28 32 34 27 5。訂正前の文「still は根元が詰まる——
   1 d ごとの固定球が 9 段目で 0 → 21……今回の条件では、動かさない拘束が詰め直しを妨げていた、を支持する」は**仮説に格下げ**——結び目の
   段の境界の効果と分けられない。still を基準にする理由は「完全固定なしで 48 手が通る」であって、「詰まるから」ではない。
3. **「settle の回数が 1/7」は回数が最大の手（5 手目、20,033 → 2,792）だけの比**。全手の比でも平均でもない。architecture.md・README も直した。
