# Task 065: 糸の色を、ハマナカ アメリーエフ《合太》の30色に置き換える

- 状態: **実装済み**（2026-09-26、Claude Code。枝 `claude/task-065-amerry-f-30-colours`。PR はまだ作っていない）。結果は末尾。
  指示書は審査側（2026-09-26）。**作者の決定で、八つ金剛返し組の淡い黄と丸源氏組の生成りは 501（写真の2色の差に近づけるため、
  いちばん近い色ではない。結果の 2）**
- 前提（先に読むこと）: **`docs/colours.md`（審査側が書き直した正本。30色の値、取り方、古いIDの読み替え2表）**、
  `docs/specifications/project-editor.md`「色の一覧」（審査側がこの指示書と同時に書き直した）、Task 063 の指示書と結果、`AGENTS.md`
- 基準: 最新の main（PR #50、`39b91ef` 以降）
- **製品コード（データ・画面・保存）に触れる。** PR の前に全件を1回通すこと
- 番号について: Task 064（色選択シートの不具合）は作者が実機で確かめて起きなかったため、コミット前に捨てた。064 は欠番。

## 0. 作者の決定（2026-09-26）

1. 西陣の38色を実機で見て「似たような色ばかりで鮮やかさがないな…」。代わりに**ハマナカ アメリーエフ《合太》の30色**（作者が渡した
   見本「30 colors」、色番号 501〜530）。
2. **38色は消して30色に置き換える。**
3. 表示には「ハマナカ アメリーエフ」と書いてよい。

## 1. いまの作り（Task 063 で入ったもの。worker が作った）

- `ThreadColorCatalog`: 38色。`code`（No.01 など）と読み、`formerIDs`（古い12のID → 38色）、`currentID(for:)`、`color(for:)` が
  古いIDを読み替える。既定は `zoge`。
- 試験は `KumihimoTests/TwelveColours.swift` の12個のIDで、色の一覧から切り離してある。
- 見本配色は写真の糸の色を `Scripts/task063/measure_colourings.py` で測り、38色から選んだ。

## 2. すること（Task 063 と同じ順。コミットを分ける）

### 2.1 30色にする

1. `colors` を `docs/colours.md` の30色にする（並び・内部ID `amerry-f-5xx`・色番号・呼び名・値は表のとおり）。
   - **公式の色名は無い。** 画面の主な表示は色番号（例「501」）、添え書きに呼び名（「オフホワイト」）。読み上げは「501、オフホワイト」の形。
     呼び名は審査側が付けた仮のもので、公式名のように見せない（例: シートの下に「色番号はハマナカ アメリーエフ《合太》。呼び名は目安です」）。
   - シートの見出しか先頭に「ハマナカ アメリーエフ《合太》」と出す。
   - 既定の色は **501**。
2. **古いIDの読み替えを2表にする**（`docs/colours.md`）: 仮の12色 → 30色、西陣の38色 → 30色。どちらも表のとおり。
   **既定の色だった `natural` と `zoge` は、いちばん近い色ではなく新しい既定の 501 へ**（表に書いた例外）。
   38色の読み替えは多対1になる（表のとおりでよい）。
3. コードの中で38色のIDを直接書いている所は、30色のIDに書き換える。
4. `Scripts/colours/sample_amerry_f.py` と見本の画像は審査側が置いた（画像は git の外 `.build/colours/amerry-f/swatch.png`）。
   スクリプトで値が表と一致することを確かめてから、このコミットに入れる。西陣の `Scripts/colours/sample_kinu.py` は、履歴として残してよい。

### 2.2 見本配色を30色へ

- Task 063 の `measure_colourings.py` で測った写真の糸の色から、30色で CIEDE2000 がいちばん近い色を選び直す（測り直しは要らない。
  同じ測り方の値を使う）。配色の中で違う色だった糸が同じ色に集まったら、次に近い色で分ける。
- 審査側の参考（江戸八つ組、写真の色での最寄り）: マゼンタ→505（ΔE 11.2）、シアン→528（11.9）、黄緑→517（6.4）、生成り→522（6.5）。
  Task 063 の値で測り直した結果が違ってよい。
- 配色の説明文を新しい色に合わせて書き直す。

## 3. 試験

1. 一覧は30色、IDが重ならない、並びは表のとおり、既定は `amerry-f-501`、値は `docs/colours.md` と一致（小数3桁）。
2. 古い12のIDと38のIDが、それぞれ表の色になる（`natural`・`zoge` は 501）。古いIDで保存した作品を開くと読み替えた色で描かれ、
   保存し直すと新しいIDになる（Task 063 の試験を30色に直す）。
3. 見本配色は30色のIDだけを使い、配色の中で違う色だった糸は違う色のまま。
4. 形状ハッシュは変わらない。色に効く固定値は、Task 063 と同じ手順（同じ配色を古いIDと新しいIDで描いて画素が同じ、見える区間が
   前と同じ）で色だけが変わったことを確かめてから固定し直す。
5. ガードを通す。

## 4. 検証

- 色選択シートを iPhone 縦と iPad 横で撮る（通常・最大の文字・ダーク）。
- 各組み方の一覧カードと3D（江戸八つ組・丸源氏・平源氏・八つ金剛返し組）を38色の版と並べた図を1枚。
- 見本配色の置き換えの表（組み方、写真の色、選んだ色、ΔE、次の候補）。

## 5. 記録

- この指示書の末尾に結果。`docs/README.md` の Task 065 の行。`architecture.md` の色の一覧の段落と `sources.md` の色の出所を30色に。
  `product.md` の色の一覧の1行も。

## 結果（2026-09-26、Claude Code）

枝 `claude/task-065-amerry-f-30-colours`。コミットは指示書の順に分けた: `33cdb3b`（審査側の文書）、`d4059e4`（2.1）、
`0bf7afd`（2.2）、`51e3a14`（記録）、そのあとに作者の決定による2色の差し替えと、その記録。

### 1. したこと

- **2.1 30色にした**:
  - `ThreadColor` は `id`・`code`（色番号 "501"）・`name`（呼び名）・`value`。読み（`reading`）はやめた。
    `ThreadColorCatalog.colors` は `docs/colours.md` の表から生成して写した（見本の並び、値は小数3桁）。内部IDは色番号から
    作る（`amerry-f-501`）。既定は 501 オフホワイト。
  - **読み替え**: `formerIDs` を「仮の12色から」「西陣の38色から」の2表（計50）にした。どちらも `docs/colours.md` の表のとおり
    （`natural`・`zoge` は 501）。`Scripts/colours/nearest.py --former` で、2表とも CIEDE2000 のいちばん近い色と一致し、
    違うのは表に書いた例外の2つだけであることを確かめた（ΔE の値は表に無いので比べていない）。
  - **色選択シート**: 先頭に「ハマナカ アメリーエフ《合太》」（見出し）、各色は色の丸・色番号（主）・呼び名（添え書き）、
    下に「番号はハマナカの色番号です。呼び名は目安で、公式の色名ではありません。」。読み上げは「501、オフホワイト」
    （`ProjectEditorStrings.threadColorAccessibilityLabel`）。**台の糸の読み上げも同じ形にした**（「糸3、501、オフホワイト」。
    判断は 5）。
  - **コードの中の38色のID**: 見本配色は 2.1 では38色からの読み替えのまま（丸源氏・平源氏で2色が 504 に集まる。2.2 で選び直した）。
    プレビューと比較プレビューのうち**12色の名前で書かれた色**（青・赤・桃・白など。コメントがそう呼んでいる）は、12色からの
    読み替えで書いた（青 513、赤 508、桃 505、白 501…。38色を経由すると青が 514 紺、紫が青と同じ 514 になる）。
    12色を見分ける `ProjectEditorPreviewData.colorfulAssignments` は、`natural` だけ最寄りの 529 にした（表どおり 501 だと白と同じ）。
  - 試験の編集の試験（`ProjectDraftTests`・`ProjectEditorPersistenceTests`・`BraidStandAndDetailTests`）は30色のIDに。
    `undecidedBraidAndPositionColorsRoundTrip` の白土は、既定と同じ 501 だと色を変えたことにならないので 529 にした。
    本の配色を写した試験の配色（`BraidReferenceColourings`、`TwelveColours`）は古い12色のIDのまま読み替えで描く（そう注記した）。
  - `Scripts/colours/sample_amerry_f.py`（審査側）を回し、**30色とも表と同じ値**（16進と 0〜1、並び、色番号、内部ID）になったことを
    確かめてから 2.1 に入れた。`nearest.py` は30色の表と読み替えの2表を読むようにした。`sample_kinu.py` は残した。
- **2.2 見本配色を写真の色から選び直した**（次の 2）。説明文を新しい色に書き直した。比較プレビューの `swapped`・`leftright` と
  `EdoYatsuTurnTests` の同じ配色もレシピの色にした。
- **作者の決定で2色を差し替えた**（2026-09-26。次の 2）: 八つ金剛返し組の淡い黄と丸源氏組の生成りを 501 に。

### 2. 見本配色の置き換え（報告の表）

**測り方は Task 063 のまま**（`Scripts/task063/measure_colourings.py` の矩形と「光の当たった側の代表色」。測り直していない）。
選び方は `Scripts/task065/choose_colourings.py`: 配色の中の (糸の役, 色) の組を ΔE の小さい順に見て、役にまだ色が無く、
色がまだ使われていなければ取る（**集まったら近いほうが残り、もう一方が次に近い色へ**）。「近い順」は、その役だけで見た順。

| 組み方（写真） | 写真の色（役） | 測った値 | 選んだ色 | ΔE00 | 近い順（上位3） |
| --- | --- | --- | --- | --- | --- |
| 丸源氏組（教科書 p.94） | ローズ | 0.92, 0.69, 0.66 | 505 ローズ（分けた） | 16.9 | 504 ピーチ 9.7 / 529 ベージュ 16.6 / 505 ローズ 16.9 |
| | サーモン | 0.93, 0.68, 0.53 | 504 ピーチ | 6.4 | 504 ピーチ 6.4 / 529 ベージュ 12.8 / 520 キャメル 15.4 |
| | 朱 | 0.94, 0.46, 0.33 | 506 オレンジ | 11.0 | 506 オレンジ 11.0 / 505 ローズ 14.1 / 504 ピーチ 20.1 |
| | 生成り | 0.90, 0.85, 0.80 | **501 オフホワイト**（作者: 写真の2色の差に近づける） | 8.1 | 529 ベージュ 6.6 / 501 オフホワイト 8.1 / 522 ライトグレー 8.7 |
| | 紺 | 0.28, 0.24, 0.32 | 526 チャコール | 7.7 | 526 チャコール 7.7 / 510 ぶどう 8.2 / 511 紫 13.7 |
| 平源氏組（教科書 p.96） | サーモン | 0.97, 0.74, 0.62 | 504 ピーチ | 3.7 | 504 ピーチ 3.7 / 529 ベージュ 11.3 / 520 キャメル 18.8 |
| | 藤鼠 | 0.80, 0.64, 0.57 | 521 グレージュ（分けた） | 12.3 | 504 ピーチ 9.1 / 521 グレージュ 12.3 / 529 ベージュ 14.3 |
| | 朱 | 0.94, 0.47, 0.35 | 506 オレンジ | 11.5 | 506 オレンジ 11.5 / 505 ローズ 13.6 / 504 ピーチ 19.5 |
| | 黒 | 0.18, 0.12, 0.12 | 519 焦げ茶 | 12.2 | 519 焦げ茶 12.2 / 524 黒 12.3 / 526 チャコール 13.6 |
| 八つ金剛組 S・Z（レシピ本 p.8 の a と S） | 淡い黄 105 | 0.95, 0.90, 0.75 | 530 薄黄緑 | 6.7 | 530 薄黄緑 6.7 / 501 オフホワイト 7.7 / 529 ベージュ 8.7 |
| | 橙 108 | 0.90, 0.64, 0.49 | 504 ピーチ | 8.5 | 504 ピーチ 8.5 / 520 キャメル 13.6 / 529 ベージュ 14.2 |
| 八つ金剛返し組（教科書 p.38） | 桃 | 0.95, 0.78, 0.78 | 504 ピーチ | 10.8 | 504 ピーチ 10.8 / 529 ベージュ 15.8 / 522 ライトグレー 17.5 |
| | 淡い黄 | 0.94, 0.84, 0.69 | **501 オフホワイト**（作者: 写真の2色の差に近づける） | 11.6 | 529 ベージュ 3.4 / 504 ピーチ 8.9 / 501 オフホワイト 11.6 |
| 丸四つ組 b（レシピ本 p.10） | 白 163 | 0.94, 0.95, 0.96 | 501 オフホワイト | 9.1 | 501 オフホワイト 9.1 / 522 ライトグレー 13.3 / 512 水色 16.3 |
| | 藤鼠 148 | 0.62, 0.55, 0.60 | 523 グレー | 13.7 | 523 グレー 13.7 / 521 グレージュ 13.9 / 505 ローズ 18.1 |
| 江戸八つ組（教科書 p.64） | マゼンタ | 0.87, 0.41, 0.59 | 505 ローズ | 10.4 | 505 ローズ 10.4 / 523 グレー 18.0 / 511 紫 27.1 |
| | シアン | 0.15, 0.64, 0.76 | 528 ターコイズ | 11.9 | 528 ターコイズ 11.9 / 512 水色 16.1 / 523 グレー 19.7 |
| | 黄緑 | 0.68, 0.75, 0.57 | 517 薄緑 | 7.7 | 517 薄緑 7.7 / 530 薄黄緑 10.7 / 522 ライトグレー 14.3 |
| | 生成り | 0.90, 0.89, 0.86 | 501 オフホワイト | 6.1 | 501 オフホワイト 6.1 / 522 ライトグレー 9.1 / 529 ベージュ 10.8 |

- **作者の決定で差し替えた2つ**（2026-09-26）: 八つ金剛返し組の淡い黄と丸源氏組の生成りを 501 オフホワイトにした。
  **写真の2色の差に近づけるため、いちばん近い色ではない。** 規則で選んだ 529 ベージュは、もう一方の糸の 504 ピーチと ΔE 8.0 で、
  返し組ではカードでも3Dでも螺旋がほとんど見えなかった。501 と 504 の差は 18.2（写真の2色の差は、返し組の淡い黄と桃 18.9、
  丸源氏組の生成りとサーモン 16.9。38色の版の返し組は 25.0）。差し替えは規則で選んだあとに当てる（`choose_colourings.py` の
  `AUTHOR`）ので、丸源氏組のローズは規則で選んだ 505 のまま（空いた 529 へは移さない。移すとサーモンと 8.0 になる）。
- **規則で分けたのは2つ**: 丸源氏のローズ（504 はサーモン、529 は規則の時点で生成りのほうが近い）と、平源氏の藤鼠（504 はサーモンのほうが近い）。
  分けたあと、どの配色でも違う色だった糸は違う色、同じ色だった糸は同じ色のまま（試験で確かめている）。
- **江戸八つ組は審査側の参考とマゼンタ・シアン・黄緑が同じ色**で、生成りだけ違う（審査側 522、こちら 501）。審査側の参考の値
  （生成り (0.86, 0.83, 0.79) など、Task 063 の指示書の値）と Task 063 で測った値の差で、指示書が許している違い。
- **ほぼ同点が2つ**: 平源氏の黒（519 焦げ茶 12.2、524 黒 12.3）、丸四つ組の 148（523 グレー 13.7、521 グレージュ 13.9）。
  規則どおり近いほうにした。どちらも1行の差し替えで済む。

### 3. 試験

- **`ThreadColorCatalogTests`（書き直した）**: 30色・IDが重ならない・色番号は 501〜530・並びは表のとおり・IDは色番号から・既定は
  `amerry-f-501`。`docs/colours.md` の30色の表と読み替えの2表を試験の中で読み（写さない）、並び・色番号・ID・呼び名・値（小数3桁）が
  一致すること、古い12のIDと38のIDがそれぞれ表の色になること（`natural`・`zoge` は既定の色）、古いIDが今のIDと重ならないこと。
  **古いIDで保存した作品**（Task 058 の前のスキーマで store を書く形。12色を12本、38色を16本ずつ3作品）を開くと、IDは古いまま
  表の色で描かれ、「変更あり」にならず、1本を変えて保存し直すと全部の糸が新しいIDで書かれて同じ色で読み戻る。
  **見本配色は30色のIDだけで、糸の色の分かれ方が前と同じ**（7つの組み方すべて。2.1 では 2.2 まで外し、2.2 で戻した）。
  シートと台の読み上げ。
- **形状ハッシュ**（`theFlatBraidsMeshIsTheShapeItWas`・`theRoundBraidsMeshIsTheShapeItWas`）は**固定値を触らずに通った**。
- **固定し直した値**（どれも色だけが変わったことを確かめてから）:
  - `BraidMeshHashTests.theOneWayCardsAreThePicturesTheyWere`: 2.1 で全部（八つ金剛 S・Z・返し組の自分の配色・8色・桃白青緑、
    丸四つ組の自分の配色・4色）、2.2 で自分の配色の4つ（試験が古いIDで書いた8色・4色は 2.2 では動かない）、作者の決定による
    差し替えで返し組の自分の配色をもう一度（2.1 と同じ値 `0xe0d5_b357_56d7_4fad` に戻った。38色の象牙・一斤を読み替えた色が
    ちょうど 501・504 だから）。
  - `FlatPatternUnchangedByTheThumbnailTests.theFlatPatternIsTheOneItWas`: 平源氏の模様のハッシュ（色IDの文字を足している）。2.1 と 2.2。
  - `ThreadColorRenderingTests.representativeColorsKeepTheirFixedRenderingValues`: 代表の5色を、青・桃・生成り・黒が読み替わる
    513・505・501・524 と、白の代わりに 529（白は生成りと同じ 501 になるため）。値は同じ計算を Float で写して出し、
    旧値（瑠璃）が再現することを確かめてから使った。計算は変えていない。
  - **確かめ方**（使い捨ての試験。コミットしていない）: 2.1 では、38色の見本配色を読み替えで描いたカードと、30色のIDで書いた
    カードが**画素まで同じ**。2.1・2.2 とも、どのカードも「画素ごとにどの区間が見えるか」（`shownMap`）が38色の配色と全く同じで、
    区間の色IDが1対1に対応する（2.1 の平源氏は読み替えが多対1なので1対1にならない。2.2 では1対1）。平源氏の模様は、38色の配色で
    描くと main の値 `0x954b_11c5_6215_76f5` になり、30色の配色では同じ場所に同じ区画があって色だけが違う。描き手と生成は触っていない。
- **全件（最後に1回、差し替えのあと）**: iPhone 16（iOS 18.5、`9EF8CDC3…`）、`-only-testing:KumihimoTests`、並列なし、1件 60 秒。
  **542 件中 成功537・失敗0・スキップ5**（`DRAW_SHEETS` の描画シート）、**テスト段階 150.5 秒**（試験ごとの和 131.1 秒）。
  **打ち切りは無い。**件数は Task 063 と同じ 542（引数つきの試験は1件に数える）。Bash の上限は 600000 ms を渡した
  （目安の 300000 より大きいが、実測でどちらでも足りる）。
- 途中の全件: 2.1 のあと、2.2 のあと（差し替えの前）とも 542 件中 成功537・失敗0・スキップ5（2.2 のあとはテスト段階 168.2 秒、
  機体が止まっていて起動から始まった）。そのあいだは関わる試験だけを回した。
- **差し替えのあと関わる試験を回したとき、見込みどおりの固定値の失敗1件のあと `simctl diagnose` が戻らなかった**（10分を超えた）。
  `kill -INT` で結果を書かせた（AGENTS.md のとおり）。
- `sh Scripts/check-braiding-is-general.sh` 通過。
- UI テストは、変えた `HomeFlowUITests.testSelectingThreadColorDismissesSheet` だけを単独で回して通った。全件は回していない（Task 015）。

### 4. 検証（画像は `.build/task065/` にある）

- **色選択シート**: `sheet-iphone.png`（iPhone 16 縦）と `sheet-ipad.png`（iPad Pro 11 (M4) 横）。どちらも標準・最大
  （`accessibility-extra-extra-extra-large`）・ダークで、先頭と、下まで送ったところ。列は iPhone 3列・iPad 4列、最大では
  1列・2列。見出し「ハマナカ アメリーエフ《合太》」、色番号が主で呼び名が添え書き、下の注記、既定の 501 に印。薄い色
  （501・530・502）にも、ダークでの濃い色（524・526）にも丸の縁が見える。撮るための UI テストは使い捨てで、見出し・注記・
  「501、オフホワイト」「529、ベージュ」「524、黒」の読み上げのボタンがあることも確かめた（6回とも通過）。文字サイズと外観は元
  （large・light）に戻した。
- **前後の図**: `before-after.png`（`Scripts/task065/compose.py`、Task 063 の作り方のまま）。7つの組み方の一覧カードと、
  江戸八つ組・丸源氏組・平源氏組・八つ金剛返し組の3D。前は main（`39b91ef`）を別の作業木で組んだもの、後はこの枝の作者の決定に
  よる差し替えのあと。同じ機体で撮った。**江戸八つ組はマゼンタとシアンが戻って写真に近く、はっきりした**。丸源氏組は紺が
  チャコールになり、V の列がローズ・ピーチ・オレンジ・オフホワイト。八つ金剛返し組はオフホワイトとピーチの螺旋が見える
  （差し替えの前の 529 では、ほとんど見分けられなかった）。八つ金剛 S・Z は薄黄緑とピーチで、前より淡い。

### 5. 判断が要る点・確かめていないこと

- **（決まった）八つ金剛返し組の2色が近すぎた**: 淡い黄 529 ベージュと桃 504 ピーチの差が ΔE 8.0 で、螺旋がほとんど見えなかった。
  (a) このまま、(b) 淡い黄を 501、(c) 桃を 505 を挙げ、**作者が (b) を選び、丸源氏組の生成りも 501 にした**（同じ 8.0 の近さ。2 の表）。
- **台の糸の読み上げ**を「糸3、501、オフホワイト」にした（仕様の例は「糸3、青」で、12色の頃の形）。呼び名だけだと公式名のように
  聞こえるので、シートと同じ形にした。
- **シートの注記の文言**はこちらで決めた（指示書の例は「色番号はハマナカ アメリーエフ《合太》。呼び名は目安です」。見出しに製品名が
  あるので、注記は「番号はハマナカの色番号です。呼び名は目安で、公式の色名ではありません。」）。
- **（直した）`docs/colours.md` の「12色はすべて別の色に移る。」**: 表では `white` と `natural` がどちらも 501 になる。審査側の
  書き間違いで、作者の指示で「白（`white`）と生成り（`natural`）はどちらも 501 に移る。ほかの10色はすべて別の色に移る。」に直した。
  **仮の12色で保存した作品で、白と生成りの糸は同じ色になる**（表のとおり）。
- 12色を見分けるプレビューの `colorfulAssignments` だけは、`natural` を表の 501 ではなく 529 で書いた（プレビュー用のデータで、
  保存された作品の読み替えではない）。
- 見ていない: 実機、iOS 17、実機の VoiceOver、UI テストの全件。
