# Task 037: 長手は積み重ねで与え、断面を最短で解く

- 状態: **037-1 を回して止まった**（2026-09-13。**表面の非貫入は 0 になったが、平源氏は潰れず、丸源氏も
  丸のままにならない——留まりが倒れ込み、両方の組が絡まりになる。畳みから始めても保たれない。**止まる条件 (d)。
  結果は末尾「037-1 の結果」。その前に「037-1 着手前の確認」と「作者の判定（2026-09-13）」。
  **037-2 以降には着手していない**）
- 優先度: **編み出せる版の本線。初期リリース（Task 025 系）には触れない**
- 作成日: 2026-09-12
- 前提: **Task 036 は 036-1 で閉じた**（`tasks/036-braid-under-the-two-weights.md` 末尾「作者の判定」）。
  **新しいセッションで始めてよいように書いてある。** ただし次を先に読むこと
  1. `CLAUDE.md` と `AGENTS.md`
  2. `docs/architecture.md` の「組み台の力学（作者の前提）」「正本の読み方（作者の指示）」
     「積み重ねの模型（確定版・密に積む）」「山は糸の半径から出る」「二つの錘の下で組む（作者の決定）」
     ——**とくにその末尾「判定（2026-09-12）— 摩擦の読み」**
  3. `docs/tasks/020-general-braid-simulator.md` の「これまでの誤り」（**5・9・14・16・17・18**）と
     「方針（作者の決定、2026-09-07）— 最終実装は球の連鎖による物理を中核にする」
  4. `docs/tasks/021-depth-by-relaxation.md` の「021b-4 の結果」（**同じ高さで交わる2本は長手を固定した
     ままでは通れない。残差 √2 − 1**）と「021c の結果」
  5. `docs/tasks/023-taut-thread-construction.md` の「023-1 の続き・第2版」（**全部の対を同時に射影すると
     52 回 0.1 秒で残差が消える**）と末尾「引き継ぎ」（**留まりが膨らむと鎖が伸びる。余りが無い**）
  6. `docs/tasks/024-crest-by-half-diameter.md` の「024 を区切る」（立つもの・保留するもの）
  7. `docs/tasks/036-braid-under-the-two-weights.md` の「036-1 の結果」と「作者の判定」

## 作者の判定（2026-09-12）— 摩擦の効果を「乗られた糸は長手に動かない」として入れる

036-1 が示したのは、**摩擦の無い糸は張力 T を端まで丸ごと伝える**ということである。縦に走る糸 1 本に
つき T が固定部を上へ引き、16T = 1600 g 対 W = 570 g で束は浮く。021c は W を体積力にして束を潰し、
036 は T を端の力にして束を浮かせた。**逆向きの二つの失敗は、摩擦が無いという一つの不在から出ている。**
実物では張力が交差を越えるたびに摩擦で減り（キャプスタン）、束の奥へは届かない。

**摩擦を係数として入れる道は採らない。** μ は測って決める材料の定数になり、摩擦のある釣り合いは履歴に
依るので時間刻みのある solver が要り、ピッチは μ と錘の比の関数になる。避けたもの全部が戻る。

**摩擦の効果だけを入れる。** 摩擦がしているのは「**一度乗られた糸は長手に動かない**」ことである。
それは**積み重ね模型そのもの**——場所ごとに層を数え、各糸の各区間に長手座標を与える。この定義は平源氏の
実測ピッチと +2.3% で合い、丸源氏も帯の内側にある（`architecture.md`「積み重ね模型の適用範囲」の訂正）。
**物理に残す仕事は断面だけである**——長手を与えたまま、横方向の位置を「全糸が同時に最短」で決める。

これは 2026-09-07 の分業「**長さは与える／面の模様は占有履歴／断面・深さ・膨らみ・傾きの向きは球の
連鎖の緩和**」そのものであり、今日の結果でその分業に物理の理由が付いた。**「適切なオモリ ＝ 最短で
結ぶ」はそのまま断面の式になる。W も T も入らない。**

**芯の重なりは許す**（数えて記録する）。023 が最後に投げた問いへの判定である。芯は描画にも模様にも
上下にも出ない。024 が丸源氏 32/32 を出したときも芯は 89 対重なったままだった。

## この段の考え

**023 の構成に、横方向の自由と、鎖の余りの出入りを足す。それだけである。**

023 は積み重ね模型で長手を与えて構成し、そのあと射影で重なりを消そうとして、二つの所で止まった。
**留まりが膨らむと自分の鎖が伸び、構成には回せる余りが無い**（両端が渡りに留められている）。
そして**芯の渡りどうしが同じ高さで交わる**（021b の √2 − 1）。前者は 036 が書いた「縁での出入り」
（余りは玉へ、不足は玉から）で解ける。後者は「芯の重なりは許す」で問いから外れる。

**断面は入力にしない。** 023/024 は `BraidCrossSection` の畳みと面積保存から断面を**与えて**いた。
037 は**両方の組を筒（種の輪）から始め**、平源氏が潰れ、丸源氏が丸のまま出るかを見る。
畳みの導出（縦糸の反射から縁が出る）は**長手の勘定（越える場所）にだけ使う**——それは手順表からの
導出であって形の入力ではない。

**潰れる理屈**（予言として書く。合わせにいかない）: 平源氏は 1 サイクルに腹を横切る弦が 12 本。
うち 8 本は縦糸の表裏の入れ替えで、二層に潰れていれば厚み 2 d ぶんを渡るだけだが、筒のままなら
直径 5 d を突っ切る。横糸の弦 4 本は縁に留まる。**弦を短くしたがる力が南北の留まりを寄せる。**
丸源氏は弦が四方向に走るので寄せる向きが無い。

---

## 1. 模型（**これが定義。読みを変えたら別の模型である**）

単位は d（糸の直径）。**台は模型に入らない**（縁の角度が玉の引く向きを与えるだけ。下記）。

### 入力（**すべて既存の導出。手で置く値は d だけ**）

- `BraidStand`（32 の角度）と `BraidMethod`（bookC の順序、1 手 1 本。八つ金剛は 1 手 1 瞬間）。
- **長手座標**: 積み重ね模型（確定版・密に積む）。**bookC の 1 手ごとに Z、場所ごとの層 × d。**
  各糸の各手について、出発 z（出発場所でのその時点の層数 × d）と到着 z（到着場所での到着層 × d）。
  **閉じ移動は紐を送らない。** 越える場所は導出した断面の上で測る（畳みがあれば幅の軸、筒なら
  リングの弧）——**023 の `construct.py` の読みに、024 の `hand_over` を掛けたもの**（2026-09-13 に直した。
  末尾「作者の判定」1）。**同じ場所を前後で引き継ぐ 2 本は、後から来た糸の到着層が前の糸の出発層の一つ上**
  （`architecture.md`「山は糸の半径から出る」）。**024 の `wefts_apart` は数えて記録する**（直さない。
  同じ高さで腹を横切る対は横に分かれる——それは solver の仕事）。**1 サイクルは k·d。k は数えて出す。**
- **横方向の初期位置**: **筒**。16 の場所を種の輪（1 辺 d の正 16 角形、半径 2.5629 d）に、
  **断面の環順**（`braid_geometry.RING_*`、`BraidCrossSection` の巡回列。023 の `surface(ring, folded=False)`
  と同じ並び）で等間隔に置く。**`stand.bundle_angle`（縁の順）ではない**（2026-09-13 に直した。末尾
  「作者の判定」2）。丸源氏では両者は同じ。**平源氏も筒から始める。** 巡回順は入力（宣言または既定）で
  あって、037 が決めるものではない。

### 糸

- **直径 d のカプセルを繋いだ鎖**（球の中心が d 間隔）。伸びない。質量を持たない。
- 区間は二種類。**留まり**: 場所 P に留まる区間、到着 z から出発 z まで。**渡り**: 出発 (P, z_dep) から
  到着 (Q, z_arr) への直線、断面の内側を通る。閉じ移動は同じ高さで隣の場所へ。
- **自由部**: 最後の留まりの上端から、その糸の縁の角度の**穴の縁の点**への直線。玉の引く向きを与えるため
  だけにある。**描画には使わない。** 穴の縁の点は**紐の座標**（積み重ね模型の z。最初の層が 0）で置く
  （2026-09-13 に直した。末尾「作者の判定」3）: 半径 `stand.hole + stand.fillet` = 8.5 d、角度はその糸の
  縁の角度、**高さは 柱の最高点 ＋ 1.658 d ＋ 0.5 d**。柱の最高点は全糸の最大 z（最後の層）、1.658 d は
  組み点から鏡の上面までの高さ（`stand.braiding_point_depth()` の符号を返したもの）、0.5 d は糸の半径。
  **台の座標に写すなら z_台 = z_紐 − 柱の最高点 − 1.658 d。**

### 自由度（**ここが 021b・023 と違う**）

- **z はすべての球で固定。** 区間の両端の z は積み重ね模型が与え、区間の途中の球の z はその間の
  線形補間。**置き直し（respace）のあとも、球の z は区間の端の z から補間し直す。**
- **x, y はすべての球で自由。** 留まりも渡りも。留まりを表面に留める拘束は無い——表面は
  **非貫入の帰結として出る**（渡りが内側を通り、留まりはその外へ押される）。
- **自由部の遠い端（穴の縁の点）だけ固定。**

### 力

**無い。** 全糸が同時に最短になる（022 の `shrink`: 球を隣の中点へ寄せる）。これが一様な張力の釣り合いで
あり、**T の絶対値は消える**（`architecture.md`「二つの錘の下で組む」）。**W は入らない**——長手が
与えられているので、W が決めるものは残っていない。

### 拘束（**二つだけ**）

1. 隣り合う球の距離が d（`space_out`）。**区間ごとに、その端点のあいだで d 間隔に置き直す。長さは保存
   しない**——余りは玉へ、不足は玉から（036 の「縁での出入り」と同じ。ただし出入りは各区間の端で
   起きる。**これが 023 の「余りが無い」の答えである**）。
2. 太さ d のカプセルどうしが通り抜けない（線分間の最短距離、`Scripts/task021/given_length.py`）。
   **押し離しは両側に振り分ける**（`push_apart`）。**z は動かさない**——押し離しの z 成分は捨て、
   x, y 成分だけを当てる。

**芯の渡りどうし**（両方が渡りで、どちらの球も束の外周より内側にある対）**も射影に含める**。
**ただし収束の判定と合否からは外す**（下記 5.）。横に逃げる余地があれば逃げ、無ければ残る。
**残った数を記録する。**

### 解き方

**一度に全部を構成し、そのあと射影する**（023 第 2 版と同じ。全部の対を同時に）。z が与えられて
いるので、手ごとに置く必要は無い——履歴は z に入っている。**1 サイクルずつ足して回す変種は
時間の対策としてだけ許す**（答えが同じであること）。

  外側の一歩   `shrink`（x, y のみ）→ 区間ごとに置き直し（z は補間）
  内側        `space_out` と `push_apart`（x, y のみ）を残差 0.01 d 未満まで（上限あり）
  収束        表面の対（留まり–留まり、留まり–渡り）の残差で判定する。芯の渡り–渡りは数えるだけ
  頭打ち      `MOST` = d/4（1 歩で糸 1 本ぶんを越えない。トンネリングを防ぐ）

### 解法の設定（**模型の定数ではない。固定して記録し、答えが依存しないことを確かめる**）

  `PROJECTIONS` 2、`SETTLED` 0.01 d、`MOST` d/4、`SHRINK` 0.1（022 と同じ）
  種の輪の半径      2.5629 d（主）と 4 d
  一度に全部 ／ 1 サイクルずつ
  芯の渡り–渡りの射影  含める（主）／外す
  留まりの上端への外向きの引き  無し（主）／各留まりの出発の角へ縁の角度の向きに、3 段（10 倍ずつ）

**最後の項目について**: 実物では留まりの上端は、それが組み点にあった瞬間に自由部から外向きに引かれ、
そのまま摩擦で固まった。主の設定はそれを持たず、密に詰めた極限を取る。**引きを入れても断面が
変わらなければ、極限で足りる。変われば量を報告する**（021d の合格条件「引きの大きさに依らない」と
同じ形）。

---

## 2. 予言（**回す前に書く。合わせにいかない**）

1. **平源氏は筒から潰れる。** 断面の幅÷厚みが 2 を超え、面は南北（縦糸）、縁は東西（横糸の折り返し）
   ——**`BraidCrossSection` が縦糸の反射から導いた畳みと同じ向き**。横糸の弦は縁に留まる。
   厚みは 2 d、横糸の弦が通る所だけ 3 d（024 の読み）。
2. **丸源氏は丸のまま。** 幅÷厚み ≈ 1、外径 ≈ 6.13 d（正 16 角形。留まりが互いに接する下限）。
3. **種の輪の半径を変えても同じ所へ落ちる。** 4 d から始めても 2.5629 d と同じ断面になる。
4. **留まりは斜めに走る。** 閉じ移動で 1 ノッチずれたぶん、到着点から出発点へ直線で傾く。
   平源氏は写真の斜めの糸筋（bookA p96・p97 拡大、025-4 が求めたもの）、丸源氏は Task 004 の周期 2 の
   傾きの向きと並べる。
5. **山は出る。** 渡りが留まりの下を通る所で留まりが外へ d/2 押される——**024 が構成で置いた山が、
   置かずに出る。**
6. **表面の非貫入は 0 になる**（留まり–留まり、留まり–渡り）。**芯の渡り–渡りは丸源氏で残る**（数を記録）。
7. **面の見え。** 丸源氏 32/32（鏡は違いを生まない——Task 034）、平源氏 p97 の 3 実験。
8. **平源氏の幅÷厚み**は 024 の構成 2.66〜2.70 と実測 3.3359 のあいだに落ちる（**記録。合否なし**）。
9. **時間は秒の桁。** 023 第 2 版は射影 52 回 0.1 秒だった。

---

## 3. 実装（**023 と 036 の流用。新しく書くのは自由度の縛りだけ**）

  `Scripts/task037/build.py`     構成。**`Scripts/task023/construct.py` の長手の読み**（`trajectories`・`pieces`）に
                                 **`Scripts/task024/build.py` の `hand_over` を掛け、`wefts_apart` で数える**。
                                 横方向は断面の環順で種の輪に置く。畳みは越える場所の勘定にだけ
  `Scripts/task037/settle.py`    射影。`Scripts/task022/taut.py` の `shrink`・`space_out`・`push_apart` を
                                 x, y に限り、置き直しのたびに z を区間の端から補間し直す。
                                 表面の対と芯の対を分けて残差を返す
  `Scripts/task037/measure.py`   (a)〜(e) の数字。断面（幅・厚み・比、平均と最大）、外径、留まりの傾き、
                                 山の高さ、芯の残り、所要時間。**測り方は 022-3・036 のものをそのまま**
  `Scripts/task037/draw.py`      `Scripts/task024/render.py` で描く。読まない
  `Scripts/task037/README.md`    走らせ方、設定、単位

**流用**（書き直さない）: `Scripts/task021/braid_geometry.py`・`occupancy.py`・`given_length.py`、
`Scripts/task022/stand.py`（種の輪の角度と半径、穴の縁の点）、`face.py`、`crossings.py`、`figures.py`、
`Scripts/task024/render.py`（描いて読む。`docs/measurement-procedures.md` 5 節）、
`Scripts/task036/settle.py` の**縁での出入り**の書き方。

**書き出し**: 022 の dump 形式（`Scripts/task021/`・`task022/` の道具が読める）。**糸ごとの中心線と d**
が出力であり、これが 037-4 で Swift の `BraidCentrelines` へ渡るものである。

---

## 4. 検査（**構成の後に測る。直さない**）

- **(a) 表面の非貫入**: 留まり–留まり、留まり–渡りで 0.01 d 超の対が 0。
  **芯の渡り–渡りは数えて記録**（最深と対の数、合否なし）。
- **(b) 反転**: 運ばれた手で数えて 0（`Scripts/task022/crossings.py`）。**z が与えられているので構成上 0。
  0 でなければ実装の誤り。**
- **(c) 面の見え**: 丸源氏 32/32、平源氏 p97 の 3 実験。`Scripts/task022/face.py` と、
  `Scripts/task024/render.py` で描いて読んだ結果の両方。
- **(d) 潰れ**: 平源氏の断面 幅÷厚み **2 以上**（023 構成 4.00、024 構成 2.66〜2.70、023 射影後 1.51、
  036 断面 1.07、実測 3.3359）、
  面が南北・縁が東西。丸源氏の幅÷厚み **0.9〜1.2**、外径 **8 d 未満**。
- **(e) 設定に依らない**: 種の輪の半径、一度に全部／1 サイクルずつ、芯の射影の有無、外向きの引きの
  3 段を振っても (a)(c)(d) が変わらない。**断面の数が変わればその量を報告する。**

**記録（合否なし）**: 断面（幅・厚み・比）、外径、留まりの傾き（角度と向き。丸源氏は列の偶奇で交互か）、
山の高さと幅（024 の d/2・1.73 d と並べる）、平源氏の膨らみ（0.45）、丸源氏の見かけの山密度（1.8〜2.15）、
芯の残り、所要時間。

---

## 5. 段（**各段の終わりに止まって報告する。承認を得てから次へ**）

  **037-1**  この指示書の承認。`Scripts/task037/` を書く。**平源氏 Fig.20 と丸源氏 Fig.32 を 2 サイクル**、
             筒から、主の設定で。図: 上・横・断面 3 枚・展開面（糸番号入り）、`render.py` の絵。
             **(a)(d) の数字を先取りで出す。止まる。作者が見る。**
  **037-2**  **4 サイクル**（最初と最後の 1 サイクルは端の除外区間）。**(a)〜(e) すべて**と記録。
             感度は平源氏で全部、丸源氏は種の半径だけ。**止まる。**
  **037-3**  **十六金剛ねじり組**（作者が示した本の p.65）。手順表を絵から起こす（Task 008 の方法、
             「詰め直しの入り方」の判定、**作者の判定を仰ぐ**）。回す前に予言を書く: **平たい・らせん**
             （横方向の位置が z とともに回る）、本の「横方向の糸は 2 倍程度長く必要」は縁の糸が軸から
             遠いらせんを走ることの帰結で、幅 w・1 回転 L として √(1 + (πw/L)²) = 2 なら **L ≈ 1.8 w**。
             **らせんが出るか、回転の速さがこれに近いかを記録する。出なければ、そのとき初めて糸ごとの
             張力の比を再検討する**（今は入れない）。同じページの十六金剛返し組は手順表の話で物理の問いは無い。
  **037-4**  **Swift。** `Kumihimo/Domain/Braiding/` に一般実装（**名前の番人**）、中心線を紐として描く。
             **Task 025 の族の描き手は退役させない**——初期リリースはそのまま。037 は**編み出せる版**の
             描き手として並べる。**指示書はこの段の前に改めて書く。**

**時間**: 秒の桁のはず。**分の桁になったら理由を報告する**（023 と同じ）。

---

## 6. 止まる条件（**直しにいく前に止めて報告する**）

- **(a) 表面の対が消えない** → 区間の種類と場所で分けて報告する。**z を動かして逃がさない**（それは
  別の模型である）。
- **(b) 反転が 0 でない** → 実装の誤り。
- **(d) 平源氏が潰れない**（筒のまま、比 2 未満） → 断面の数字と図を並べて報告する。**畳みから始めた
  変種の結果も添える**（それが潰れていて筒からは潰れないなら、答えが種に依っている——(e) の不合格として
  報告する）。
- **(e) 設定で答えが変わる** → 設定と答えの対応を表にして報告する。
- **承認された設計が破綻したら、代替せずに止まる。**

---

## 守ること

- **規則の追加禁止。力は無し、拘束は二つ。定数は d だけ。** 糸ごとの張力の違いは入れない
  （型として持てる形でもよいが、既定は一様で、使わない）。**見た目に合わせる定数禁止。**
- **z を動かさない。** 長手は積み重ね模型の定義であり、solver の出力ではない。
- **測り方を新しく作らない。** `docs/measurement-procedures.md`。
- **product code は変更しない**（037-4 まで）。**テストは走らせず、その旨を報告に書く。**
- `git add -A` を使わない。**docs / Scripts のコミットを分ける。**
- **一つの実行に一つの待ち**（`AGENTS.md`）。
- `.build/` に判断に効く事実を残さない。数字は本書へ写す。
- **各段の終わりに止まる。一度に複数の段をやらない。**

---

# 037-1 着手前の確認（2026-09-13）— **指示書と他の文書の食い違いを 3 件見つけた。推測で埋めずに止まる**

branch `claude/task-037-cross-section` を `claude/task-036-two-weights`（e346fb2）から切り、「前提」の
読み物を読んだ。**長手座標の入力（023 の `trajectories`）を先に確かめたところで、模型の入力が
一つに決まらない所が 3 つ出た。** どれも z を固定する模型では**入力そのもの**なので、片方を選んで
回すと別の模型になる（本書「読みを変えたら別の模型である」）。**`Scripts/task037/` は書いていない。**

## 1. 引き継ぎの高さ（**止まる理由**）

- **本書**: 長手は「023 の `construct.py` の読みそのまま」（1.「入力」、3. `trajectories`・`pieces`）。
- **`architecture.md`「山は糸の半径から出る」**（1196 行）と **024「作者の決定（追記、2026-09-08）」**:
  「**同じ場所を前後で引き継ぐ2本は、積み重ね模型のとおり、後から来た糸の到着層が前の糸の出発層の
  一つ上である。端どうしは d 離れる。**」024 はこれを `Scripts/task024/build.py` の `hand_over` で実装した。
- **023 の `trajectories` はそうなっていない。** 同じ場所で前後する 2 本の、前の糸が離れる z と
  後の糸が着く z を突き合わせた:

| | 同じ z で引き継ぐ（最初のサイクル／本体） | 前の糸が離れる前に着く（同） | `hand_over` が上げる到着 |
|---|---|---|---|
| 平源氏 2 サイクル | 12 ／ **8** | 6 ／ 0 | **26**（024-1' の記録「引き継ぎで上げた層 26」と一致） |
| 丸源氏 2 サイクル | 8 ／ 0 | 12 ／ 0 | 20 |
| 平源氏 4 サイクル | 12 ／ **24** | 6 ／ 8 | 50 |
| 丸源氏 4 サイクル | 8 ／ 0 | 12 ／ **16** | 36 |

  **平源氏の本体の「同じ z」は z = 2, 5, 8, … に毎サイクル 8 件ずつ出る。端の効果ではない。**
  （**023 自身の記録**「023-1 の結果」「高さの読み方」は「場所を引き継ぐ糸は前の糸が終わった高さから
  始まらない」と書いているが、コードの出力は上の表である。記録のみ。）

**選択肢**: (a) 本書の文言どおり 023 のまま——同じ z の 2 本は x, y で横に分かれるしかない。
(b) `architecture.md` の文言どおり `hand_over` を足す——本書の「023 の読みそのまま」から外れる。

## 2. 平源氏の筒の並び（**止まる理由**）

- **本書**: 「16 の場所を種の輪（1 辺 d の正 16 角形、半径 2.5629 d、**`stand.bundle_angle`**）に置く」（1.「入力」）。
- **`architecture.md`「台の座標と紐の座標は別である」**（1153 行）と **022「022 から立って残るもの」**:
  「**束の入口は紐の断面の環順に等間隔**」。
- **`stand.bundle_angle` は台の位置番号の順（縁の順）に並べる。** 平源氏の断面の環（`RING_HIRA`）とは
  順が違う。**環で隣り合う 16 対のうち、`bundle_angle` の並びでも隣り合うのは 10 対**（丸源氏は 16 対）。
  `stand.py` の doc comment は「縁の順が断面の環である」と書くが、**平源氏では成り立たない**
  （`architecture.md` が平源氏について記録している「円盤の隣接と紐の隣接が一致しない」と同じ事柄）。

**選択肢**: (a) `stand.bundle_angle`（縁の順）。(b) 断面の環順（023 の `surface(ring, folded=False)` と同じ）。
**効くのは種だけ**だが、種に依らないかを確かめるのが予言 3 と (e) なので、主の実行はどちらかに決まって
いなければならない。

## 3. 自由部の遠い端の高さ（**止まる理由**）

- **本書**: 自由部は「最後の留まりの上端から、その糸の縁の角度の**穴の縁の点**（`stand.hole + fillet`、
  **z = +0.5 d**）への直線」（1.「糸」）。
- **z = +0.5 d は `stand.py` の座標**（鏡の上面が 0、紐はその下へ垂れ、組み点は −1.658 d）。
  **長手は 023 の座標**（最初の層が 0、新しい層ほど上）。**二つの座標の原点の合わせ方が書かれていない。**
- **023 の座標にそのまま置くと**、2 サイクルで紐は z 0〜8 d、**最後の留まりの上端は z 3〜8 d**
  （平均 平源氏 6.50、丸源氏 5.50）なので、**穴の縁の点は 2.5〜7.5 d 下**になる。
  **自由部は外へ上るのではなく、紐の脇を下る。** 玉が留まりの上端を引く向きがこの線で決まる。

**選択肢**: (a) 文字どおり z = +0.5 d。(b) 台の座標を紐の新しい端に合わせる（**どこに合わせるかは
書かれていない**）。

## 4. 記録値の出どころ（**止める理由ではない**）

4.「(d) 潰れ」の「**024 構成 4.00**」は、**023 の構成**の値である（「023-1 の結果」: 8.00 × 2.00 d、比 4.00）。
024 の構成は **2.66〜2.70**（「024 を区切る」、楕円）で、本書の予言 8 はそちらを引いている。
**合格の閾値「2 以上」は変わらない。**

## 状態

**037-1 は着手していない**——`Scripts/task037/` は書いておらず、何も回していない。**1〜3 の判定があれば、
残りは本書のとおりに書ける。** 037-2 以降には触れていない。

**product code は変更していない。よってテストは走らせていない。**

---

# 作者の判定（2026-09-13）— **3 件とも答えた。037-1 へ進む**

**3 件とも指示書の書き誤りであり、模型の問いではない。** 1 と 2 は `architecture.md` に作者の決定が
すでにあり、指示書がそれと違うことを書いていた。3 は座標の原点を書き忘れた。**本文はこの判定で直してある**
（1.「入力」「糸」、3.「実装」、4.「(d)」）。

## 1. 引き継ぎの高さ → **(b) `hand_over` を掛ける**

正本は `architecture.md`「山は糸の半径から出る」の追記——**「同じ場所を前後で引き継ぐ 2 本は、積み重ね
模型のとおり、後から来た糸の到着層が前の糸の出発層の一つ上である。端どうしは d 離れる」**（作者の決定、
2026-09-08）。024 はこれで両方の正本と一致した（平源氏 p97 両面、丸源氏 32/32）。**「023 の読みそのまま」
と書いたのは、024 がその上に置いた作者の決定を落とした書き誤りである。** 023 自身の記録「場所を引き継ぐ糸は
前の糸が終わった高さから始まらない」は**書いてあることとコードの出力が食い違っている**ので、023 の記録の
側に「コードは同じ z を出す（037 の着手前の確認）」と読む注を付けておくこと（本文は変えない）。

**`wefts_apart` は直さずに数える。** 同じ高さで腹を横切る 2 本（024-1'' の 8 か所、表裏の入れ替えの対）は、
024 では `side_step` で ±w/2 に振り分けた。**037 では横方向が自由なので、振り分けは solver の仕事**——
射影が横へ逃がす。数は記録し、逃げ切れなければ (a) の芯の残りとして数える。

## 2. 平源氏の筒の並び → **(b) 断面の環順**

正本は `architecture.md`「台の座標と紐の座標は別である」——**「束の入口は紐の断面の環順に等間隔」**。
`stand.bundle_angle` の doc comment「縁の順が断面の環である」は**丸源氏でだけ成り立つ**（平源氏は
16 対のうち 10 対。`architecture.md` が「円盤の隣接と紐の隣接が一致しない」と記録しているとおり）。
**指示書が `bundle_angle` と書いたのは書き誤りである。** `stand.py` の doc comment は 037 では直さない
（022 の記録。報告に挙げるだけ）。

**巡回順は 037 の入力であって出力ではない。** 平源氏の巡回順は宣言（bookA p96）、丸源氏は台の周の順、
編み出した組み方は宣言が無ければ台の周の順（`architecture.md`「断面は巡回列である」）。**037 が試すのは
形（潰れるか丸か）であって、順ではない。** 順を物理が決められるかは別の問いで、ここでは立てない
（「これが決めないこと」に足す）。

## 3. 自由部の遠い端の高さ → **(b) 紐の座標で置く。合わせ方は「柱の最高点を組み点に」**

長手は紐の座標（最初の層が 0、新しい層ほど上）。台の座標（鏡の上面が 0、紐は下へ）との関係は、
**柱の最高点（全糸の最大 z）が組み点 −1.658 d に来る**ことで決まる——組み点は糸が束を離れる所であり、
それは最後の層である（022・036 の `tops` と同じ読み）。**したがって穴の縁の点は、紐の座標で
z = 柱の最高点 ＋ 1.658 d ＋ 0.5 d、半径 8.5 d、角度はその糸の縁の角度。** 自由部は外へ上る。
**「+0.5 d」とだけ書いたのは原点を書き忘れた書き誤りである。**

## 4. 記録値の出どころ → **直した**

「024 構成 4.00」は 023 の構成の値だった。4.「(d)」を「023 構成 4.00、024 構成 2.66〜2.70」に直した。
閾値「2 以上」は変わらない。

## これが決めないこと（追記）

- **断面の巡回順を物理が決められるか。** 037 は宣言・既定の順から始める。
- **`stand.bundle_angle` の doc comment**（022 の記録のまま）。

**037-1 はこの判定で進めてよい。** `Scripts/task037/` を書き、本文 5.「段」の 037-1 のとおりに回す。

---

# 037-1 の結果（2026-09-13）— **平源氏は潰れない。丸源氏も丸のままにならない。留まりが倒れ込み、両方の組が絡まる。止まる**

**止まる条件 (d)「平源氏が潰れない」に当たる。** 指示どおり断面の数字と図を並べ、**畳みから始めた変種を添える。**
**畳みから始めても潰れた形は保たれない**ので、答えが種に依っているのではない。**模型は変えていない。**

## 作ったもの（`Scripts/task037/`）

  `build.py`    構成と harness。023 の `trajectories`・`pieces` に 024 の `hand_over` を掛け（判定 1）、`wefts_apart`
                を数える。種は**断面の環順の筒**（判定 2。回る向きと位置 1 が保つ角度は `stand.bundle_angle` のもの）。
                自由部の遠い端は**紐の座標で 柱の最高点 ＋ 1.658 d ＋ 0.5 d**、半径 8.5 d、糸の縁の角度（判定 3）。
                `--seed fold` は止まる条件 (d) が求める変種。022 の dump を台の座標で書く
  `settle.py`   022 の `shrink`・`space_out`・`push_apart` を **x, y だけ**に。区間ごとに d で置き直し、z を端から補間。
                対を種類で分け、**収束と (a) は表面の対だけで見る**
  `measure.py`  (a) を対の種類ごとに、(d) を 022 `face.py`・024 の切り方で。外径、長軸と畳みの縁の向きの差
  `draw.py`     022 の `figures.py`（展開面は紐自身の半径で）と 024 の `render.py`

**実装で決めたこと（模型の定数ではない。記録する）**

- **押し離しの x, y は、z を保ったまま拘束を満たす水平距離 √(d² − gz²) を解いて当てる。** 補正ベクトルの z を
  捨てるだけだと、上下に重なった対（水平距離 0）に届かない。水平距離が 0 の対だけは、2 本の区間の中点を結ぶ
  向きで離す（022 の (1, 0, 0) と同じ役）。
- **区間の端の短い一本は、d より短くてよいが長くはならない。** 最初は 022 の縁の一本と同じく間隔の拘束から
  外していた。**両側の球が自由なので際限なく伸び、次の置き直しがそれを糸に変えた**（2 サイクルで 401 球 →
  976 球、収束しない）。**実装の誤りとして直した。**
- **芯**は、渡り–渡りの対で、4 つの球がどれもその高さ ±d の留まりの球の凸包（平面）の内側にあるもの。
- 収束の見方は 022 のまま（`STILL`・`PATIENCE`・`SWEEPS` 1000）。動きは区間ごとに長さの 5 つの割合での x, y で
  測る（置き直しで球数が増減しても比べられる）。

## 構成（入力）

| | 平源氏 | 丸源氏 |
|---|---|---|
| k ／ 柱の最高点 ／ 穴の縁の点の z（紐の座標） | 3 ／ 8.0 d ／ 10.158 d | 3 ／ 8.0 d ／ 10.158 d |
| `hand_over` が上げた到着 | **26** | **20** |
| `wefts_apart`（同じ高さで同じ列を横切る横糸の対） | **8** | 0 |
| 球 ／ 区間 | 368 ／ 96 | 344 ／ 96 |
| 構成直後の 0.01 d 超の対: 表面 ／ 芯 ／ 芯の外の渡り同士 ／ 自由部 ／ 同じ糸 | 214 ／ 540 ／ 63 ／ 95 ／ 41 | 218 ／ 328 ／ 134 ／ 24 ／ 56 |

## 走った結果（主の設定。筒から、2 サイクル）

| | 平源氏 | 丸源氏 |
|---|---|---|
| 所要 | **279.9 秒** | **263.1 秒** |
| 外側 ／ 内側（上限に当たった回） | 1000（上限）／ 175,713（644） | 1000（上限）／ 199,833（**999**） |
| 静止したか | **しない** | **しない** |
| 隣接の残差 | 9.97e-3 | **9.16e-2**（1 本が 0.01 d 超） |
| **(a) 表面の 0.01 d 超** | **0**（最深 0.003 d） | **0**（最深 0.001 d） |
| 芯の 0.01 d 超 | 0（最深 0.002 d） | 5（最深 0.063 d） |
| **(d) 断面 幅 ／ 厚み（平均）** | **15.25 ／ 12.07 d** | **15.35 ／ 11.94 d** |
| **幅÷厚み**（平均の比、断面ごとの範囲） | **1.26**（0.85〜1.75） | **1.29**（0.69〜2.17） |
| 外径 2 max(r) + d | 21.65 d | **19.08 d** |
| 紐の中心の軸からのずれ | 1.97 d | 2.27 d |
| 長軸 対 畳みの縁の向き | 90.3° 対 78.8°（差 11.5°） | — |

## 合格条件（037-1 が先取りで出すもの）

- **(a) 表面の非貫入: 合格**（両方とも 0）。芯は許して数えた——丸源氏で 5 対（最深 0.063 d）。
- **(d) 潰れ: 不合格。** 平源氏 **1.26**（2 以上が要る）。丸源氏 **外径 19.08 d**（8 d 未満が要る）、比 1.29
  （0.9〜1.2 が要る）。

## 何が起きているか（**数字と図が同じことを指している**）

**留まりが倒れ込む。** z の幅が d 以上の留まりを端から端で見ると、鉛直からの傾きは **中央値 48°**（平源氏 33 本。
10 分位 30°、90 分位 66°）、**49°**（丸源氏 18 本。31°、66°）。道のりは z の幅の **2.00 倍**（平）・**1.67 倍**（丸）。
**予言 4 の「1 ノッチずれたぶんの傾き」とは大きさが違う。**

**紐が長くなる。** 留まりと渡りの長さの和は 平源氏 **210 → 314（150 歩）→ 328 d（1000 歩）**、丸源氏
**185 → 251 → 261 d**。**留まりだけで 72 → 142 d（平源氏）。** 自由部は 115 → 70 d に縮んだ——**糸は玉から
入ってきた**。

**形は落ち着いていない。** 150 歩と 1000 歩で、平源氏の断面は 14.18 × 10.16 → 15.25 × 12.07 d、外径 18.54 →
21.65 d、中心のずれ 0.59 → 1.97 d。丸源氏も同じ向き（12.91 × 10.28 → 15.35 × 11.94 d、外径 15.53 → 19.08 d）。
平源氏 300 歩の probe では、100 歩を過ぎると**動きの中央値は 1 歩 5〜7e-3 d、90 分位 1.5〜3e-2 d**、
**最大 0.1〜0.2 d は 1〜2 本の渡り**（糸 9 の z 2.5〜2.75、のち糸 15）。**2 歩での動きは 1 歩の約 2 倍**——
行き来ではなく、さまよい続けている。**1000 歩の数字は、止まる前の一場面である。**

**図**: 平源氏の真ん中の断面（台の座標 z −5.66）は、**筒でも 2 段の平たい形でもなく**、正 16 角形の円の外へ
偏った不揃いな塊で、糸 11 が離れて立つ。**同じ断面に 1 本の糸が 4〜7 球並ぶ**（留まりが寝ている）。丸源氏も
同じ形で、糸 3 が離れて立つ。表の絵は斜めの太い筆致の絡まりで、**縦の棒は出ない**。展開面に規則は無い。

**読み（worker）**: この模型では留まりの x, y が自由で、力は無い。渡りが留まりの両端をそれぞれ別の場所へ引き、
留まりはそちらへ倒れる。倒れた糸どうしがぶつかり、z が固定なので横へ逃げ、断面が膨らむ。**「表面は非貫入の
帰結として出る」（本文 1.「自由度」）が出ていない**——渡りが内側から留まりを外へ押すより先に、留まりが場所を
離れる。

## 畳みから始めた変種（止まる条件 (d) が求めるもの）

`--seed fold`: 023 の `surface(ring, folded=True)`（8 列 × 2 段、面は d 離れ）を、幅の軸が筒の縁の場所を結ぶ
向きに、表が筒の表の側に来るよう回し、中心を軸に置いた（**長さは 1 つも選んでいない**）。場所どうしの最短距離
1.000 d。

| | 畳みから |
|---|---|
| 構成直後の 0.01 d 超: 表面 ／ 芯 | 382 ／ 342 |
| 所要 ／ 外側 ／ 内側（上限） | 117.5 秒 ／ 1000（上限）／ 89,891（6）。**静止しない** |
| (a) 表面 | **0**（最深 0.003 d）、隣接 9.96e-3 |
| **(d) 幅 ／ 厚み、比** | **17.76 ／ 10.67 d、1.66**（断面ごとに 1.08〜2.58） |
| 長軸 対 畳みの縁 | 24.5° 対 78.8°（**差 54.3°**） |
| 外径 ／ 中心のずれ | 21.50 d ／ 0.41 d |
| 留まりの傾き（中央値） | **54°**（10 分位 39°、90 分位 60°）、道のり ÷ z の幅 2.00 |
| 長さ 留まり ／ 渡り ／ 自由部 | 72.0 → 140.0 ／ 95.5 → 129.0 ／ 125.0 → 65.1 d |

**畳みから始めても潰れた形は保たれない。** 比は 2 に届かず、長軸は畳みの縁から 54° 回っている。真ん中の断面では
**糸 12 が 9 球の輪に、糸 5 が弧に丸まって塊になり、正 16 角形の円の中は空いている。** **筒からも畳みからも
同じ絡まりに行く**ので、止まる条件 (d) の「答えが種に依っている」には当たらない。

## 診断（設定を振っただけ。模型は変えていない）

**芯の渡り–渡りを射影から外した**（`--no-core`。本文の解法の設定。80 歩で打ち切り）:

| 80 歩 | 平源氏 主 ／ 芯を外す | 丸源氏 主 ／ 芯を外す |
|---|---|---|
| 隣接の残差 | 6.38e-2 ／ **9.82e-3** | 1.23e-1 ／ **8.10e-3** |
| 内側が上限に当たった回 | 79 ／ 2 | 80 ／ 1 |
| 芯の 0.01 d 超 | 2 ／ 318 | 6 ／ 356 |
| 断面 幅 × 厚み、比 | — ／ 12.11 × 9.25 d、1.31 | — ／ 10.98 × 8.98 d、1.22 |

**主の設定で丸源氏の隣接の残差が 0.01 d に届かないのは、z を固定したまま芯の対を離そうとする衝突である**
（021b-4 の √2 − 1 と同じ形）。**芯を外せば届く。膨らみは芯を外しても出る**——留まり–渡りの対が残るからで、
芯を外した平源氏では外径を決める球が紐の最上段の留まり（自由部に引かれて r 8.8 d）だった。

## 時間（「秒の桁」は外れ。理由）

外側の一歩が 0.1〜0.3 秒。**形がさまよい続けて `STILL`（1e-4 d）にも `PATIENCE` にも当たらず、`SWEEPS` 1000 で
止まる。** 丸源氏はさらに**芯の衝突で内側の射影が毎歩 200 回の上限まで回る**。023 第 2 版（52 回・0.1 秒）は
縮めを持たない射影だけで、形は構成が決めていた。037 は毎歩 縮め → 置き直し → 射影 を繰り返す。

## 予言の当たり外れ（回した範囲で）

1. 平源氏は筒から潰れる **外れ**（1.26。畳みからでも 1.66）
2. 丸源氏は丸のまま、外径 ≈ 6.13 d **外れ**（19.08 d、比 1.29）
3. 種の輪の半径を変えても同じ所へ落ちる **未判定**（037-2。筒と畳みはどちらも絡まる）
4. 留まりは斜めに走る **外れ**（傾きは 1 ノッチぶんではなく中央値 48〜49°）
5. 山は出る **未判定**（表面が出ていない）
6. 表面の非貫入は 0 **当たり**。芯の渡り–渡りは丸源氏で残る **当たり**（5 対）
7. 面の見え **未判定**（037-2。面に規則が無い）
8. 平源氏の幅÷厚みは 2.66〜3.34 のあいだ **外れ**（1.26）
9. 時間は秒の桁 **外れ**（4〜5 分）

## 図（`.build/task037-figures/{hira,maru,hira-fold}/`、git の管理外）

  `braid-section.svg` ／ `braid-above.svg` ／ `braid-hole.svg`   横・上・穴の拡大（自由部込み）
  `braid-across-{-8,-6,-4}.svg`                                 紐自身の高さで切った断面 3 枚（台の座標で z −7.66 ／ −5.66 ／ −3.66）
  `braid-unrolled.svg`                                          展開面（糸番号入り。列の半径は紐から取った）
  `painted/`                                                    `render.py` の絵。場所の角度から 16 枚とそれを並べた 1 枚、面 2 枚・縁 2 枚

## 次に決めてほしいこと（**どれも実装していない**）

**模型のとおりに組むと、留まりが場所に留まらない。** 判断の材料として事実を挙げる。

- **留まりを場所に留めているものが模型に無い。** 023 は留まりを表面の法線方向にしか動かさず（`Scripts/task023/free.py`）、
  024 は構成で置いた。037 は x, y を全部自由にした（本文 1.「自由度」）。
- **本文の解法の設定に「留まりの上端への外向きの引き」（3 段）がある**（037-2 で振るもの。まだ実装していない）。
  **主の設定は引き無しで密に詰めた極限を取るはずだったが、詰まらずに膨らんだ。**
- **糸の古い端（最初の球）は隣が 1 つで、何にも引かれない。** 2 サイクル（8 d）では、どの断面も端から 4 d 以内に
  ある。**本体と端を分けるのは 037-2 の 4 サイクル**である。
- **1000 歩で静止していない。** 150 → 1000 歩で、断面は膨らむ向きに動き続けていた。

**`stand.bundle_angle` の doc comment は直していない**（判定 2 のとおり。022 の記録）。
**判定 1 の注は 023 の「高さの読み方」に付けた**（本文は変えていない）。

**product code は変更していない。よってテストは走らせていない。**
