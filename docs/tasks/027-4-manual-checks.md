# Task 027-4: 手で確かめる項目

- 状態: **一覧**（2026-09-09 作成）
- なぜ手なのか: **Task 015**（UI テストで RealityKit が落ちる）が自動検証を塞いでいる。
  **通しで走らせるたびにどれか1件が落ちる**（毎回違うテストで、単独実行では通る）ので、
  **UI テストは既定の検証に含めない**（`AGENTS.md`）。**自動化の再挑戦はしない**（作者の指示）。
- **この一覧は「自動で確かめられないもの」だけを並べる。** 値や導出の一致は
  ユニットが見ている（`tasks/027-screens-on-recipes.md` の各段の差分テスト）。

**やり方**: 実機かシミュレータでアプリを起動し、上から順に。**期待と違ったら、そこで止めて
記録すること**（直しながら進めない）。

## 1. 組み方の選択と立体（027-1）

| # | 画面 | 操作 | 期待する表示 |
|---|---|---|---|
| 1.1 | 作品編集 | 糸数を16にする | **丸源氏と平源氏が選べる**（他の糸数では出ない） |
| 1.2 | 作品編集 | 丸源氏のサムネイルを押す | **筒の立体**が出る。回せる |
| 1.3 | 作品編集 | 平源氏のサムネイルを押す | **平らな紐の立体**が出る |
| 1.4 | 作品編集 | 配色を変える | **立体の色が変わる**（閉じ直さずに） |
| 1.5 | 作品編集 | 各組み方の注記を読む | **その組み方の試作の注記**が出ている（丸源氏と平源氏で違う文） |
| 1.6 | 作品編集 | サムネイルの読み上げ | 「**〈組み方の名前〉を3Dで見る**」 |

**「描き手なし」の表示は、いまの2つの組み方では出ない**（どちらにも描き手がある）。
**確かめられるのは、描き手の無い組み方を足したときである。** それまでは
**ユニットが押さえている**（`BraidScreenChoosesByFamilyTests`）。

## 2. 保存と読み込み（027-2）

| # | 画面 | 操作 | 期待する表示 |
|---|---|---|---|
| 2.1 | 作品編集 | 組み方と配色を決めて保存 → 一覧へ戻る | 一覧に**その組み方の名前**が出る |
| 2.2 | 一覧 | いま保存した作品を開く | **同じ組み方が選ばれている。配色も同じ** |
| 2.3 | 一覧 | **古い版で保存した作品**を開く（あれば） | **同じ組み方と配色で開く**（鍵の読み替え） |
| 2.4 | 作品編集 | 組み方を変えて上書き保存 → 開き直す | **新しい組み方で開く** |

**2.3 が確かめられるのは、古い版の保存データが手元にある場合だけである。**
**無ければ「確かめていない」と書くこと。** 読み替えの中身は
ユニットが押さえている（`BraidSavedBraidTests`）。

## 3. 模様図と立体の切り替え（027-3）

| # | 画面 | 操作 | 期待する表示 |
|---|---|---|---|
| 3.1 | プレビュー | 「模様図」を選ぶ | **2Dの図**が出る。**画面遷移は起きない**（同じ場所が入れ替わる） |
| 3.2 | プレビュー | 平源氏で模様図 | **表と裏の2枚**が並ぶ |
| 3.3 | プレビュー | 丸源氏で模様図 | **8列の図**が1枚。**下に列の角度**（0.0° 67.5° 90.0° …。**等間隔ではない**） |
| 3.4 | プレビュー | 丸源氏の図の下を読む | **未決の注記**が小さく出ている（鏡像は一致扱い／Task 004 の列順は未決） |
| 3.5 | プレビュー | 「立体」に戻す | **立体が出る。図の状態は残っていなくてよい** |
| 3.6 | プレビュー | 模様図の下の注記 | **「寸法の推定を含みません」** |

## 3c. 一覧の平源氏のサムネイル（029）

| # | 画面 | 操作 | 期待する表示 |
|---|---|---|---|
| 3c.1 | 一覧 | 平源氏のカードを見る | **一周ぶんが出ている**——上下の端に細い帯（右縁の半分ずつ）、その内側に表 6・左縁 2・裏 6 |
| 3c.2 | 丸台 | **裏側に来る位置**（9〜14 のあたり）の色を変える | **カードが追従する。** 029 の前は表しか出ていないので変わらなかった |
| 3c.3 | 一覧 | 矢羽の傾き | **歪んでいない**（縦と横で同じ縮尺。縁だけ斜線が急、ということが無い） |
| 3c.4 | 一覧 | 縁の帯 | **表・裏のレーンと同じ太さ**（どのレーンも糸 1 本ぶん） |
| 3c.5 | 一覧 | 丸源氏と並べて見る | **同じくらいの細かさ**に見える（両方とも一周を高さに載せている） |
| 3c.6 | 一覧 | 上から順に帯を数える | **右縁 2・表 6・左縁 2・裏 6 の 4 本の帯**として読める。**上端と下端に細い帯が分かれて出ていない**（029-2） |
| 3c.7 | 一覧 | 縁の帯の継ぎ目を端から端まで目で追う | **直線の継ぎ目が 1 本も混ざらない**（030 の前は 4 段に 1 本だけ直線だった） |
| 3c.8 | プレビュー（立体） | 紐を回して縁を見る | **縁の継ぎ目が段ごとに同じに見える**（4 段に 1 本だけ違う、が無い） |

## 4. 文字の大きさと配色（`AGENTS.md` の検証ルール）

| # | 操作 | 期待 |
|---|---|---|
| 4.1 | 標準の文字サイズ | 1〜3 の表示が崩れない |
| 4.2 | アクセシビリティの大きい文字サイズ | **文言が切れない**（切れるなら、そこを記録） |
| 4.3 | ライトモード | 図と立体の色が判別できる |
| 4.4 | ダークモード | 同じ |
| 4.5 | iPad | プレビューが並ぶ配置で 1〜3 が通る |
| 4.6 | 両方 | **平源氏のサムネイルの 16 レーンが潰れずに出る**（3c を、ライト／ダークと大きい文字サイズで） |

## 記録の書き方

**通った項目は「通った」だけでよい。** 期待と違ったものは
**画面・操作・見えたもの**の3つを書く。**直し方は書かない**（それは次の作業である）。
**確かめられなかった項目は「確かめていない」と、その理由を書く。**
