# Task 054: 丸四つ組（4本）を足す

- 状態: **実装済み・作者の確認待ち**（2026-09-22）。結果は末尾の「結果」。指示書として出したのは同日（042 以降の型）
- 前提（先に読むこと）: `docs/tasks/025-5-adding-a-recipe.md`（レシピを1つ足す手順）、
  `docs/tasks/049-yatsu-kongo-appearance-and-source-closure.md` の 2.3 節（帯の向きの食い違い）、
  `docs/tasks/053-yatsu-kongo-gaeshi.md`、`docs/architecture.md`「正本の読み方」
- 資料: **bookA p.10（完成写真 a・b・c）と p.56（手順の図と「糸の配色と配置」）**。
  **`.build/task054/source/` に置いてある**（git の外。倍率は bookA p.8–9 の拡大と同じ
  3584×2058 の窓から切り出したもの）。**同フォルダの `README.md` を先に読むこと**——
  審査側が p.56 から読んだもの（材料・2手・配色 a/b/c）と、**読めなかったもの（対の中の先後）**を書いてある。
  写すのは `bookA-p56-maru-yotsu-instructions.png`、測るのは `bookA-p10-maru-yotsu-photograph.png`
- **製品コードに触れる。** 研究タスクではないので、PR の前にユニットテストを一度通すこと

## 0. なぜ今これか

作者の決定（2026-09-22）。物理の線（036〜044）は止めたまま、**別の組み方を足す段階**に入る。
本数セレクタ（`ProjectDraft.supportedThreadCounts = [4, 8, 12, 16]`）は 4 と 12 を出すのに
組み方が無く、「この本数の組み方はまだありません」が出る。**丸四つ組で 4 を埋める。**

**八つ金剛の S と Z の入れ替えは、この指示書から外した**（段階0 を見ること）。
**八つ金剛には一切触れない。**

---

## 段階0: 取り下げ（八つ金剛には触れない）

**この段は作者が同日に差し戻した。**

作者は一度「S と Z を入れ替えて良い。どちらにしても大差ない」と言ったが、
**入れ替えの中身が「アプリの八つ金剛S が、組ひもディスクの本 p.36（8Z-スパイラル）の表を使う」
ことだと分かった時点で取り下げた**——「これはまずいな。S と Z は戻す」（2026-09-22）。
**本が 8Z と刷っている表を S と呼ぶわけにはいかない。**

したがって:

- `yatsuKongoS8` は **p.37（8S-スパイラル）** の表のまま。`yatsuKongoZ8` は **p.36（8Z）** のまま。
- **返し組も鏡にしない。**
- **この指示書で八つ金剛のコード・表・配色・文言・試験に触れてはいけない。**
  形状ハッシュも八つ金剛を含めて**全族が不変**であること（段階4 の確認項目）。

**049 2.3 の食い違い（描いた S の色の帯が −55°、bookA p.8 の写真の S は +55°）は未決のまま残る。**
選択肢2（名前の入れ替え）はこれで閉じ、**選択肢1（作者の図を優先し、帯の向きが写真と鏡であることを
受け入れる）が現状である。** 次に手を入れるなら、名前ではなく**なぜ描き手が鏡に出るのか**から。

## 段階1: 4本の台と、丸四つ組の手順表

### 台

`BraidStands.round4` を足す。`round(id:positionCount:groups:)` をそのまま使い、
**群は16本・8本と同じ数え方**（外側から、北・東・南・西）で1位置ずつ:

    north: [1], east: [2], south: [3], west: [4]

`BraidMethodCatalog.stands` に加える。`stand(for:)` は休む位置の数で選ぶので、**分岐は増えない。**

### 休むノッチ

32ノッチのディスクに4本は8ノッチおき。**bookA は丸台の本でディスクの図を持たないので、この番号づけは
この repo のもの**（`diskRestingNotchesForEight` と同じ事情。doc comment にそう書くこと）:

    diskRestingNotchesForFour = [1: 1, 9: 2, 17: 3, 25: 4]

位置の番号とノッチが同じ向きに回るように選んである。

### 手順表（bookA p.56）

本が刷るのは2手と「1〜2をくり返す」である:

| 刷られた手 | 本の言葉 | 動く糸 |
| --- | --- | --- |
| 1 | 右手R が上の糸、左手L が下の糸 | **縦の対を入れ替える**（北↔南） |
| 2 | 右R が左の糸を右へ、左L が右の糸を左へ | **横の対を入れ替える**（西↔東） |

**ディスクの記法へ写したもの**（審査側が先に回して1サイクルになることを確かめた。
`BraidDiskNotation.method(...)` と同じ手順を紙の上で回している）:

    maruYotsuDisk の moves:
      (1, 18), (17, 2), (18, 17), (2, 1),      // 刷られた手1: 縦
      (9, 26), (25, 10), (26, 25), (10, 9),    // 刷られた手2: 横

長い手（15ノッチ）が組みの手、1ノッチの手が着地の詰めで、`isRepositioning` が距離だけで
見分ける。**検算の結果**: 運ばれた糸は 1・3・2・4 の順、終わりの休み場所は
**1→3、3→1、2→4、4→2**、`closing` は空。**`threadsPerStep` は 2、`stepNames` は 2 つ。**

- **`stepReading` は `.oneStepAnInstant`。** bookA は1手に右手と左手の2本を刷り、**どちらが先かを
  書いていない**（`docs/architecture.md` の bookA p94–95 と同じ事情）。bookC に丸四つ組の図は無い。
  八つ金剛が `diskOfEight` で採ったのと同じ読みにする。
- **ただし八つ金剛と違い、この紐では対の2本が互いの場所へ行く。** 八つ金剛では Task 031 のテスト6が
  「先後を入れ替えても立体が1頂点も動かない」ことを示したが、**それはその紐に交差が無かったからである。**
  **丸四つ組では対が交差するので、先後が形に届く可能性が高い。**
  → **段階1 の報告に、先後を入れ替えた表との差（占有履歴・断面・メッシュ）を数で付けること。**
  届くなら `unsettled` に「対の中の先後は bookA から読めない」と書いて残す（八つ金剛と同じ扱い）。

### 検算（025-5 の1）

- `BraidRecipe.worked(on: round4)` が `nil` を返さないこと。返したら**表がこの台の1サイクルに
  なっていない。**
- **1サイクルで全糸が2場所進むこと**（位置1→3、2→4、3→1、4→2）。
  **これは八つ金剛と同じ構造である**（8本で2場所）——半分の本数で同じ2場所なので、
  **丸四つ組のほうが速く回る。** 螺旋はここから出る。
- **紐のまわりの並び順は台の縁の順そのまま**（管なので何も宣言しない。025-5 の4）。

---

## 段階2: レシピとプリセット

### 配色（025-5 の2）

bookA p.56 の「糸の配色と配置」は3つ刷っている: **a（147 縦 / 169 横）、b（163 縦 / 148 横）、
c（124 だけ）**。**採るのは b**——p.10 の写真 b が縦と横で一番はっきり分かれていて、**判定できる**。
c は1色なので模様の判定に使えない。

カタログの色名への対応は**読みであって測定ではない**。doc comment に「bookA p.56 の b、
縦 163・横 148 を、カタログの近い色名へ置いた」と書くこと。糸番号そのものは残す。

    colouring(on: stand4, [
      "north": ["<縦の色>"], "south": ["<縦の色>"],
      "east":  ["<横の色>"], "west":  ["<横の色>"],
    ])

### 測った形の値（025-5 の3）

**何も測っていないなら空で出すこと**（`BraidShapeValues()`）。八つ金剛がそうしている。
**目で合わせた数を `.observed` にしない。** 段階3 で描き手が持つ数は `.declared` で、
「写真に対して目で較正した、導出ではない」と必ず書く。

### レシピとプリセット

- `maruYotsu4Recipe`（id `maru-yotsu-4`、name 「丸四つ組」）を `recipes` に足す。
- `BraidPresetID.maruYotsu4` と `BraidPresetCatalog.maruYotsu`
  （displayName 「丸四つ」、`supportedThreadCounts: [4]`、`crossSectionProfile: .round`、
  `verificationLevel: .movementRules`）を足し、`presets` に入れる。
- `prototypeNotice`: **手順表は bookA p.56 から写した試作であること、bookC の図が無く対の中の
  先後が読めないこと、立体は描画上の近似であること**を書く。

**これで本数セレクタの 4 が埋まる。** 12 は空のまま（Task 012〜014）。

---

## 段階3: 4本の描き手

**作者の判定（2026-09-22）: 4本専用の描き手を新しく作る。** 既存の筒8本の描き手を広げない。

- 族は `.roundTube(threads: 4)`。`BraidFamilyDrawing.Drawing` に case を足し、`families` に
  `RoundTube4SurfaceMesh.shape` を足す。**族の読み方そのもの（`BraidFamily.family(of:)`）は
  触らない。**
- **`RoundTube8SurfaceMesh` と `RoundTube8SurfacePattern` を先に読むこと。** 構造は同じ
  （1サイクルに2場所進む筒、対が半ピッチで交替する）ので、**作りは真似てよいが共通化はしない**
  ——族ごとに描き手を持つのが今の作りで、形の値は族のものである。
- 組み目は **Task 051・053 で受領したもの**（頭は鈍く上に載り、尾は幅を保って隣の列へ曲がり、
  後に置かれた束の下へ潜る。束の傾きは運ぶ向きの符号から取らない）を 4本へ持ち込む。
  **051・053 で直した誤りを作り直さないこと。**
- 一覧カードの展開図・詳細の立体・模様図が 4本で出ること。**模様図（2D）は族に関わらず出るので、
  段階2 の時点で出ているはず**——出ていなければ段階2 に穴がある。

### 予言

- 4本は8本の倍の速さで回るので、**展開図の斜めは8本より急**になる。
- 束は**縦の対と横の対が半サイクルずれて交替する**（八つ金剛の半ピッチと同じ構造）。
- 写真（bookA p.10 の b）との差は残る。**寄せ切らなくてよい。差を測って報告すること**
  （測り方は `docs/measurement-procedures.md`。新しい測り方を作る前に読む）。

---

## 段階4: 検証

- **025-5 の6 の3つを自分の紐で確かめる**: `BraidRecipeIsThreeThingsTests` と
  `BraidFamilyDrawingTests` が、導出・図・積みに手を入れずに通ること、族が紐から読めて
  描き手がメッシュを返すこと。
- `sh Scripts/check-braiding-is-general.sh` を通すこと（組み方の名前が
  `BraidMethodCatalog.swift` の外に出ていない見張り）。
- **既存の族の形状ハッシュは1つも動かないこと**（丸源氏・平源氏・八つ金剛）。動いたら段階3 が族の外へ漏れているか、触ってはいけない所に触れている。
- 全件を1回。**テスト段階はいま約280秒で上限300秒に近い**（053）。
  **足す試験は最小限にし、テスト段階の秒数を必ず報告すること。** 上限に当たったら、
  そこで止めて報告する（自分で上限を上げない。Task 018 の例外は閉じてある）。
- 手動は iPad 1 機で、**新しく編む → 4本 → 丸四つ → カード → 立体 → 模様図**まで。
  iPhone・ダーク・大きな文字・実機・VoiceOver・UIテスト一式は対象外。
  **RealityKit の解放で落ちる事象（Task 015）が出たら、`.ips` の名前だけ記録して先へ進む。**

## 報告に必ず入れるもの

1. 段階1 の**先後を入れ替えた表との差**（届くか届かないか、数で）。
2. 段階1 の検算（`worked(on:)` が通ること、1サイクルで2場所進むこと）。
3. 段階3 の写真との差（測り方の番号つき）と、**`.declared` にした数の一覧**。
4. **全件の件数・失敗・スキップ・打ち切り・テスト段階の秒数。**
5. 読み替えた所（本の糸番号 → カタログの色名など）と、**読めなかったもの。**

## やらないこと

- **すず菜組（bookA p.11 / p.57）は入れない。** 細く組んだ紐を1本おもり玉につけて組む紐で、
  「糸が紐である」という模型の拡張が要る。**別のタスクにする。**
- 12玉（Task 012〜014）は今回の外。
- **八つ金剛には触れない**（段階0 は取り下げ）。
- 物理の線（036〜044）は止めたまま。
- 049 の形状の申し送り（腹の高さ、見かけの傾き17°、網目の質感）は今回の外。

---

## 結果（2026-09-22、worker）

### 段階1: 台と手順表

- `BraidStands.round4`、`BraidMethodCatalog.stand4`・`diskRestingNotchesForFour`・`maruYotsuDisk`・`maruYotsu4`（stepNames `uprightPair`・`flatPair`）。
- **検算**: `worked(on: round4)` は通る。手は `[[1→3, 3→1], [2→4, 4→2]]`、`closing` は空、組みの手は全て15ノッチ。
  **1サイクルで全糸が2場所進み、反復は2サイクル**（`MaruYotsuTests.everyThreadGoesTwoPlacesACycle`）。
  積みは1サイクル2層（`BraidStacking`）、族は `.roundTube(threads: 4)`、`faceColumns` は nil（閉じが空なので）。
- **対の中の先後: 届かない（数で）。** 4通り（刷られた順／対の中を入れ替え × `.oneStepAnInstant`／`.oneThreadAnInstant`）で
  - 占有履歴（全糸の場所の並び）: **4通りとも同一**
  - 断面: 4通りとも台の縁の順 `[1, 2, 3, 4]`
  - 模様図の形: 4通りとも同一
  - メッシュ: **4通りとも 4,560 頂点、ハッシュ同一、頂点の最大移動 0.0**
  - 違うのは `.oneThreadAnInstant` のときの置いた手番（どちらが上か）だけで、描くものは誰もそれを読まない。
  - `passingsWithinOneInstant` は 0 件。**注意**: `runsMustPassEachOther` は端を共有する弦（互いの場所へ行く2本）を
    「すれ違う」と数えない。本の図では2本は左右に振り分けて通る（実物では交差する）が、導出はそれを交差として持っていない。
  - 届かないので `unsettled` は付けていない。プリセットの注記には「対の2本のどちらが先かは読めません」と書いた。
  - 固定した試験: `whichThreadOfAPairGoesFirstDoesNotReachTheDrawing`。

### 段階2: レシピとプリセット

- `maruYotsu4Recipe`（`maru-yotsu-4`、「丸四つ組」）、`BraidPresetCatalog.maruYotsu`（「丸四つ」、`[4]`、`.round`、`.movementRules`）。
- **配色の読み替え**: bookA p.56 の b、**縦 163 → `white`、横 148 → `purple`**。頁の 163 は淡色、148 は灰みの藤、p.10 の写真 b は白と藤。
  カタログに藤が無いので最も近い `purple`（写真より彩度が高い）。糸番号は doc comment に残した。**読みであって測定ではない。**
- 測った形の値は空（`BraidShapeValues()`）。
- **4本で「この本数の組み方はまだありません」が出なくなった。** 12本は空のまま。
  これに合わせて既存の試験2か所を直した: `MaruGenjiSimulationTests.presetIsAvailableOnlyForSixteenThreads`（4本の一覧が空という期待）と、
  UIテスト `testNoCompatiblePresetStillShowsUndecidedSelection`（新規エディタの既定4本で空状態を見ていたので、12本を選んでから見る）。
  **UIテストは回していない。**

### 段階3: 4本の描き手

- 新規: `Kumihimo/Domain/RoundTube4SurfacePattern.swift`（`RoundTube4SurfacePattern`・`RoundTube4Bundle`・生成器）、
  `Features/BraidSimulation/RoundTube4SurfaceMesh.swift`・`RoundTube4StrandTexture.swift`・`RoundTube4ThumbnailView.swift`。
  8本の描き手を写して族の数を持たせた。**8本側は1行も変えていない**（共通化していない）。
- つないだ所: `BraidFamilyDrawing`（`Drawing.roundTubeOfFour`、`families`、`mesh` の戻り値に `tubeOfFour`）、`BraidSurfaceScene`、
  `BraidPreviewForFamily`（立体は筒の同じビュー、カードは `RoundTube4ThumbnailView`）。`BraidFamily.family(of:)` は触っていない。
- 組み目は 051・053 のもの（鈍い頭が上、尾は幅を保って隣の列へ曲がり、後の束の下へ潜る。傾きは運ぶ向きの符号から取らない）。
- **予言との照合**
  - 「展開図の斜めは8本より急」: **配色 b では外れた——斜めが無い。** c = 2、s = 2 なので場所はずっと同じ色で、色は紐に沿って
    まっすぐな4本の帯（白・藤・白・藤）になる。写真 b も藤の列が紐に沿ってまっすぐ通っており、**こちらは写真と合う。**
    4本の糸を別の色にすれば、1サイクルで半周回る螺旋が出るはず（見ていない）。
  - 「縦の対と横の対が半サイクルずれて交替」: **当たった**（`drawnPhaseByColumn == [0.5, 1, 0.5, 1]`）。
- **写真との差**（`.build/task054/sheets/v1.png`・`v2.png`、写真 b と iPad の立体を同じ紐幅に揃えて上下に並べた）
  - **1サイクル÷幅（測り方6、ただし色でなく組み目の周期。`docs/measurement-procedures.md` 6 の追記）**:
    写真 b **0.80**、描いたもの **0.97**（周期 183 px、輪郭 189 px。下の直しの後は 0.99）。描いた輪郭は山の直径の約 0.83 しかないため。**寄せていない。**
  - 束の傾き: 写真 b の藤の束は紐に対し約 23°、右上がりで、尾は上の白の列の下へ入る。描いたものは右上がりにした（下の `stitchLean`）が、
    写真ほど尖った楕円にはならず、頭が鈍い（051 の決まり）。
  - 太さ: 写真の藤の束は紐幅の約6割。描いたものはやや細い。
- **`.declared` にした数**（`RoundTube4SurfaceMesh.shape`。下の「作者の指摘」で直した後の値）:
  束の傾き 0.45 列/サイクル、尾の曲がり 0.5 列、束の半幅 0.64 列、頭の丸め 0.35 サイクル、谷からの高さ 0.6（半径比）、
  潜る長さ 0.55・弧の長さ 1.2・曲がり始め 0.5・細り始め 1（以上4つは8本の値のまま、サイクル単位なので）、繊維の縞の角 8°・本数 24・起伏 0.003、谷の陰 2つ（16本の値を借用）、画面上の半径 0.48。
  `.observed` は 1サイクル÷直径 0.80（幅 0.77〜0.80、上の差と「測り方6の解像度に届かない」を `unsettled` に書いた）、`.derived` は半糸の高さと山の位置。
- **`stitchLean` は +1（8本は −1）。** 画面で組み点を右にして束が右上がり、尾が上の列の下へ入るのが写真 b と合う向き。
  **8本と逆向きになった理由は分かっていない**——p.10 の紐のどちらの端が組み点かは分からず、049 2.3 の鏡の問題がここへ届くかも見ていない。

### 段階4: 検証

- **全件1回**（`.build/test-results/task054-full.xcresult`、iPhone 16、`-only-testing:KumihimoTests`）:
  **479件中 成功474・失敗1・スキップ4**（`DRAW_SHEETS` のシート、機体に変数なし）、**テスト段階 272.6 秒**（呼び出し全体 301 秒）。**打ち切りは無い。**
  失敗1件は上の `presetIsAvailableOnlyForSixteenThreads`（4本の一覧が空という古い期待）。直して単独で通過（`task054-fix.xcresult`）。**直したあとの全件は回していない。**
- 丸源氏・平源氏・八つ金剛（S・Z・返し）の形状ハッシュの試験はすべて通過（不変）。
- `BraidRecipeIsThreeThingsTests`・`BraidFamilyDrawingTests` は導出・図・積みに手を入れずに通過（後者は `mesh` の戻り値に1つ足した分だけ直した）。
- `sh Scripts/check-braiding-is-general.sh` 通過。
- 足した試験は `MaruYotsuTests` の9件（合計 0.2 秒）。
- **手動**（iPad Pro 11 M4、ライト・標準文字）: 新しく編む → 4本 → 丸四つのカード → 色を b に → カード → 立体 → 模様図。
  すべて出た（`.build/task054/manual/`）。保存はしていない。**落ちなかった**（`.ips` なし）。
  途中、機体に14時台から残っていた古いアプリのプロセスが新しいビルドの起動を妨げていたので、一度終了して起動し直した。
- 対象外: iPhone の手動、ダーク、大きな文字、実機、VoiceOver、UIテスト一式。

### 読めなかったもの・判断が要るもの

- **対の中の先後**（bookA は右手・左手だけ、bookC に図が無い）。いまの描き方には届かない。
- **`stitchLean` の向きが8本と逆**。写真に合わせたが、なぜ逆になるかは説明できていない。
- **1サイクル÷幅の差**（写真 0.80 に対して描いたもの 0.97）。直すなら「紐の直径」を輪郭で数えるかどうかの判断が要り、八つ金剛にも関わる。
- 配色 b の 148 に合う色がカタログに無い（`purple` は写真より鮮やか）。

### 作者の指摘と直し（2026-09-22）

作者（生成りの立体を見て）:「ニョッキみたいだなあ。もう少し半円というか、真ん中が盛り上がってるようになるかな？
それと形状をもう少し寄せて欲しい。完成画像の中段くらいの形状が良い」（中段 = p.10 の写真 b）。

**原因**: 束の高さ（谷から半径の 0.44）と頭の丸め（0.12 サイクル）を8本のまま持ち込んでいた。4本では束の幅が周の4分の1あり、
同じ高さでは上が平らな枕になり、短い頭の丸めで端が四角く切れた。

**直した**（すべて `.declared`、写真 b と同じ紐幅で並べて目で合わせた。`.build/task054/sheets/v3.png`・`v4.png`）:

| 数 | 前 | 後 |
| --- | --- | --- |
| 谷からの高さ（半径比） | 0.44 | **0.6**（真ん中が盛り上がり、横断面が半円に近い） |
| 頭の丸め（サイクル） | 0.12 | **0.35**（頭へ向かって細る卵形） |
| 束の傾き（列/サイクル） | 0.3 | **0.45**（写真の約23°へ近づけた） |
| 束の半幅（列） | 0.62 | **0.64**（列のあいだに谷底を見せない） |

メッシュのハッシュは `0x506f_2561_7c50_30c5` → `0x4ae7_8577_341e_6bf1`（頂点数 4,560 は不変）。
**まだ残る差**: 写真の束は右端がもう少し尖り、傾きももう少し強い。1サイクル÷幅は同じ測り方で 0.99（写真 0.80）。
直しの後は `MaruYotsuTests` だけを回して通過（9件）。**全件は直しの前の1回だけ**。
