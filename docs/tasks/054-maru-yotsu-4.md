# Task 054: 丸四つ組（4本）を足し、八つ金剛の S と Z を入れ替える

- 状態: **指示書**（2026-09-22）。**worker へそのまま渡す。承認待ちの段は挟まない**（042 以降の型）
- 前提（先に読むこと）: `docs/tasks/025-5-adding-a-recipe.md`（レシピを1つ足す手順）、
  `docs/tasks/049-yatsu-kongo-appearance-and-source-closure.md` の 2.3 節（帯の向きの食い違い）、
  `docs/tasks/053-yatsu-kongo-gaeshi.md`、`docs/architecture.md`「正本の読み方」
- 資料: **bookA p.10（完成写真 a・b・c）と p.56（手順の図と「糸の配色と配置」）**。
  作者が撮った頁は `.build/task054/source/` に置く（**git に入れない**）
- **製品コードに触れる。** 研究タスクではないので、PR の前にユニットテストを一度通すこと

## 0. なぜ今これか

作者の決定（2026-09-22）。物理の線（036〜044）は止めたまま、**別の組み方を足す段階**に入る。
本数セレクタ（`ProjectDraft.supportedThreadCounts = [4, 8, 12, 16]`）は 4 と 12 を出すのに
組み方が無く、「この本数の組み方はまだありません」が出る。**丸四つ組で 4 を埋める。**

同じ日に作者は **「S と Z を入れ替えて良い。どちらにしても大差ない」** と判定した
（049 2.3 の選択肢2）。**段階0 として先に片づける。**

---

## 段階0: 八つ金剛の S と Z を入れ替える

### 何が起きているか

049 2.3 が、写真と描いた紐の色の帯の向きを同じ測り方（Task 031 の `colour_angle`、紐を立てて
中央帯）で測った:

| | ぼかし31 | ぼかし61 |
| --- | --- | --- |
| 写真 S（bookA p.8） | **+55.0°** | +54.5° |
| 写真 Z-a（bookA p.9） | −53.0° | −51.0° |
| 描いた S | **−55.0°** | −50.0° |
| 描いた Z | **+55.0°** | +50.0° |

**大きさは合っていて、向きだけが鏡である。** 描く側を逆に回す道は 049 が試して閉じている
（作者の要求「列に沿って1束ごとに色が替わる」と半ピッチの対の条件が同時に満たせず、
一覧・詳細が「完成イメージを生成できません」になる）。**残っていたのは名前の側だけで、
作者がそれを採った。**

### やること

**Z の表を S の名前へ、S の表を Z の名前へ移す。** 表は Task 053 のまま1文字も書き換えない
——**移すのは、どの表をどのプリセットが使うかだけ**である。

1. `BraidMethodCatalog.yatsuKongoS8` が `yatsuKongoZDisk`（組ひもディスクの本 p.36、8Z-スパイラル）を、
   `yatsuKongoZ8` が `yatsuKongoSDisk`（同 p.37、8S-スパイラル）を使うようにする。
   **`yatsuKongoSDisk` / `yatsuKongoZDisk` の名前は本の頁の名前なので変えない。**
   取り違えたのかと読まれないよう、**入れ替えの理由（写真の帯の向き）を doc comment に書く。**
2. **返し組も同じ鏡にする。** `yatsuKongoGaeshiRounds` を `BraidDiskNotation.reflected(about:source:)`
   で鏡に写し、**アプリの返し組が、アプリの新しい S と同じ向きから始まる**ようにする。
   軸は S と Z の表を互いに写す軸と同じものを使い、`reflected` が `nil` を返さないことを試験にする
   （返したら軸が違う。休むノッチが自分自身へ戻る軸を選ぶこと）。
   **これを入れないと、返し組の前半だけが他の2つと逆に倒れる。**
3. **文言**: `BraidPresetCatalog` の `prototypeNotice` を直す。「S は本の 8Z-スパイラルの表である。
   本の名前とは逆だが、**完成写真（bookA p.8 の S）の帯の倒れる向きに名前を合わせた**」が書いてあること。
4. **配色は動かさない。** S と Z は同じ配色（bookA p.54 と p.55 の a）で、入れ替えても同じ。

### 試験

- `YatsuKongoAgainstBookCTests`: **bookC Fig.129（本の Z）はアプリの新しい S と一致する。**
  いまの「Fig.129 はアプリの Z」を書き換え、**理由をテスト名か注に残す**（消さない）。
- `YatsuKongoTests` の鏡の試験（S と Z が互いの鏡であること）は**そのまま通るはず**。通らなければ
  入れ替え以外のものを動かしている。
- `sZAndSAndZShareOneStitch`・`aRunLeansTheStitchsWayWhateverTheCarry`（053）は**通ったままのはず**。
  組み目は S・Z・返しで同じで、入れ替えは組み目を動かさない。
- **新しい試験を1つ**: 返し組の前半が、アプリの S と同じ段の集合で始まること。

### 予言（外れたらそれが答え。改善しなくてもそのまま報告する）

- `colour_angle` をもう一度測ると、**描いた S は +50〜55°、描いた Z は −50〜55°** になり、
  写真と同じ符号になる。
- 立体と一覧カードは**鏡に写るだけで、組み目・半ピッチ・升の長さは1つも変わらない。**
  変わったら入れ替え以外のものが動いている。
- 形状ハッシュ（`BraidMeshHashTests`）は**丸源氏・平源氏は不変**、八つ金剛は入れ替わる。
  入れ替わったハッシュは理由つきで付け替える。

---

## 段階1: 4本の台と、丸四つ組の手順表

### 台

`BraidStands.round4` を足す。`round(id:positionCount:groups:)` をそのまま使い、
**群は16本・8本と同じ数え方**（外側から、北・東・南・西）で1位置ずつ:

    north: [1], east: [2], south: [3], west: [4]

`BraidMethodCatalog.stands` に加える。`stand(for:)` は休む位置の数で選ぶので、**分岐は増えない。**

### 休むノッチ

32ノッチのディスクに4本は8ノッチおき。**bookA は丸台の本でディスクの図を持たないので、この番号づけは
この repo のもの**（`diskRestingNotchesForEight` と同じ事情。doc comment にそう書くこと）:

    diskRestingNotchesForFour = [1: 1, 9: 2, 17: 3, 25: 4]

位置の番号とノッチが同じ向きに回るように選んである。

### 手順表（bookA p.56）

本が刷るのは2手と「1〜2をくり返す」である:

| 刷られた手 | 本の言葉 | 動く糸 |
| --- | --- | --- |
| 1 | 右手R が上の糸、左手L が下の糸 | **縦の対を入れ替える**（北↔南） |
| 2 | 右R が左の糸を右へ、左L が右の糸を左へ | **横の対を入れ替える**（西↔東） |

**ディスクの記法へ写したもの**（審査側が先に回して1サイクルになることを確かめた。
`BraidDiskNotation.method(...)` と同じ手順を紙の上で回している）:

    yatsuMaruYotsuDisk の moves:
      (1, 18), (17, 2), (18, 17), (2, 1),      // 刷られた手1: 縦
      (9, 26), (25, 10), (26, 25), (10, 9),    // 刷られた手2: 横

長い手（15ノッチ）が組みの手、1ノッチの手が着地の詰めで、`isRepositioning` が距離だけで
見分ける。**検算の結果**: 運ばれた糸は 1・3・2・4 の順、終わりの休み場所は
**1→3、3→1、2→4、4→2**、`closing` は空。**`threadsPerStep` は 2、`stepNames` は 2 つ。**

- **`stepReading` は `.oneStepAnInstant`。** bookA は1手に右手と左手の2本を刷り、**どちらが先かを
  書いていない**（`docs/architecture.md` の bookA p94–95 と同じ事情）。bookC に丸四つ組の図は無い。
  八つ金剛が `diskOfEight` で採ったのと同じ読みにする。
- **ただし八つ金剛と違い、この紐では対の2本が互いの場所へ行く。** 八つ金剛では Task 031 のテスト6が
  「先後を入れ替えても立体が1頂点も動かない」ことを示したが、**それはその紐に交差が無かったからである。**
  **丸四つ組では対が交差するので、先後が形に届く可能性が高い。**
  → **段階1 の報告に、先後を入れ替えた表との差（占有履歴・断面・メッシュ）を数で付けること。**
  届くなら `unsettled` に「対の中の先後は bookA から読めない」と書いて残す（八つ金剛と同じ扱い）。

### 検算（025-5 の1）

- `BraidRecipe.worked(on: round4)` が `nil` を返さないこと。返したら**表がこの台の1サイクルに
  なっていない。**
- **1サイクルで全糸が2場所進むこと**（位置1→3、2→4、3→1、4→2）。
  **これは八つ金剛と同じ構造である**（8本で2場所）——半分の本数で同じ2場所なので、
  **丸四つ組のほうが速く回る。** 螺旋はここから出る。
- **紐のまわりの並び順は台の縁の順そのまま**（管なので何も宣言しない。025-5 の4）。

---

## 段階2: レシピとプリセット

### 配色（025-5 の2）

bookA p.56 の「糸の配色と配置」は3つ刷っている: **a（147 縦 / 169 横）、b（163 縦 / 148 横）、
c（124 だけ）**。**採るのは b**——p.10 の写真 b が縦と横で一番はっきり分かれていて、**判定できる**。
c は1色なので模様の判定に使えない。

カタログの色名への対応は**読みであって測定ではない**。doc comment に「bookA p.56 の b、
縦 163・横 148 を、カタログの近い色名へ置いた」と書くこと。糸番号そのものは残す。

    colouring(on: stand4, [
      "north": ["<縦の色>"], "south": ["<縦の色>"],
      "east":  ["<横の色>"], "west":  ["<横の色>"],
    ])

### 測った形の値（025-5 の3）

**何も測っていないなら空で出すこと**（`BraidShapeValues()`）。八つ金剛がそうしている。
**目で合わせた数を `.observed` にしない。** 段階3 で描き手が持つ数は `.declared` で、
「写真に対して目で較正した、導出ではない」と必ず書く。

### レシピとプリセット

- `maruYotsu4Recipe`（id `maru-yotsu-4`、name 「丸四つ組」）を `recipes` に足す。
- `BraidPresetID.maruYotsu4` と `BraidPresetCatalog.maruYotsu`
  （displayName 「丸四つ」、`supportedThreadCounts: [4]`、`crossSectionProfile: .round`、
  `verificationLevel: .movementRules`）を足し、`presets` に入れる。
- `prototypeNotice`: **手順表は bookA p.56 から写した試作であること、bookC の図が無く対の中の
  先後が読めないこと、立体は描画上の近似であること**を書く。

**これで本数セレクタの 4 が埋まる。** 12 は空のまま（Task 012〜014）。

---

## 段階3: 4本の描き手

**作者の判定（2026-09-22）: 4本専用の描き手を新しく作る。** 既存の筒8本の描き手を広げない。

- 族は `.roundTube(threads: 4)`。`BraidFamilyDrawing.Drawing` に case を足し、`families` に
  `RoundTube4SurfaceMesh.shape` を足す。**族の読み方そのもの（`BraidFamily.family(of:)`）は
  触らない。**
- **`RoundTube8SurfaceMesh` と `RoundTube8SurfacePattern` を先に読むこと。** 構造は同じ
  （1サイクルに2場所進む筒、対が半ピッチで交替する）ので、**作りは真似てよいが共通化はしない**
  ——族ごとに描き手を持つのが今の作りで、形の値は族のものである。
- 組み目は **Task 051・053 で受領したもの**（頭は鈍く上に載り、尾は幅を保って隣の列へ曲がり、
  後に置かれた束の下へ潜る。束の傾きは運ぶ向きの符号から取らない）を 4本へ持ち込む。
  **051・053 で直した誤りを作り直さないこと。**
- 一覧カードの展開図・詳細の立体・模様図が 4本で出ること。**模様図（2D）は族に関わらず出るので、
  段階2 の時点で出ているはず**——出ていなければ段階2 に穴がある。

### 予言

- 4本は8本の倍の速さで回るので、**展開図の斜めは8本より急**になる。
- 束は**縦の対と横の対が半サイクルずれて交替する**（八つ金剛の半ピッチと同じ構造）。
- 写真（bookA p.10 の b）との差は残る。**寄せ切らなくてよい。差を測って報告すること**
  （測り方は `docs/measurement-procedures.md`。新しい測り方を作る前に読む）。

---

## 段階4: 検証

- **025-5 の6 の3つを自分の紐で確かめる**: `BraidRecipeIsThreeThingsTests` と
  `BraidFamilyDrawingTests` が、導出・図・積みに手を入れずに通ること、族が紐から読めて
  描き手がメッシュを返すこと。
- `sh Scripts/check-braiding-is-general.sh` を通すこと（組み方の名前が
  `BraidMethodCatalog.swift` の外に出ていない見張り）。
- **丸源氏・平源氏の形状ハッシュは不変であること。** 動いたら段階3 が族の外へ漏れている。
- 全件を1回。**テスト段階はいま約280秒で上限300秒に近い**（053）。
  **足す試験は最小限にし、テスト段階の秒数を必ず報告すること。** 上限に当たったら、
  そこで止めて報告する（自分で上限を上げない。Task 018 の例外は閉じてある）。
- 手動は iPad 1 機で、**新しく編む → 4本 → 丸四つ → カード → 立体 → 模様図**まで。
  iPhone・ダーク・大きな文字・実機・VoiceOver・UIテスト一式は対象外。
  **RealityKit の解放で落ちる事象（Task 015）が出たら、`.ips` の名前だけ記録して先へ進む。**

## 報告に必ず入れるもの

1. 段階0 の `colour_angle` の測り直し（写真と描いた紐、S と Z、ぼかし31・61）。
2. 段階1 の**先後を入れ替えた表との差**（届くか届かないか、数で）。
3. 段階1 の検算（`worked(on:)` が通ること、1サイクルで2場所進むこと）。
4. 段階3 の写真との差（測り方の番号つき）と、**`.declared` にした数の一覧**。
5. **全件の件数・失敗・スキップ・打ち切り・テスト段階の秒数。**
6. 読み替えた所（本の糸番号 → カタログの色名など）と、**読めなかったもの。**

## やらないこと

- **すず菜組（bookA p.11 / p.57）は入れない。** 細く組んだ紐を1本おもり玉につけて組む紐で、
  「糸が紐である」という模型の拡張が要る。**別のタスクにする。**
- 12玉（Task 012〜014）は今回の外。
- 物理の線（036〜044）は止めたまま。
- 049 の形状の申し送り（腹の高さ、見かけの傾き17°、網目の質感）は今回の外。
