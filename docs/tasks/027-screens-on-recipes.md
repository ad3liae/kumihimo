# Task 027: 画面を 025 の経路に繋ぐ

- 状態: **完了**（2026-09-09）。**345 通過・0 落ち・2 skip。打ち切りは無くなった。** 結果は末尾「027 の結果」。手で確かめる項目は `tasks/027-4-manual-checks.md`
- 優先度: **高。初期リリースの残りのうち、画面の側**
- 前提: **導出と描画は Task 025 で区切りがついた。** 先に読むもの——
  `docs/tasks/026-initial-release-inventory.md`（棚卸し。とくに「旧の経路が残っているところ」）、
  `docs/tasks/025-5-adding-a-recipe.md`（レシピの足し方）、
  `docs/architecture.md`「初期リリースはレシピのある紐に限る（作者の決定）」

**Task 026 の必須5件のうち、画面の3件と、棚上げの2件（015・018）を片付ける。**
**形の定数には触らない**（凍結は「形の定数を変えない」の意味で続いている）。

## 027-1: 立体プレビューの選択を族へ

**いま画面は `preset.id == .hiraGenji16` で分岐して立体のビューを選んでいる**（3か所、
`tasks/026-initial-release-inventory.md` の一覧）。**族の判定（`BraidFamilyDrawing`）に
置き換える。**

1. **プリセットからレシピを引く**（プリセット ID の生の値はレシピ ID と同じ文字列である）。
2. **族から描き手を選ぶ。** **描き手が無いレシピは描かない**——**その旨を画面に出す**
   （空の表示。**文言は短く、日本語**）。
3. **差分テスト**: 平源氏と丸源氏で**表示されるメッシュのハッシュが置き換え前後で同一**。
   **架空の紐は「描き手なし」の表示になること。**
4. **番人の範囲に画面（`ProjectEditor` / `Home` / `App`）を入れるかは、この段で組み名が
   消えたら入れる。消えなければ理由を書く。**

## 027-2: 保存の鍵をレシピ ID へ

**いま保存はプリセット ID で紐を指している**（`KumihimoProject.selectedBraidPresetID`）。

1. **レシピ ID にする。**
2. **既存の保存データは読み込み時に旧 ID → レシピ ID へ読み替える。**
   **対応表は1か所に置き、試験で固定する。**
3. **書き戻しは新しい鍵で。**
4. **差分テスト**: **旧形式の保存データ（平・丸それぞれ）を読んで同じレシピと配色が復元される**
   こと。**新形式で保存して読み戻して同一である**こと。

## 027-3: 2D の模様図を画面に

**`BraidPatternView` はあるがどの画面からも呼ばれておらず、中身が特定の紐を直接組んでいる。**
**丸源氏の図（`BraidTubeFigure`）を出す画面は無い。**

1. **`BraidPatternView` を一般化し、レシピから占有履歴経由で図を出す**
   （**特定の紐を直接組む箇所を消す**）。
2. **丸源氏の図を出す**——**列は着地する場所の角度、鏡像は一致扱い、E/W は未決。**
   **未決の注記は図の中に小さく残す**（`unsettled` をそのまま表示）。
3. **置き場所は既存のプレビューの隣**（2D／立体の切り替え）。**新しい画面遷移は増やさない。**
4. **差分テスト**: **平源氏の図は既存の期待値と不変**、**丸源氏は fixture の 8×4 と一致。**

## 027-4: Task 018 と 015

- **018**: `everyRegionCarriesTheRidgeIncludingBothEdges` **だけ**テスト単位の上限を
  **300 秒**にする。**理由をテストのコメントに書く**——**遅いだけで壊れていない、
  300 秒なら通る。** **他の試験の上限は変えない。**
  **以後の報告から「既知の打ち切り」を無くす。**
- **015**: **UI の crash で自動検証が塞がれている範囲を、手で確認する項目の一覧にして
  docs に書く**（画面、操作、期待する表示）。**自動化の再挑戦はしない。**

## 守ること

- **規則を足さないこと。** 説明できない現象が出たら、規則を足さずに報告して止まる。
- **見た目に合わせた定数を置かないこと。形の定数には触らないこと。**
- **承認された設計が破綻したら、代替を作らずに止まって報告すること。**
- `git add -A` を使わない。**docs / code のコミットは分ける。**
- **画面の文言は短く、日本語。** ビューの中に表示文字列を重複させない（`AGENTS.md`）。
- テストは `AGENTS.md`「テストの回し方」に従い、**件数で書く。**

## 段

**027-1 が終わったところで止まって報告**（作者が画面を見る）。**承認後に 027-2〜4。**

---

# 027 の結果（2026-09-09）

**着手前（main）: 329 通過・1 打ち切り・2 skip。**
**終わり: 345 通過・0 落ち・2 skip。** **「既知の打ち切り」は無くなった。**

| 段 | コミット | 通過 |
|---|---|---|
| 指示書 | `171c63f` | — |
| 027-1 立体の選択を族へ | `2937350` | 334 |
| 027-4 手で確かめる項目（015） | `12144fc` | — |
| 027-2 保存の鍵をレシピ ID へ | `e25685f` | — |
| 027-3 模様図を画面に | `e96dd22` | — |
| 027-4 遅い試験に5分（018） | `72561ae` | — |
| 027-4 AGENTS.md に記録 | `c1d9b6e` | **345** |

## 027-1: 立体プレビューの選択を族へ

**3か所の `preset.id == .hiraGenji16` を消した。** プリセットの ID の生の値はレシピ ID と
同じなので、画面は**レシピを引き → 族を訊き → その描き手を出す**（`BraidPreviewForFamily` /
`BraidThumbnailForFamily`）。**描き手が無い紐は描かず**、その場所に
**「この組み方の立体はまだ描けません」**の1行を出す。

**per-braid の文言も2つビューから出た**——試作の注記は**プリセットが持つ**ように、
3D のラベルは**紐の名前から組む**ように。**文言は変えていない**（旧定数と一致を試験で固定）。

**差分テスト**: 両方の紐で**画面が出すメッシュの頂点ハッシュが不変**（族の描き手経由）。
架空の紐は筒なので**描かれる**（描き手がある）。**別の平らな紐は「描き手なし」。**

**番人の範囲に画面は入れていない（理由）**: 画面のロジックからは組み名が消えたが、
**SwiftUI の Preview 用データ**（`ProjectEditorPreviewData` / `HomePreviewData`）が
具体的な紐を名指している。**これはカタログと同じくデータ**であり、`AGENTS.md` も
Preview 用データを本番と分ける前提なので、**番人の対象にするのは筋が違う**と判断した。
（作者の確認済み、2026-09-09。）

## 027-2: 保存の鍵をレシピ ID へ

**`BraidSavedBraid` が読み書きの唯一の場所である。** 保存の列は**古い版が書いた名前のまま**
（SwiftData の `originalName`）で、**プロパティの名前が中身を言う**ようにした。

**読み替えの表は空である。これが正直な状態である**——**既に書かれている鍵はプリセットの
ID で、プリセットの ID はレシピの ID と同じ文字列**なので、今日直すものが無い。
**表があるのは次の改名の行き先が要るからで、どこも通らない読みは誰も変えられない。**
**この主張自体を試験で固定した。**

**この版にレシピの無い紐を名乗る保存データは、開ける。** **描き手なしと同じ道**を通り、
何も描かれず、画面がそう言う。**以前は「不正なデータ」として弾いていた。**

**差分テスト**: 旧形式（平・丸）を読んで**同じレシピと配色**／新形式で保存して読み戻して同一／
**レシピの無い ID は落とさず開く。**

## 027-3: 2D の模様図を画面に

**`BraidPatternForRecipe` が紐を読む**——畳めれば表裏の2枚、管なら
**`BraidTubePatternView`** の1枚（**列は着地する場所の順**、**下に各列の角度**を出す。
等間隔でないことがここで一番間違えやすいので）。

**図が自分について未決だと言っていることを、読む場所に出す**——鏡像は一致扱い、
Task 004 の列順は未決。**その注記を図に届けるため、丸源氏のレシピは既に使っていた
断面（台の縁と同じ順）を渡すようにした。渡しているのは注記だけで、順序は宣言していない。**

**画面は既存のプレビューの場所で切り替える。新しい遷移は増やしていない。**
**図に描き手は要らない**ので、**描き手の無い紐でも図は出る**（試験で固定）。

**差分テスト**: 平源氏の図は**列6・段4・12段・寸法 (9,12) のまま**（既存の期待値）。
丸源氏は **fixture の 8×4 と一致**、列は `[0,3,4,7,8,11,12,15]`。

## 027-4: Task 018 と 015

- **018**: `everyRegionCarriesTheRidgeIncludingBothEdges` **だけ**に
  `@Test(.timeLimit(.minutes(5)))` を付け、**理由をテストに書いた**（全頂点を4回歩いて約2分、
  **遅いだけで壊れていない**、既定60秒では毎回打ち切られ**打ち切りが平常になって他の異常を
  隠していた**）。`-maximum-test-execution-time-allowance` を **300** にし、
  **`AGENTS.md` にこの1件の扱いを追記。他の上限は変えていない。**
  **これで打ち切りは無くなった（345 通過・0 落ち）。**
- **015**: **手で確かめる項目の一覧**を `tasks/027-4-manual-checks.md` に書いた
  （組み方の選択と立体・保存と読み込み・模様図と立体の切り替え・文字の大きさと配色）。
  **027-1〜3 で触った画面を含む。** **自動化の再挑戦はしていない。**
  **確かめられない2項目**（描き手の無い紐の表示、古い版の保存データ）は
  **その旨と、代わりにユニットが押さえていること**を書いた。

## 報告事項: 一度だけ出た落ち（**再現せず**）

**018 の上限を上げた直後の回で、`theDrawingsShapeIsUnchanged` が
`Test crashed with signal kill` で落ちた。**
**同じ試験だけを回すと通り、その後の通しでも通った**（345 通過・0 落ち）。
**再現していない。** 見立ては、**長い試験が打ち切られずに完走するようになったことで
1回の走りの負荷が上がり、実行体が落とされた**というものだが、**確かめていない。**
**再び出たら、そこで止めて調べること。**
