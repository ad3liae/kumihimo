# Task 044: 固定球の出自と、「固める前に張る」の局所試験（48 手の走りは無い）

- 状態: **指示書。worker に渡す。承認待ちは挟まない**（2026-09-17。043 の判定を受けて審査側が書き、**同僚の 15 回目の答え（同日: 043 受領、
  044-0 → 044-1 に賛成、A1 を先にする必要は無い）で 3 点を直した**——(1)「外側の球が判定に合う」とはまだ言わない（先端・既存固定端・
  区間内部を分ける）、(2)「握る」と「余りを払い出す」の意味を明記（既存固定端は位置と材料を固定、新しい交差点は位置だけ固定して材料が
  通過できる仮想のガイド、短縮した長さの行き先を記録、張った後に固定条件を再確認）、(3) 対照は同じ開始状態から `FIX=now` をその場で作り、
  端点の値は別欄にして区間内部の形・弧長・移った長さ・接触で読む。044-0 → 044-1 を止まらずに続け、最後に一度報告する。止まるのは止まる
  条件だけ。同僚の答えが途中で来て指示が変われば作者が伝える——来なくても待たない）
- 優先度: **編み出せる版の本線。初期リリース（Task 025 系）には触れない**
- 作成日: 2026-09-17
- 前提: **Task 040 の「現行仕様」（1〜10 項。2 は `REST=still`、5 は即時固定 `FIX=now`）**が模型と解法。道具は 041〜043 のまま
  （`Scripts/task041/checked_run.py`、`Scripts/task043/{delayed,measure,same}.py`）。**044 は本線の模型を変えない**——044-0 は保存状態を
  数えるだけ、044-1 は保存状態から 1 手だけを別の規則で回す**局所試験**（試験の中だけの模型変更）で、現行仕様には入れない。48 手の走りは
  無い（結果を見て 045 で決める）。
- 枝: `claude/task-041-crossing-order` の続き（push しない）。走りの環境変数は全部明示する
  （`CHECK=all CAP=0.25 ONTOP=rest REST=still SUPPORT=others CARRY=sweep ROUTE=under-then-over` に `FIX=now`／`FIX=taut`）。

## 何を確かめるか

043 で 1 手遅らせても形は変わらなかった。代わりに見えたこと（043 の判定 2・3、審査側が保存状態から数えた）:

- **固定球の半分は束の半径の外**にある（結び目を除く固定球のうち r > 2.5629 d: held 73%、still 55%、next 53%。r > 4 d: 19／13／11%）。
  固定部の外径 11.4〜12.0 d は 024 の束（6.13 d）の 2 倍近い。
- **固定は「まとめて」起きる**: still は 25〜36 手目に 197 球中 107 球（30 手目に 27 球）、next も 25〜36 手目に 92 球。`cover` は判定に
  合った最も外の球（先端）から固定端側を**まとめて**固める（039 から。043 の遅らせ方も先端しか遅らせない）。
- **ただし「外側の球が判定に合う」とはまだ言わない**（同僚の 15 回目の指摘。審査側が 4 手 17 run で確かめた）: still の 30 手目・糸 11 では、
  新しく固まった 12 球の最大半径は **5.375 d** だが、**判定に合った先端は r 1.672 d、既存の固定端は r 4.658 d（z −7.12）**で、最大半径の球は
  区間の途中（既存固定端の隣）。他の run も同じ形——先端は束の近く（r 1.4〜2.9 d、z −1.7〜−3.4）、既存の固定端は深い（z −3〜−8.4 d）、
  最大半径は途中（2.1〜5.4 d）。つまり**深い古い交差から束の脇を上り、肩で外へ張り出して天辺に乗る区間が、先端が乗られた瞬間に
  その形のまままとめて固まる**。「外側に張り出した区間がまとめて固まる」は観測に合う。「外側の球が判定に合う」は合わない。

審査側の仮説（確かめていない。044 で確かめる）: **乗った区間は張らない（040 の 2）＋ 判定に合ったらまとめて固める（040 の 5）＝ 張り出した
形のまま凍る**。実物では、糸は最後の交差と新しい交差（乗られた点）のあいだで玉に引かれて張る。模型にはその張りが無い。押し付け A1 は縦の
詰めだけを足す。**実物の交差と同等だとは、まだ扱わない**（試験の中の模型変更）。

## 044-0. 固定球の出自（保存状態だけ。走りは無い）

still（`.build/task042/still-1-12/`・`still-13-48/`）と next（`.build/task043/next/`）の各手の pickle の差から、**結び目を除く固定球の 1 つ 1 つに**:

1. 固まった手 h（`made[t]` が伸びた手。手番号 `carried[t]` ではない）、そのときの r・z（固定球は送りで一緒に下がるだけなので r は固まった
   ときのまま。z は固まった手の pickle で読む）、糸 t、run の中の位置（既存固定端から何番目）、**役割**——**先端**（判定に合った最も外の球
   ＝その手の `made[t][-1]`）／**区間内部**／（比較のため）**既存の固定端**（その手の前の `made[t][-1]`）。
2. **run**（同じ手に同じ糸で固まった球の並び。**既存の固定端から最初の新固定球への繋ぎを含める**）ごとに: 新しい球数、弧長（既存固定端から
   先端まで）、既存固定端と先端の直線距離、その比、**既存固定端の r・z／先端の r・z／区間内部の r の最大・中央値と z の中央値**（3 つを
   分ける）、区間内部の球のうち**下に何も無い球**（真下 d 以内・1.25 d 以内に固定球も芯も無い）の数。
3. 表: 手 × (固まった球数、run の数、run の最大の長さ、先端の r、内部の r の最大)。48 手の合計で、run の長さの分布（1／2〜5／6〜10／11 以上）
   と、その長さの run に入る球の数。「まとめて固まる」がどこで起きるかを、手（サイクルの中の位置）と、先端・既存固定端・内部の r で示す。
4. 束の半径の外（r > 2.5629 d、> 3.06 d、> 4 d）で固まった球の割合を、**先端／内部**に分けて再計算（043 の判定 2 の合計と合わなければ止まって報告）。
5. 043 の「再判定に合った 74 球／巻き込まれた 115 球」が、どの run（長さ・先端の r・内部の r）に入っていたか。**115 球が即時固定と同じ手に
   固まったかは、この集計からは言えない**（043 の判定の断定は外した）。

script は `Scripts/task044/origin.py`（保存状態を読むだけ。`__main__.Braid043`・`Braid041` を名乗らせる）。数字は文書に写す。

## 044-1. 「固める前に張る」の局所試験（保存状態から 1 手だけ。模型の変更はこの試験の中だけ、`FIX=taut`）

### 規則（同僚の 15 回目の答えのとおり。追加規則として試す）

手 h の `cover` で、糸 t の判定に合った最も外の球（先端 `free[t][cut]`）が決まったら、固める前に、既存の固定端から先端までの区間を張る:

1. **既存の固定端**（`made[t][-1]`）は**位置と材料を固定**する（今までどおり動かない。材料はそこを通らない）。
2. **新しい交差点**（先端の位置）は**位置を固定するが、糸の材料は通過できる仮想のガイド**として扱う——区間が短くなった分の材料は、
   ガイドを通って**縁側の自由部**（`free[t][:cut]`）へ、さらに縁の出し入れで**縁の外**へ移る。伸びない糸なので、**両端の材料を固定したまま
   区間を短くすることはできない**——短くなるのは材料がガイドを通り抜けるから。
3. 区間の張りは 022 の張り（縮め 0.1・置き直し・settle。settle は全部の球を相手に現行どおり——非貫入・台の押し出し・上限 d/4・
   040 の 10 の検査）。区間の置き直しは既存の固定端からガイドまでを d 間隔で置き直し、余った材料は縁側へ渡す（縁側は現行どおり置き直しと
   縁の出し入れ）。**短縮した長さが、縁側の自由部または縁の外へどれだけ移ったかを記録する**（区間の弧長の減少 ＝ 縁側の弧長の増加 ＋
   縁から払い出した長さ。**球の削除で長さを消しただけにならないこと**を人工例で確かめる）。
4. **張った後、元の固定条件（乗られている・他の糸の固定球か芯に 1.25 d 以内で触れている・縁の端でない）がガイドの位置の球で成立しているかを
   再確認する**。成立していれば既存固定端からガイドまでを固める。**外れていれば固定せず**（その手はその糸を固めない）、その結果を記録する。
5. 乗った区間の規則（still）・判定の条件・閾値・縮めの値・送り・上限・検査は変えない。**どう実装したかを先に書く**（ガイドの作り、置き直し、
   払い出し、再確認）。作りに迷ったら止まって報告——仕様を勝手に足さない。

### 試験

まとめて固まる手を 4 つ——still の **27・30 手目**、next の **29・34 手目**。**各手は同じ開始状態（`start-hNN.pkl`）から `FIX=now` と
`FIX=taut` を回して比べる**（検査つき）。still の `FIX=now` は保存済みの `h27`・`h30` と球ごとに一致するはず（一致しなければ止まって報告）。
**next 由来の開始状態でも `FIX=now` の対照をその場で作る**——元の `FIX=next` の終了状態を対照にはしない。

各手・各 run について、対照と試験で:

1. 固まった球数と run の長さ。**端点は別欄**——既存固定端の r・z、先端（ガイド）の r・z（端点を固定するので、区間の最大半径は端点の半径より
   小さくできない。「最大半径が 0.5 d 縮む」を一律の判定にしない）。
2. **区間内部の形**——内部の r の最大・中央値、z の中央値、下に何も無い球の数、区間の弧長と両端の直線距離（比だけでは、障害物を回るための
   長さと余りを区別できないので、比は参考）。
3. **移った長さ**——区間の弧長の減少、縁側の自由部の弧長の増加、縁から払い出した長さ（3 つが釣り合うこと）。
4. 張りの前後で区間の球がどれだけ動いたか（最大・中央値）、何 sweep で静まったか、settle の上限到達、上限が抑えた球。
5. 貫入 (a)、検査（接触・未確定）、縁の出し入れ、その手の時間。
6. **再確認の結果**——ガイドの球が固定条件を満たしたか。満たさなかった run は固定せず、その旨。
7. run が**どこへ行ったか**を 1 行——**下がる／内側へ寄る／浮く／変わらない**——を、内部の r と z の変化・移った長さ・接触で裏づける。

人工例（`Scripts/task044/taut_check.py`。小さな状態、糸 2 本・芯数球）: (i) 両端を握った区間が障害物の上を張って下がる、(ii) 短くなった
長さが縁側へ移り、**材料の総長（区間＋縁側＋払い出し）が変わらない**、(iii) 既存固定端は動かず、ガイドの位置も動かない（ガイドを通る
材料は変わる）、(iv) 張った後の再確認で条件から外れた区間は固定されない。

script は `Scripts/task044/taut_then_fix.py`（`Scripts/task043/delayed.py` の `Braid043` を継承し `cover()` だけ上書き。`FIX=taut` で有効。
`checked_run.py`・`probe.py`・040〜043 の script は変えない）。走りは `.build/task044/<still|next>-hNN-<now|taut>/`。

## 予言（回す前に書く。合わせにいかない）

- 044-0: 固定球の半分以上が長さ 6 以上の run に入る。長い run ほど内部の r の最大が大きく、先端の r は束の近く（3 d 未満）、既存固定端は
  深い（z −5 d 以下）——4 手 17 run で見た形が 48 手でも続く。下に何も無い球は区間内部（肩）に集まる。
- 044-1: 張ると区間内部は**内側へ寄って下がる**（内部の r の最大は対照より小さく、z の中央値は低い）。移った長さは長い run（6 球以上）で
  1 d 以上、材料の総長は保存される。再確認で条件から外れる run は 4 手で 1 つ以下。貫入は 0.02 d 未満、接触 0。**外れたら（浮く・変わらない・
  詰まる・条件から外れる）、それが答え。**

## 合格条件・止まる条件

**合格**: (1) 044-0 の表（still と next。先端・既存固定端・内部を分けて）。043 の判定 2 の割合が再計算で合う。(2) 044-1 の 4 手の対照
（同じ開始状態から `FIX=now`）と試験の表、実装の説明、人工例 (i)〜(iv)、移った長さの釣り合い、再確認の結果、各 run の「どこへ行ったか」。
(3) 予言との突き合わせ、未解決事項。

**止まる**: (a) 044-0 の再計算が 043 の判定 2 と合わない（数え方の違いを報告）。(b) 044-1 で通り抜け・未確定（保存して報告）。
(c) 044-1 の 1 手が計算で 10 分を超える。(d) 規則の作りに仕様の追加が要る（止まって報告）。(e) still の `FIX=now` が保存済みの結果と
一致しない。

## 段

- 044-0 → 044-1 → 報告 1 回。**承認待ちは挟まない。** 48 手の走りはしない。結果（下がる・内側へ寄る・浮く・変わらない）に応じて次を選ぶ。

## 守ること

- 本線の現行仕様は変えない。044-1 の規則は試験の中だけ（`FIX=taut`）。実物の交差と同等だとは扱わない。
- 数字は**糸の固定球（結び目を除く）・結び目・自由球・人工の芯**を分ける。**先端・既存固定端・区間内部**を分ける。数字の名前を分ける。
  1 件から誤差幅を出さない。
- product code に触れない。テストは走らせない。`.build/task044/` の外に走りを置かない。docs と Scripts のコミットは分ける。`git add -A` はしない。
  VM から push しない。前で回す。重い走りは 1 つずつ。
