# Task 001: iOSプロジェクトと起動画面

- 状態: Done
- 優先度: 最優先
- 仕様: `docs/specifications/home-screen.md`

## Workerへの依頼

SwiftUIによるiOSアプリの土台を作り、起動画面仕様を実装する。

作業前に、ルートの `AGENTS.md` と以下を読むこと。

- `docs/product.md`
- `docs/architecture.md`
- `docs/specifications/home-screen.md`

## 実装範囲

- iOS 17以降を対象とするXcodeプロジェクト
- SwiftUIアプリのエントリーポイント
- `NavigationStack` と `List` を使った起動画面
- 「新しく編む」行
- 保存作品の一覧と空状態
- 作品行とサムネイルのプレースホルダー
- 新規作品画面と編集画面の仮遷移先
- SwiftDataによる初期作品モデルとローカル保存
- 確認付き削除
- 空／複数件／長い名前を確認できるPreview
- 並べ替えと削除に関する自動テスト

## 実装上の注意

- Preview用サンプルを本番データへ投入しない。
- 実際の組紐描画や編集機能は実装しない。
- 組み方の種類を正式な製品仕様として固定しない。
- 外部パッケージを追加しない。
- 仕様上の未決事項を独断で大きく拡張しない。

## 完了条件

`docs/specifications/home-screen.md` の受け入れ条件をすべて満たし、利用可能なビルドとテストが成功すること。

シミュレータを利用できる場合は、少なくとも次を目視確認する。

- 保存作品なし
- 保存作品あり
- ダークモード
- アクセシビリティ文字サイズ
- 削除のキャンセルと確定

## 完了時の文書更新

このファイルの状態を `Done` に変更し、末尾に次を追記する。

- 完了日
- 実装概要
- 実行した検証
- 残った制約または次の作業候補

## 完了記録

- 完了日: 2026-08-23
- 実装概要: iOS 17以降を対象とするSwiftUI／SwiftDataアプリを作成し、起動画面、新規作成と作品編集の仮遷移先、更新日時順の保存作品一覧、空状態、サムネイルのプレースホルダー、確認付き削除、Previewを実装した。保存コンテナ生成または一覧取得に失敗した場合は、正常な空状態と区別したエラー表示と再試行手段を提供する。編集仮画面にはSwiftDataモデルではなく作品UUIDを渡す。
- 実行した検証:
  - Xcode 27.0 Beta、iPhone 16（iOS 18.5）でDebug Schemeをビルドし、単体テスト5件とUIテスト3件の計8件が成功した。
  - 更新日時順と同一日時の第2キー、削除の状態処理、コンテナ生成／一覧取得失敗からの再試行を単体テストで確認した。
  - 空状態から新規作成画面を開く、保存作品から編集画面を開く、削除をキャンセルした後に削除を確定するフローをUIテストで確認した。
  - Release構成のiOS Simulator汎用ビルドが成功した。
  - Simulatorで保存作品なし／3件、長い作品名、サムネイルなし、ライト／ダークモード、最大アクセシビリティ文字サイズを確認した。
- 残った制約または次の作業候補:
  - VoiceOverの実際の読み上げ順序とVoiceOver経由の操作、iOS 17ランタイムおよび実機での実行は未確認。
  - 削除保存失敗時のUI表示は実装済みだが、失敗注入によるUIテストとロールバックの自動検証は今後の候補。
  - 実サムネイル生成、作品の作成・編集機能、正式なアプリアイコンは後続タスクで実装する。
