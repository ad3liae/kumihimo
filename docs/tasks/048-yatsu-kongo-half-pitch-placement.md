# Task 048: 八つ金剛の束を半ピッチずつずらし、螺旋としてつなぐ

- 状態: **指示書・実装前**（2026-09-19、Codex）→ **実装済み・審査待ち**（2026-09-20、Claude Code）
- 実装担当: Claude Code。Codexは実装・画像・報告を審査し、ソースを変更しない。
- 作者の指摘: **「タイルの置かれ方が違う。綺麗な螺旋になっていない。半ピッチずつずれていくため、きれいな螺旋になる。1ピッチずれることはない。」**
- 対象: 八つ金剛S/Zの描画用の配置、立体と一覧の一致。丸源氏の重なりの再指示はTask 047として継続する。
- 045/046の外観受領・PR #29のマージは履歴として残す。**今回発見された配置の問題を、以前の「追加調整不要」を理由に見送らない。**

## 1. 読むものと今回の決定

`AGENTS.md`、`CLAUDE.md`に従い、product → architecture → `braid-preset-expansion.md`・
`project-editor.md` → 本書の順に読む。関連する経緯は031、032の到着位相、033の周期継ぎ目、
045の一覧と立体・最新配色の修正、046の糸束の腹と差し込み。

**今回は、手順中の到着時刻をそのまま完成表面の長手位置にする仮定を見直す。**
045/046にあった「到着位相を維持」は、手順の履歴として維持する。
その値を描画上の位置に直接使い続ける制約は、今回の作者の指摘を受けて解除する。
Task 025の「形は族の描き手、色は占有履歴」の範囲で直す。
半ピッチは作者が指定した見た目の配置条件であり、手順から導出済みの物理的結論と記載しない。

ここでの1ピッチ `P` は、**同じ周方向の列で隣り合う束の対応する基準点の長手間隔**とする。
現行では1サイクルの長さに相当する。束の潜り込みを含む全長1.9サイクル、配色の反復周期、
螺旋が一周する長さとは別である。基準点には各束の腹の中央など同じ局所位置を用いる。

作者のスクリーンショットは現状の不具合例として保存した:

- `.build/task048/author-reference/2026-09-19-yatsu-kongo-s-a.png`（21:48:33）
- `.build/task048/author-reference/2026-09-19-yatsu-kongo-s-b.png`（21:48:45）

実物資料は `.build/task031-photos/bookA-p8-9-zoom-b-a-S.png`、手順・配色は
`.build/task031-photos/bookA-p54-55-yatsu-kongo.png`。
`.build/`は管理外なので、判断・式・結果は本書に残す。

## 2. 確認できた現行の問題

Codexが確認した基準は `2d08a95`。048の開始時にも実際のHEADと既存変更を記録する。

`RoundTube8SurfacePatternGenerator.generate` は各列について
`phase = arrivalInstant / method.steps.count` とし、束の基礎区間を
`row - 1 + phase ... row + phase` に置く。
Sの到着手番は場所1〜8で `1, 3, 2, 4, 1, 3, 2, 4`、分母は4。
現行コードとTask 032の記録を照合すると、次の配置になる:

| 場所 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 列の位相 / P | ¼ | ¾ | ½ | 1 | ¼ | ¾ | ½ | 1 |
| 次の列との差 / P（正方向、mod 1） | ½ | ¾ | ½ | ¼ | ½ | ¾ | ½ | ¼ |

**全隣接列が半ピッチではない。** 束の傾き `.35 columns/cycle` と輪郭の変更だけでは、
この配置の不揃いは解消しない。上表はコードと既存記録の読み取り・数値計算であり、
Codexが今回アプリやテストを実行して測定した結果ではない。
スクリーンショットのどの境界が厳密に1Pに見えるかまでは測定していない。

現行の `noCellBoundaryRunsTheFullHeightOfTheCard` と `noRingOfCellEndsGoesRoundTheSolid` は、
隣接列の境目が揃わないことを確かめる。¼や¾のずれでも通り、今回の条件を保証しない。
Task 033で直した「周期端で全列の位相が落ちる」問題とも区別する。

## 3. 修正する配置

1. **周方向に隣へ進むごとに、束の基準点が半ピッチずつ同じ向きへ進む配置にする。**
   円筒を展開して追えば滑らかな斜めの列になり、円筒へ戻せば螺旋になること。
   S/Zは既存の向きの約束に従って鏡となるようにする。
2. 周方向を一周しても、長手のリピートをまたいでも、途中で1P飛ぶ・足踏みする・逆へ戻る箇所を作らない。
   展開した列番号を `j`、対応する束の基準点を `x_j` とすれば、螺旋を追う向きで
   `x_(j+1) - x_j = ±P/2` が同じ符号で続くことを示す。
   8列を一周した幾何上の進みは4Pになるが、これを糸IDや配色が4Pで反復する証明に使わない。
3. **位相の余りだけを0と½にして終えない。** 周をまたいだときの行番号・リピート番号との対応も持ち、
   円筒の同じ場所には同じ区間が戻ることを確認する。
   描画用の再配置と、元の `(サイクル, 場所, 糸ID)` の対応を少数の具体例と表で示す。
4. 糸IDと色はその対応から運ぶ。色が同じだから隣へつなぐ、配色に応じて形を変える、
   見栄えのよい順にIDを付け替える実装にはしない。
   描画の行番号をずらす場合は、その整数分を取り落とさず対応表へ含める。
5. 立体・カード・可視面の選択・下地を、同じ描画用配置から作る。
   全体を回す、カメラを傾ける、束の表面だけを斜めにする、色や影を描き足す対応では完了にしない。

最初の比較では046で受領した束の輪郭・素材を保ち、配置だけの効果を示す。
その配置で隙間・不自然な重なりが出る場合は、八つ金剛の描き手の範囲で必要な幅・潜り込みを調整してよい。
調整理由と配置のみの版との差を残す。新しい共通基盤や物理研究へ広げない。

維持するもの:

- 手順表、盤の順序、占有履歴と糸ID、手番の数え方。Task 035の未確認事項は未確認のまま。
- 初期比較の `pitchOverDiameter = 0.403`、名目径、照明、露出、カメラ。全体を伸縮して半ピッチに見せない。
- 045追補で揃えたカードの向きと可視面、最新の要求キーだけを表示するローダー。
- 丸源氏・平源氏の出力、保存形式、編集操作。

現行テストに「到着時刻と描画位置が同一」という仮定があれば、今回の仕様変更として明示して更新する。
新配置から元の占有区間を引ける検証に置き換え、色の正しさを古い座標との一致だけで判定しない。
もし半ピッチの配置と糸IDの対応が両立しない具体例が出た場合は、図とIDで矛盾を示し、
手順表や配色を黙って変えずに報告する。単に古い描画テストが落ちることを矛盾とはしない。

## 4. 比較画像と検証

まず既存の製品経路から、束の基準点と列番号を読める展開図を作る。
修正前後で全8列、長手2リピート以上を並べ、半ピッチの歩みと両方向の継ぎ目を示す。
注記は診断図だけとし、製品表示へ追加しない。

配色は生成り単色、2色、1本だけ色を変えたもの、糸IDを区別できる診断色を使う。
作者の画面の初期配色を確認できるなら盤番号との対応を記録して再現する。
画像から初期配色を確定できなければ推測で「同じ」とせず、既知の2色配置と明記する。
単色で束の並びを、2色で帯のつながりを、1本色・診断色でIDの対応を確認する。

必要な自動検証:

- 製品が生成した束の基準点を読み、全8組の隣接列と周期端で半ピッチの関係が成立する。
  期待値の式をもう一度計算するだけのテストにはしない。幾何の繰り返しと糸IDの繰り返しを分けて検査する。
- S/Zの鏡、糸IDと入力配色の対応、色を変えても形が不変であること。
- 立体とカードの可視区間の一致、全周と長手の継ぎ目で欠落・二重描画・色の飛びがないこと。
  描画用配置を新しく持つなら画像キャッシュのキーにも必要な差が反映されること。
- 最新配色を維持する既存のローダー回帰テスト。丸源氏・平源氏の既存出力が変わらないこと。

新しい半ピッチの検証は現行配置を拒否できるものにする。
確認のために製品ソースを一時的に壊して全件テストを走らせる必要はない。
検証対象の小さな配置データや旧配置を使い、局所的に不足を再現する。

見た目はS/Zの正面・背面・側面と、全周を追える回転表示で確認する。
同一寸法・同一撮影条件の修正前後を残し、カードも立体と対応づける。
iPadを主比較とし、iPhoneの表示、ライト／ダーク、大きな文字も利用可能な範囲で確認する。
配置の数値が合うだけで外観を合格とせず、作者へ比較画像を提示する。

ビルドとテストはAGENTSの時間制限・機体・結果記録の規則に従う。
関連テストを先に、全件は最終版で1回。UIテスト一式は既定に含めない。
`DRAW_SHEETS`等を置いた場合は必ず外し、braidingの一般性検査も実行する。
打ち切りと未確認事項を、成功した実行と分けて報告する。

## 5. 作業の分離と完了報告

047の丸源氏再指示は撤回しない。048は別の不具合修正として差分と結果を分ける。
作業開始時の他タスクの変更を取り込んだり戻したりせず、既存の枝と未コミット変更を確認して進める。

報告には次を含める:

- 修正前後の8列の配置表、`P`の定義、周と長手の継ぎ目、糸IDへの対応。
- 配置のみの比較と、必要だった輪郭調整。最終コードに対応する画像の場所。
- 変更ファイル、コミット、関連・全件テストの結果と時間、未確認の範囲。
- 元の未解決事項（035等）と、今回残った配置・外観の問題を区別した申し送り。

実装後は**審査待ち**とする。以前の045/046のマージ指示を、この新タスクの自動マージ指示として使わない。

## 実装結果（2026-09-20、Claude Code）

状態: **実装済み・審査待ち。**見た目の採否は作者と Codex に委ねる。

### 1. 開始状態・コミット・変更ファイル

- 着手時の作業ツリーは `claude/task-047-maru-genji-appearance`（`10499d2`）で、未コミット変更は Codex の文書（`docs/README.md`、`docs/architecture.md`、
  046 の冒頭追記、本書は未追跡）。**047 は `origin/main` に PR #30 で入っていた（`82e7fed`）**ので、`82e7fed` から新しい枝
  `claude/task-048-yatsu-kongo-half-pitch` を切り、文書を `a074e59` として入れた。047 の差分は取り込みも戻しもしていない。`prototypes/` と `Claude outputs/` は触っていない。
- `c1366db` 配置の変更と試験。`YatsuKongoComparisonPreview.swift`（デバッグのみ）と `Scripts/task048/unrolled.py` は次のコミット。本書と README は最後のコミット。

| ファイル | 変えたこと |
| --- | --- |
| `Kumihimo/Domain/RoundTube8SurfacePattern.swift` | 升の描画上の開始位置を、到着の位相から `halfPitchPlacement` の位相へ。到着の位相（`arrivalPhaseBySlot`）と描画の位相（`drawnPhaseBySlot`）を両方持つ |
| `KumihimoTests/RoundTube8HalfPitchTests.swift`（新規） | 下の 4 |
| `KumihimoTests/RoundTube8CardAgreesWithSolidTests.swift` | 045 の審査点の試験で、列0の糸束を到着 −0.75 ではなく描画の位相から探すように（主張は「その点でカードと立体が同じ糸束」に。045 当時の「先着が見える」は旧配置での具体例だったので外した） |
| `KumihimoTests/RoundTube8SurfaceTests.swift` | ハッシュ |
| `Kumihimo/Features/BraidSimulation/YatsuKongoComparisonPreview.swift` | 比較用の配色 `redblue`・`one` を追加（`#if DEBUG`、本番UIは不変） |
| `Scripts/task048/unrolled.py`（新規） | 展開図 |

**束の輪郭・素材・照明・カメラ・カード・可視面の規則・ローダーは1行も変えていない。**輪郭の調整は不要だった（下の 3）。

### 2. 配置（P = 同じ列で隣り合う束の対応点の長手間隔 = 1サイクル）

**描画の位相**: 周方向の隣の場所と半ピッチずつ交互（8 は偶数なので一周で同じ交互に戻る）。交互の取り方は2通りあり、**全場所の到着からの移動量の和が小さいほう**を採る
（同点なら偶数番目の場所が ½）。位相は (0, 1] で持ち（1サイクルを 1 と書く）、升は今までどおり自分の行の境目をまたいで立つ。**手順から導いたのではなく、作者が指定した見た目の条件である。**

| 場所 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S 到着の位相（変更前の描画）/ P | ¼ | ¾ | ½ | 1 | ¼ | ¾ | ½ | 1 |
| S 描画の位相（変更後）/ P | ½ | 1 | ½ | 1 | ½ | 1 | ½ | 1 |
| S 次の場所との差（変更前、mod 1） | ½ | ¾ | ½ | ¼ | ½ | ¾ | ½ | ¼ |
| S 次の場所との差（変更後） | ½ | ½ | ½ | ½ | ½ | ½ | ½ | ½ |
| Z 到着の位相 / P | 1 | ½ | ¾ | ¼ | 1 | ½ | ¾ | ¼ |
| Z 描画の位相 / P | 1 | ½ | 1 | ½ | 1 | ½ | 1 | ½ |

- 移動は各場所で 0 か ¼ サイクル（最大 ¼）。**S と Z は鏡**（場所 p ↔ 9−p で描画の位相が一致する）。
- **糸束の傾き（手順表の運びの向き）の方へ隣の場所をたどると、毎回 +½P**。8場所で一周すると **4P 先**の同じ場所の束に戻る（試験で確認）。
  **この 4P は幾何の繰り返しで、糸 ID や配色の繰り返し（8サイクル）とは別である。**
- **半ピッチのずらし自体には向きが無い**（+½ と −½ のどちらにも束がある）。螺旋が上る向きは糸束の傾きのほうで決まっており、それは手順表から来る（045 から不変）。
- 周の継ぎ目（場所8→1）もリピートの継ぎ目も、同じ ½ で続く（試験と展開図）。

**糸 ID との対応**（`(サイクル, 場所, 糸)` は占有履歴のまま。描画の開始だけが動く。S の抜粋、製品から書き出した値）:

| サイクル | 場所 | 糸（位置番号） | 到着の開始 | 描画の開始 |
| --- | --- | --- | --- | --- |
| 0 | 1 | 1 | −0.75 | −0.50 |
| 0 | 2 | 2 | −0.25 | 0.00 |
| 0 | 3 | 3 | −0.50 | −0.50 |
| 0 | 4 | 4 | 0.00 | 0.00 |
| 1 | 1 | 4 | 0.25 | 0.50 |
| 1 | 2 | 5 | 0.75 | 1.00 |

描画の開始から描画の位相を引いた値は必ずそのサイクルの整数行になり、その行の占有履歴でその場所にいる糸が升の糸と一致する（全 64 升、S・Z、試験）。
行番号を整数ずらす処理は入れていないので、取り落とす整数分も無い。半ピッチと糸 ID が両立しない例は見つからなかった。

### 3. 比較画像（`.build/task048/`）

- `before/`（`82e7fed` = main。比較用の配色だけ同じ `YatsuKongoComparisonPreview.swift` を入れてビルド）と `after/`（最終コードと同じ内容）。S/Z × 生成り・赤青・1本だけ青・8色 ×
  0/60/90/180/270° とカード。iPad (10th generation) シミュレータ、同じ撮影条件。
- `front-before-after.png`: 正面の変更前後（S/Z × 生成り・赤青・1本）。
- `unrolled.png`: **8列 × 2リピートの展開図**。各束の腹（開始から 0.75 サイクル）を点に、糸の位置番号を書き、傾きの向きの次の場所で ½P 先の束を緑、そうでないものを赤で結ぶ。
  変更前は全ての場所で赤が混じり、変更後は周の継ぎ目も含めて全て緑（右端の赤は図が 16 サイクルで切れているためで、次の束が図に無いだけ）。点は製品が生成した升を書き出した `cells.json` から描いた。
- `author-reference/`: 作者の画面（変更前）。**作者の初期配色は画像から確定できないので、再現とは言わない。**比較の赤青は bookA p.54 の並び（黄橙橙黄）の2色を赤と青に置き換えた既知の配置である。
- `manual/`: 手で見た画面。

**見え方**: 生成り単色で、束が不揃いにずれていた所がなくなり、束が斜めの列にそろって並ぶ。赤青では帯が途中で段になっていた所が滑らかにつながる。1本だけ青の糸は、同じ間隔で一定の向きに進む。
**配置だけを変えた版がそのまま最終版**で、隙間や不自然な重なりは目立たなかったため、幅・潜り込みの調整はしていない（下地の見える割合の番人 1% 未満も通っている）。

### 4. 試験

- 新規 `RoundTube8HalfPitchTests`（S・Z）:
  - `everyPlaceIsHalfAPitchFromTheNext` — 製品の升から各束の腹の位置を読み、**全8組の隣接（8→1 を含む）と3リピートにわたって**、どの束にも隣の場所に ½P 離れた束があり、同じ高さ・¼・1P ずれが無い
  - `theArrivalPlacementWouldFail` — **同じ検査が変更前の配置（到着の位相）を拒む**。S の到着と描画の位相の値も固定
  - `followingTheLeanTheSpiralClimbsHalfAPitchAPlace` — 傾きの向きへ8場所たどると毎回 +½P で、4P 先の同じ場所に戻る
  - `everyDrawnCellIsTheThreadTheBraidingPutThere` — 全升が占有履歴の `(サイクル, 場所, 糸)` に戻る。到着からの移動は ¼ 以下
  - `theShapeIsTheSameWhateverTheColours` — 配色を変えてもメッシュのハッシュが同じ
- 既存: カードと立体の可視面の一致（1リピート全体、除外帯は 045 のまま広げていない）、端が別の束の下で終わること、下地 1% 未満、S/Z の鏡、閉じ・端面なし、ローダーの完成順、全て通過。
  隣接列の境目がそろわないことを見る既存の2試験（`noCellBoundaryRunsTheFullHeightOfTheCard`・`noRingOfCellEndsGoesRoundTheSolid`）も通る（½ のずれは境目をそろえない）。
- 八つ金剛のハッシュ `0x94ae_7327_2c10_992d` → `0x059f_de6a_459f_8b01`（頂点数 36,480 は不変）。**丸源氏・平源氏のハッシュは不変。**
- **関係試験**（`RoundTube8HalfPitchTests`・`RoundTube8SurfaceTests`・`RoundTube8CardAgreesWithSolidTests`・`RoundTube8CardLoaderTests`・`BraidOrientationTests`・`BraidMeshHashTests`・`YatsuKongoTests`）:
  1回目は 54 成功・2 失敗（ハッシュ、045 の審査点の試験が旧配置の −0.75 を探していた）。**この回は Bash の上限 300000 ms に達し**、ハッシュの失敗後の診断収集で延びて
  649 秒で自然に終了した（停止していない、試験ごとの打ち切り 0）。直した後の2回目は **56 件全て成功、47 秒**（`.build/test-results/task048-related2.xcresult`）。
- **全件を最後に1回**: `-only-testing:KumihimoTests -parallel-testing-enabled NO -test-timeouts-enabled YES -default-test-execution-time-allowance 60 -quiet
  -resultBundlePath .build/test-results/task048-all.xcresult`（直前に `build-for-testing`）、iPad (10th generation) `440CB589-…`、上限 300000 ms。
  **435件中 431件成功・0件失敗・4件スキップ、試験ごとの打ち切り 0件、テスト段階 238 秒（全体 239 秒）。**スキップは `DRAW_SHEETS` のときだけのシート4件（機体に置かれていないことを確認）。
  expected failure は既知の平源氏の1件。番人 `check-braiding-is-general.sh` 通過、ビルド警告なし。UI テスト一式は実行していない。
- 展開図用の書き出しは、通る一時試験がファイルへ書く形にした（意図的に失敗させる計数はしていない。一時試験はコミットしていない）。1回目は macOS に `timeout` コマンドが無く xcodebuild が走らなかった。

### 5. 手で確かめたこと・確かめていないこと

- iPad (10th generation)、ライト・標準文字: 実際のエディタで8本・糸1を青にし、一覧のカード（S・Z）で青の糸が等間隔の螺旋になること → S の詳細（カードと同じ）→ 回転 → リセット。
- iPhone 16、ダーク・アクセシビリティ最大文字: 起動引数で Z の立体とカード（赤青）。確認後にライト・標準へ戻し停止。
- **確かめていない**: 実機、VoiceOver、横向き、iPad のダーク・大きな文字、iPhone での実際の一覧→詳細の操作と拡大。

### 6. 申し送り

- **今回の配置**: 半ピッチは作者の指定した見た目の条件として入れた。手順の到着時刻との差（最大 ¼ サイクル）は描画上の近似であり、実物の組みの先後や到着を確定したものではない。
  交互の取り方の選択（到着からの移動の和が小さいほう）も描画上の選択である。
- **046 から残る外観の差**（束の傾きが写真より大きい、腹・輪郭・質感）は変わっていない。
- Task 035（bookC との照合、対の中の先後）と、写真の組み点側は未確認のまま。
