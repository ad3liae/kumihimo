# Task 050: 平源氏の中央の丸みと、縁の潜り込みを描く

- 状態: **作者が現状の外観を受領・PR #32でマージ済み**（2026-09-21、main `57cfba2`）。実装 `f18e59b`、報告修正 `eb693cd`、審査後の記述訂正 `0b86f17`。当時の審査と申し送りは末尾9節に残す。
- 起点: 作者「正直なところあまり改善していないが、PR作成させてマージした。平源氏の見た目の改善に取り組みたい」。
- 基準: main `47f396a`（PR #31）。048/049はマージ済みだが、八つ金剛の外観が合格したという意味ではない。残る差は保留し、今回は平源氏へ進む。
- 対象: 平ら16本の描き手、平源氏の立体と一覧カード。物理計算、手順表の再検討、他の組み方の改善は含めない。

## 1. 今回の到達点

**中央には糸束の丸い腹があり、縁の糸束は中央へ曲がってその下へ潜る。**
通常の表示寸法でもこれが分かるようにする。細かい糸筋を増やしただけ、明暗を強めただけの変更では完了にしない。

現行の `Flat16SurfaceMesh` は、各升を断面に沿わせて高さを加える構造である。
`crossingRelief` は腹と横向きの盛り上がりの最大値を取り、どちらも同じ升の色で描く。
この構造では、別の糸が中央の下へ入り込む関係を表しにくい。
`Flat16StitchTexture` の四辺の暗化と、カードの升ごとの輪郭線も格子の印象を強める候補である。
これはコードからの診断であり、現行版の比較画像H0で見える問題と照合してから直す。

**配色の場所を表す升と、糸束の外形を分ける。** 升の占有履歴は保ち、可視部分の糸束を
丸み・肩・潜る端を持つ面として描く。少なくとも縁と中央の接点には、実際に前後して隠れる別々の面が必要である。
全16本の内部経路や繊維1本ずつのメッシュ、接触を解く物理模型は不要。

## 2. 比較する資料

資料はすべてローカルで読める。形の比較に使う写真と、配色の検査に使う資料を区別する。

| 資料（リポジトリ相対パス） | 見るところ／限界 |
| --- | --- |
| `.build/task005h-references/hiragenji-face-closeup.png` | 手に持った桃・黄・白の実物。中央の束の丸み、隣との接し方、束に沿う糸筋。曲がりと遠近があるので写真全体からピッチを測らない。 |
| `.build/task005h-references/hiragenji-edge-view.png` | 同じ実物の縁。白い束の厚み、縁の起伏、中央の下へ入る境目。写真だけから隠れた糸の接続を作らない。 |
| `.build/bookA-genji-photos/bookA-hira-genji-finished.png` | 「23 平源氏組 Hira-genji p.72」と載る完成写真。中央と白い縁の形、長手の反復。フォルダ名と書籍の呼称を混同しない。濃紺の明暗から高さを逆算しない。 |
| `.build/task007-references/book-step-7-and-color-variants.png` | bookA p.97の配色実験。横糸だけの着色、縁の矢絣、本体のはしごを確認する。 |

過去の `task007e`・`task007j`・`task025` のレンダーは試作の記録であり、現行版H0の代わりに使わない。
写真との比較は局所の紐幅をそろえ、縦横を別々に引き伸ばさない。写真の初期配色を推定できない場合、同配色を再現したとは言わない。

## 3. 維持する対応と、変更してよい形

手順表、一般の導出、糸ID、4行の反復、各場所の占有、表裏・縁の対応、現在の到着位相を維持する。
製品経路は `Flat16SurfacePatternGenerator.working` と `Flat16WeaveFromWorking` である。
旧 `HiraGenjiWeaveDerivation` を製品の正本に戻さない。

描画用の区間は、糸ID・行・表裏・場所へ戻れるようにする。
`Flat16WeavePattern` の `patches`・`edgePlaces`・`weftCrossings` と次の場所を使い、
同じ色の隣接区間を同じ糸と見なしてつながない。配色を変えても形は同じであること。

- 中央の本体は片面の列1〜4。最外列0・5と、その外の縁の場所を混同しない。最外列0・5の可視束を消さない。
- 横切る糸は本体の下を通る。縁には表側・裏側の糸があり、それぞれ自分の半分にいる。**縁の1本を表から裏へ厚み一周のU字にする案は採らない。** 007Jで対応の誤りが判明している。
- 縁から中央へ入る箇所では、外で隣り合う区間と、中央の下へ潜る区間を分ける。外の腹に中央の薄い端を突き刺して「重なった」としない。見える端を切断面のまま並べない。
- 同じ糸が縁の場所から最外列へ続く箇所で、升の端というだけの理由で厚みを0にしない。007Jで両端付近に背景の穴が開いた原因である。全周と長手の反復の継ぎ目も連続させる。
- 中央の腹・肩・縁の引き込み・繊維の局所方向は変更してよい。色の段の傾きと、腹の輪郭や撚りの傾きを分ける。`faceStitchLean = 0` を理由に腹まで長方形に固定せず、逆に写真の糸筋だけを根拠に本体のはしご模様を斜めに変えない。

寸法はピッチ/紐幅 `0.3665`、素の断面の幅/厚み `3.3359`、旧形状の頂 `0.45` を開始値とする。
旧関数の係数0.45を新しい形の写真一致の証明には使わない。形状関数が変われば、最終輪郭の幅・厚みと束の見える長さを確かめ直す。
写真に近づけるための形の調整は今回の範囲で行ってよい。動かした値、根拠、写真から測れた量と描画上の選択を記録する。
一律の暗い溝や、全面を覆う平らな色板で形の隙間を隠すことは解決としない。

007I/007Jは失敗と訂正を読む資料であり、旧ブランチ全体を取り込む指示ではない。
過去の「定数を変えず、各段階で承認まで止まる」という作業手順を再開しない。
このタスクの範囲で、小さな比較を挟みながら実装と検証を進める。糸の対応に資料と矛盾が出た場合は、矛盾点を報告し、その部分の対応を推測で変更しない。

## 4. 形を先に比較し、その後に材質とカードを整える

1. **H0**: 現行mainの生成り単色と、既存の横糸着色または矢絣の配色を撮る。正面と縁が見える斜めの固定角度に絞る。
2. **H1**: 同じカメラ・照明・配色で、中央の腹と縁の潜り込みを変更した製品レンダーを出す。形の比較用に細かい糸筋を外した共通材質の対照も少数残す。中央・縁それぞれについてH0との差を確認する。
3. **最終候補**: 形が読めることを確かめてから、束に沿う糸筋、反射、接触部の陰影を調整する。候補はまず1つ、必要なら対照をもう1つに絞る。形がほぼ変わって見えないまま大量撮影や全件試験へ進まない。

八つ金剛や丸源氏の形状係数を平源氏の正解として転用しない。共通の材質定数を変更して他の紐まで変えない。
最終比較は通常表示寸法と拡大した中央・縁の切り抜きを両方示す。拡大した糸筋だけで改善を主張しない。

**カードも今回の対象。** 現行 `Flat16ThumbnailView` の輪郭付き升を残して、立体だけ別の外形にしない。
立体と同じ描画用区間・糸ID・重なりから可視部分を決め、一周の展開図として描く。
Task 029-2の上から右縁・表・左縁・裏の順、16場所を一周すべて見せる意味は保つ。
投影方法が違うので画素の一致ではなく、対応する場所で見える糸と境界の向きを照合する。

047の申し送りにあるカードの密度差は、平源氏自身のピッチと断面を基準に確認する。
丸源氏の比1.25へそろえるために平源氏を伸ばしたり、片面だけへ戻したりしない。
現行比0.54975には「表の弧＝紐幅」の近似がある（029に記録）。今回、最終形状と整合させる必要があれば
長手1反復の長さと展開に用いる断面の周長から比を計算し、基準断面と凹凸のどちらを採るか、変更前後を記録する。
変更するなら029の据え置き判断を050で更新したことを明記する。
非同期の画像生成を導入する場合だけ、045追補2の「要求中の配色だけを表示する」条件と回帰検査を引き継ぐ。

## 5. 受け入れ条件

| 観点 | 最終製品で確かめること |
| --- | --- |
| 中央の形 | 単色の通常表示でも、腹から肩へ丸く下がる束が読める。升ごとの平板、連続した横棒、全面の深い格子が主役にならない。H0と同条件で示す。 |
| 縁の形 | 斜めから見ても縁に厚みがあり、中央へ入る部分が腹の下に隠れる。外で隣り合う束を突き抜けない。実物の縁と並べる。 |
| 色の対応 | 横糸だけの着色は中央4列へ露出しない。矢絣は縁取り、本体のはしごは既存の段・表裏の対応を維持する。既存の資料配色を使う。 |
| 継ぎ目 | 縁の区間と最外列の接続、長手の反復、表裏と縁の境目に不自然な切断、背景の穴、色の飛びがない。 |
| 一覧との対応 | 同じ入力に対して、カードと立体の対応位置で同じ糸が見える。展開図の一周と最新配色を保つ。 |
| 他の描き手 | 丸源氏と八つ金剛の形・色・材質を変えない。 |

配色の検査は元の占有配列だけで終えず、**新しいメッシュまたはその可視面判定に対して行う**。
中央と縁の重なる箇所、反復の境目を含める。検査が製品と同じ式をそのまま再計算するだけにならないよう、
三角形への視線などで可視の糸IDを確認し、別の糸IDや上下逆の人工例を拒む小さな検査も置く。
全体の最大値が部分集合の最大値以上、というような自動的に成立する条件で不一致を除外しない。

メッシュの水密性自体は要求しない。実際の輪郭の外まで不透明を要求せず、紐の内側での不自然な背景露出を調べる。
既存の平源氏には三角形の向きに既知の問題がある。変更した面の表裏・法線を確認し、背面を消す通常設定で穴が出ないこと。
全体の符号反転、両面表示への切替、検査範囲の切り捨てで不具合を隠さない。

## 6. 作業と検証の範囲

主に読むファイル:

- `Kumihimo/Domain/Flat16SurfacePattern.swift`、`Flat16WeaveFromWorking.swift`、`Flat16WeavePattern.swift`
- `Kumihimo/Features/BraidSimulation/Flat16SurfaceMesh.swift`、`Flat16StitchTexture.swift`、`Flat16StitchTwist.swift`、`Flat16ThumbnailView.swift`
- `docs/tasks/007j-hira-genji-as-strands.md` の訂正と未完理由、`029-flat-thumbnail-round-the-braid.md`、`047-maru-genji-overlap-rework.md` の申し送り

新しい描画用の型は必要な分だけ追加する。保存形式、エディタの操作、一般導出の規則、組み方の名前を使う分岐の追加は対象外。
既存のレシピ限定条件を勝手に解除しない。共通化や過去の試作の整理を同時に行わない。

関連試験は平源氏のSurfacePattern・SurfaceMesh・Texture/Twist・一周カード・Orientationと、
変更した経路の配色／一般導出との一致を中心に先に回す。ファイル名 `Flat16SurfaceMeshTests.swift` のsuite名は
`HiraGenjiSurfaceMeshTests` である。既存の形状ハッシュを更新する場合、旧形状に固定していた値と保持すべき条件を区別する。
断面外形を頂点ごとに引き直すなど、018で直した重い処理を戻さない。

全件は最終版で1回。AGENTSのコマンド上限300秒、試験単位60秒、最大時間の追加指定なし、
同一UDID・並列なし・結果バンドルあり・待ち受け1つ・描画用環境変数の解除を守る。
`sh Scripts/check-braiding-is-general.sh` も実行する。上限到達・手動停止・試験単位の打ち切りは分けて報告する。

手動確認は **iPad・ライト・標準文字**で、実際のエディタから配色を変え、一覧と平源氏の詳細を確認する範囲。
作者が今回不要とした **iPhone、ダーク、大きな文字、実機、VoiceOver、回転・拡大・リセットの再確認**は引き続き対象外。
形を見るための固定角度の撮影は行うが、操作の再試験は増やさない。UIテスト一式は回さない。

## 7. 納品

mainから今回用の枝で進め、既存の未コミット作業に触れない。
結果は `.build/task050/`、指示書の末尾に実装結果を残す。画像には基準と最終のコミット、配色、角度、通常材質か形の比較材質かを記録する。

報告には、写真・H0・最終候補を見比べられる少数の画像、中央と縁の変更点、変えた寸法、
配色と可視面の検査、カードの対応、検証結果、残る差を載せる。
**数値や試験が通ったことを、外観の採用と書き換えない。** 実装後は「実装済み・外観審査待ち」で渡す。
改善が小さい場合も、そのまま比較を出し、どこが変わっていないか明記する。
今回のPR作成・push・マージは、実装を見た後の別指示とする。

---

## 実装結果（2026-09-21、Claude Code）: **実装済み・外観審査待ち**

基準 main `47f396a`。枝 `claude/task-050-hira-genji-appearance`。
**数値と試験が通ったことは外観の採用ではない。** 下の「残る差」を先に読むこと。

### 1. 何を変えたか

**升と糸束を分けた。** 升（`Flat16SurfacePatch`）はそのまま——場所・糸ID・色・行・表裏・
到着位相は1つも動かしていない。その上に**可視の糸束**（`Flat16Bundle`）を新しく載せ、
立体・一覧・検査の3つがその1つの形を読む。

| ファイル | 変更 |
| --- | --- |
| `Kumihimo/Domain/Flat16Bundle.swift` | **新規。** 束の形（`Flat16BundleShape`）と1本の可視区間（`Flat16Bundle`）。`Flat16SurfacePattern.runsStanding` が「その場所で何が見えるか」の唯一の規則 |
| `Kumihimo/Domain/Flat16SurfacePattern.swift` | 升ごとに束を作る。区画の弧の割り当て（`arcSpan`）をここへ移した（メッシュは読むだけ） |
| `Kumihimo/Features/BraidSimulation/Flat16SurfaceMesh.swift` | 升の格子＋浮き彫りをやめ、束を面として描く。床を升ごとに敷く。三角形の向きを**束自身の法線**で決める |
| `Kumihimo/Features/BraidSimulation/Flat16ThumbnailView.swift` | カードを立体と同じ規則から描く。`Flat16CardImage` / `Flat16CardLoader`（045追補2の「要求中の配色だけを表示する」を引き継いだ） |
| `Kumihimo/Features/BraidSimulation/Flat16StitchTexture.swift` | 陰影を「四辺からの距離」から「その場所の束の高さ」へ。溝が暗くなり、隠れる縁は暗くならない |
| `Kumihimo/Features/BraidSimulation/Flat16StitchTwist.swift` | 糸筋を1升でなく1区間に渡す。整数への丸めをやめた（区間ごとに別の糸なので継ぎ目が無い） |
| `Kumihimo/Features/BraidSimulation/YatsuKongoComparisonPreview.swift`、`BraidSurfaceScene.swift` | 比較用の起動引数に `hira` を足し、`--yatsu-kongo-no-detail` を平ら16本にも効かせた（描画は変えない） |
| `Scripts/task050/render.sh`、`compose.py` | 固定角度の撮影と、紐幅をそろえた比較シート |

**丸源氏・八つ金剛は1行も変えていない。** `BraidMeshHashTests` の両者のハッシュは不変。
共通の材質定数（`RoundTube16StrandTextureFactory` の各値）も動かしていない。

### 2. 中央の形

**束は自分の升より幅広く（1.694レーン）、長い（1.855段）。** 隣の升の脇に乗り、同じレーンの
前後の区間と重なる。**どの方向にも谷まで落ちない**——落としていたのが、H0の「全面の深い格子」の
正体である。

- **腹**: 盛り上がりは**持ち上げた余弦**。円弧（＝丸い糸の断面そのもの）も放物線も試したが、
  どちらも幅の中ほどがほぼ平らで、正面からの照明では**平らな升に細い溝を切った**ようにしか
  見えなかった。余弦は半分の位置で半分の高さになるので、腹から肩へ丸く下がる。
  **これは「束がどう読めるか」の描画上の選択で、糸の断面についての主張ではない。**
- **レンズ**: 升の両端で幅が 0.62 に絞られる。四つの束が**点**で分かれるので、レーンの境目が
  一直線の溝にならない。等幅で試すと面が長方形の刺し子になった（＝「升ごとの平板」）。
- **絞りすぎると別の失敗をする**: 本体の升は全レーンが同じ位置で終わるので、絞った幅が
  1レーンを割ると**紐を横断する一直線の隙間**が開き、床が見えた（1.18×0.7=0.83で実見）。
  いまは 1.694×0.62=1.050 で、いちばん細いところでも重なっている。

### 3. 縁と、中央への潜り込み

- **縁の2レーン**は超楕円の先端を回る束として描かれ、斜めから**厚みのある丸い粒の連なり**に
  見える（`.build/task050/sheets/edge-h0-h1.png`）。
- **最外列 c0・c5 の区間**は007Jが移動規則から決めた形をそのまま使う——
  **曲がり始めはその糸の横切りが始まる位置、向きは `passedColumns.first`、速さは
  1段で `passedColumns.count + 1` レーン。3つとも読んだ値で、選んでいない。**
- **曲がると同時に潜る**（`Flat16Bundle.diving`）。1レーン進む＝自分の升の終わりまでに完全に
  床下へ入る。**これは見栄えではなく必須だった**: ゆっくり沈めると、本体の四隅が分かれる点から
  横糸の色が覗き、「横糸だけの着色が中央4列へ露出しない」が破れる（実際に破れた）。

### 4. 動かした寸法と、動かしていない寸法

**動かしていない**: `stitchPitchPerBraidWidth` 0.3665、`crestHeightRatio` 0.45、
`widthToThicknessRatio` 3.3359、`faceStitchLean` 0、`edgeStitchLean` 0.24、
`longitudinalPhases`（到着位相）、手順表・一般導出・糸ID・4行の反復・占有・表裏と縁の対応。

**新しく置いた形の値**（すべて `Flat16SurfaceMesh.shape` に出所つきで載る）:

| 値 | 出所 |
| --- | --- |
| 同じレーンの前後の区間が出会う高さ 0.16 | 目視（bookA p.72の紺の本体と作者の紐の粒の分かれ方） |
| 隣のレーンの束と出会う高さ 0.36 | 目視。長手より浅い——長手は2本が入れ替わる交差、幅方向は並んで押し合うだけ |
| 升の端での幅 0.62 | 目視（レンズ。ただし上の「一直線の隙間」が下限を決める） |
| 乗り上げる端の増し 0.16 / 潜る端の減り 0.30 | 目視。どちらが上かは目視ではない（来た糸が去る糸に乗る） |
| 埋まる先端の伸び 0.25、沈み 0.10、細り 0.45 | 先端が埋まることだけ |
| 床の沈み 0.02（半厚比） | 束の縁から見えないことだけ |
| 束の幅 1.694 / 腹の張り出し 0.678 | **導出**。上の2つの高さを盛り上がりの式で逆に解いたもの |

**一覧の比を 0.54975 → 0.6113 に直した（029の据え置き判断を050で更新）。**
029は「表の弧＝紐幅」で6/16倍していたが、1レーン＝半厚なので表6レーンは 6/3.3359=1.799 半幅、
紐幅は2半幅である。**描いている輪郭の周長で測り直した**（凹凸ではなく素の断面。凹凸は升の
置き場所ではない）。

**向きの訂正（9節の指摘1）**: これで**カードの升は長手に約11.2%「長く」なる。**
同じカード高（112pt）で1反復は 61.572pt → 68.47pt。升の比（1手の長さ÷1レーンの高さ）は
2.199 → 2.445 で、**029のカードは升を幅に対して短く描いていた。** 「11%短くなった」は逆である。
コード側（`Flat16SurfaceMesh`・`Flat16SurfacePattern`・`Flat16ThumbnailView`）と
`FlatThumbnailRoundTheBraidTests` の同趣旨のコメントも直した。**計算と試験の値は最初から
この向きで整合しており、動かしていない。**

**2.05倍の訂正（9節の指摘2）**: これは**丸源氏のカードと平源氏のカードの比**（1.25 / 0.6113）で、
立体との差ではない。047の申し送りの比較がその分だけ縮んだということであり、
**別々の組み方のカードの密度を一致させる必要はないので、未解決の不具合として数えない。**

**bookA p.72で測り直した1手のピッチ**: 各レーンの自己相関で99〜104px（平均102.75）、
紐幅290px → **0.354**。出荷値0.3665（0.351と0.382の平均）に対し−3.4%で、帯の中にある。
**3つ目の資料が一致したので、値は動かしていない。**

### 5. 配色と可視面の検査

**升の配列ではなく、新しい可視面判定（`runsStanding`）に対して掛けた**
（`KumihimoTests/Flat16VisibleThreadTests.swift`）。

- **横糸だけの着色**: 本体4レーンの真ん中は全段・両面とも縦糸。横糸が入るのはレーン境界から
  1/16レーン以内、本体の最外2レーンだけで、見た点の0.5%。**束が隣の脇に乗る以上これは起きる**
  ことで、本体の内側2レーンには一切届かない。
- **矢絣（bookA p97左）**: 本体はすべて生成り、縁取りだけが4色。**はしご（p97右）**: 本体の
  4レーンが1段まるごと同色、表裏で逆。
- **縁から引き込まれた区間**: 自分の升を越えた先では1点も見えない（本体が上）。
- **継ぎ目**: 周の切れ目の両側で同じ糸。本体の各レーンは1段に1回だけ糸が替わり、境目に
  空きが無い。
- **検査が落ちること**: 横糸の区間を1.5レーン内側へずらした人工例を**拒む**。重なりの上下を
  逆に読むと別の糸が出ることも確かめる。

**カードと立体の一致**（`Flat16CardAgreesWithSolidTests.swift`）: 生成した三角形へ断面の外向き
法線に沿って視線を撃ち、いちばん外側に当たった面の糸IDを、カードの規則が名指す糸と比べる。
16レーン×16点＝256か所で**食い違い0**（上位2本の高さが0.01未満の場所は別数、今回は0件）。
カードの画素そのものも規則の標本であることを別に確かめ、4区画が上から右縁・表・左縁・裏の順に
並ぶこと（029-2）も見ている。

**この一致は最初から出たのではない。** 束の枠は2方向に傾いている（最外列と縁は007Gの位相で
升が平行四辺形になり、引き込みは1段で6レーン動く）ので、「長手から along、弧から across」と
読むと半幅ぶん外し、両者を反復させると発散した。**枠は along について区分線形なので、
across を消去すると枝ごとに1次方程式になる**——それを解いた（`Flat16Bundle.places`）。
引き込む区間は自分が通り過ぎたレーンの上へ戻るので**1点が1区間の2か所に当たることがあり**、
そのときは高いほうが見える。

### 6. 検証

- **ユニット全件（最終版で1回）**: `.build/test-results/task050-run.xcresult`、278.96秒。
  `-only-testing:KumihimoTests`、単位上限60秒（最大時間の追加指定なし）、
  同一UDID（iPhone 16 `61B4E643…`）、`-parallel-testing-enabled NO`。
  **内訳を区別して書く（9節の指摘）**: 識別子459件のうち**成功455・スキップ4・予期しない失敗0**
  （結果バンドルの要約が `passedTests 455 / failedTests 0 / skippedTests 4`）。
  スキップは `DRAW_SHEETS` で明示したときだけ描く比較シート4件（`BraidComparisonSheetTests`）。
  引数つきの実行行を含めると成功604行で、**既知問題が1件**ある——
  `BraidOrientationTests.everyTriangleFacesOutward(drawer: "flat sixteen")` が
  `withKnownIssue`（032以来）として記録される。**「459件すべて成功」ではない。**
  **この最終実行では、コマンド上限への到達・手動停止・試験単位の打ち切りはいずれも無い。**
- **AGENTSの300秒に収めるために1件の作りを直した。** メッシュが1/3大きくなり
  `everyRegionCarriesTheRidgeIncludingBothEdges` が23秒→35秒になった。費用は
  「頂点数×外形1028点」で、**選べるのは後ろの因子だけ**である（018と同じ形）。外形を角度で
  256の枡に分け、窓の端に当たったときだけ全周を歩く**答えの変わらない**索引にして9秒。
  束の長手の刻みも24/段→10/段へ落とした（区間の盛り上がりは緩い余弦。頂点数361,350は
  格子のときの366,552とほぼ同じで、面積は3倍になっている）。
- **途中の実行（9節の補足に対する申し送り）**。上の「上限到達なし」は**最終実行だけの話**である。
  作業中の全件実行は次のとおりで、ツール呼び出しの上限は300秒に設定していた。

  | 結果バンドル | 記録時間 | 予期しない失敗 | 呼び出しの扱い |
  | --- | ---: | ---: | --- |
  | `task050-all` | 322.91秒 | 7 | **300秒で呼び出しが切れ、背景で完走** |
  | `task050-all2` | 350.40秒 | 2 | 上限600秒で実行、完走 |
  | `task050-all3` | 337.48秒 | 0 | 上限600秒で実行、完走 |
  | `task050-final` | 309.50秒 | 0 | **300秒で呼び出しが切れ、背景で完走** |
  | `task050-final2` | 294.44秒 | 1 | 上限600秒で実行、完走 |
  | `task050-run` | 278.96秒 | 0 | **300秒以内で完走（最終版）** |

  **手動で止めた実行が2つある。** どちらも走っている途中に手元の原稿を変えてしまったためで、
  結果は使っていない: 平源氏だけを回した1つ（`task050-flat3`）と、`final2` の最初の試み。
  止めた側の結果バンドルは後の実行で上書きした。
  途中の失敗には旧形状のハッシュ・旧格子の条件との不一致が含まれる。
  `final2` の1件は「いちばん手前が裏向き」を10本までとした条件に対し13〜37本あったもので、
  最終版では**割合（1%以内）の条件へ緩めた**。**すべての従来条件をそのまま満たしたとは書かない。**
  この緩和と「背景へ抜ける視線0」は別の確認事項である。
- `sh Scripts/check-braiding-is-general.sh` 通る。
- **背面を消す設定**: 4方向とも**背景へ抜ける視線0**。「いちばん手前が裏向き」は5,760本中
  13〜37本（1%未満）で、引き込みが折り返す場所である。**旧の升の格子には0本あった**——
  畝は断面から真っ直ぐ立つので折り返しようがない。**折り返しは束と一緒に来たもの**である。
  今回の最初の試みでは94〜129本あり、それは折り返しではなく誤りだった（素の輪郭の外向きで
  巻いていたため潜る斜面が裏返っていた）。束自身の法線で巻き直して13〜37本になった。
- **`everyTriangleFacesOutward` は平源氏の既知問題のまま**（032以来）。最終版で 120,450 枚中
  11,030 枚が内向き。**巻き直しはこの数を目的にしていない**——この判定は面が紐の*軸*から外を
  向くかを見るもので、平たい紐では束の内側の斜面が正しく巻いていても軸からは外を向かない。
- **不透明**: 素の断面の内側では、幅・厚みどちらの視線でも背景へ抜ける線が0本。
  **輪郭の外までは要求していない**——完成した紐のシルエットは1手ごとに波打ち（`crestHeightRatio`
  はその波から測った値である）、山と山のあいだを通る視線は本当に背景を見る。
  格子のときの強い不変量（内部の辺が必ず2枚、升の縁が隣の升の頂点）は**重なる束の集まりには
  意味が無いので、弱めたのではなく置き換えた**（丸源氏と同じ扱い）。
- **手動確認（iPad Pro 11-inch・ライト・標準文字）**: エディタで16本にし、糸1を青へ変え、
  一覧の平源氏カードと丸源氏カードが両方とも新しい配色で描き直り、平源氏の3D詳細で同じ
  レーンに青が出ることを見た（`.build/task050/manual/`）。
  **作者が今回不要とした iPhone・ダーク・大きな文字・実機・VoiceOver・回転/拡大/リセットの
  再確認は行っていない。UIテスト一式も回していない。**

### 7. 画像（`.build/task050/`）

H0 = main `47f396a`（比較用の起動引数だけ当てたもの）、H1 = 本実装。
どちらも iPhone 16・ライト・同じカメラと照明、紐幅をそろえて並べてある。
`sheets/record.txt` に切り出しと倍率が1枚ずつ残っている。

| シート | 中身 |
| --- | --- |
| `sheets/face-h0-h1.png` | bookA p.72 / 作者の紐 / H0・H1の生成り単色 / H0・H1の矢絣（正面、通常材質） |
| `sheets/shape-only.png` | 同じ正面を**糸筋と陰影の地図なし**で（形だけの対照） |
| `sheets/edge-h0-h1.png` | 縁の写真 / H0・H1を60度回した単色と横糸着色 |
| `sheets/edge-on-h1.png` | 縁の写真 / H1を90度回したもの（**H0の90度は撮影が1枚流れたので入っていない。H0との比較は60度のシートで見ること**） |
| `sheets/cards-h0-h1.png` | 一覧カードのH0とH1（生成り単色・矢絣・横糸着色） |
| `*-zoom.png` | 各シートの中央1/3を3倍 |
| `h0/`・`h1/` | 元の撮影（`-card` 付きが一覧カード、`-nodetail` が形だけ） |
| `manual/` | iPad・ライトの手動確認 |

### 8. 残る差（**ここが審査の要点**）

1. **本体の粒が長方形の格子に並ぶ。写真は斜め（千鳥）である。** bookA p.72を拡大すると、
   紺の粒は長軸が紐の軸から30〜40度傾き、隣のレーンとは半粒ずれている。いまの描画は
   粒1つ1つは丸いが、並びは升の格子そのままである。
   - 束の輪郭を傾ければ写真に近づくが、**30度に足りる傾き（1段で1.27レーン）は束を隣の
   レーンへ運んでしまい、色の対応が壊れる。** 小さい傾き（0.5レーン＝12度）では写真に届かない。
   - **導出側には材料がある**: 一般導出は列1・4が手3〜4、列2・3が手5〜6で**0.286段ずれる**と
   言っており（`longitudinalPhases` の「報告するが適用しない」）、写真の千鳥と向きが合う。
   ただし050は「現在の到着位相を維持する」と指示しており、**位相は動かしていない。**
   - もう1つの候補は**横糸の通り道そのもの**である。横糸は1段で面を横断するので、その下を
   通る位置はレーンごとに1/6段ずつずれた斜めの線になる。縦糸がその上へ乗る盛り上がりを
   そこに置けば斜めが出る。ただし各面を1段に2本が**逆向きに**横切るので出るのは矢羽根で、
   「本体は平ら」というp97の読みと突き合わせる必要がある。**推測で置かず、報告に留める。**
2. **縁に接する本体の外側で横糸が0.5%覗く**（上の5節）。**重なりを作れば必ず起きる物理的
   事実ではなく、この描画の近似である。** 「中央4列の全点で露出0」という当初条件を完全に
   達成したとは書かない。
3. **「いちばん手前が裏向き」が1%未満残る**（上の6節）。穴ではないが、折り返しの描き方に
   まだ余地がある。カードと立体の一致試験256点は**各レーン中央の標本**であり、三角形を
   両面から交差判定している。**実際の背面除去を含む縁・重なり境界の全点一致までは確認していない。**
4. `crestHeightRatio` 0.45 は007Eの測定のまま。**較正式 `sigma = 9.55 × crest` は旧形状
   （升を埋める畝）での記録であり、`theCrestIsWhatTheFinishedBraidsEdgeMeasures` が通っても
   新形状の輪郭を測り直した証拠にはならない。** シルエットの波の再測定は今回やっていない
   （コードと試験のコメントもその旨へ直した）。
5. **`everyTriangleFacesOutward` は平源氏の既知問題のまま。** 最終版で 120,450 枚中
   **11,030 枚**が内向き（表 5,499・裏 5,427・帯 52+52）、辺と平行 50 枚。
   この判定は「面が紐の**軸**から外を向くか」であり、平たい紐で束の内側の斜面は
   正しく巻いていても軸からは外を向かない。**今回の巻き直しはこの数を目的にしていない。**
   実際に効く判定（背面を消すカメラ）は背景へ抜ける視線0である。

## 9. 作者の受領とCodex審査・PR申し送り（2026-09-21）

作者「外観は悪くはない。こんなものでいいと思う。他に問題がなければPRを作成させようと思う」。
**この外観を今回の到達点として受領する。確認した範囲でPR作成を止める重大な不具合は見つからなかった。**
形をさらに調整するための差し戻しは行わない。写真との一致や、任意の位置での可視面の完全な一致を保証する判定ではない。

### 確認したこと

- 審査対象は `47f396a...eb693cd`。描画用の束、立体、カード、材質、可視面の試験を読んだ。
  一般導出・手順表・元の占有と到着位相を変更していない。丸源氏・八つ金剛の形状ハッシュは旧値のまま通っている。
- 一覧のローダーは表示を配色キーで限定し、完了時に取り消しと最新要求を確認している。
  ただし平源氏の新しい試験は順次読み込みであり、045追補2の完成順逆転を直接再現する試験までは移されていない。
- 保存済み最終結果 `.build/test-results/task050-run.xcresult` を読み、**予期しない失敗0・警告0、278.96秒**を確認。
  テスト識別子は459種類。引数ごとの実行を含む記録は561件で、成功556・スキップ4・既知問題1。
  既知問題は `BraidOrientationTests.everyTriangleFacesOutward`、スキップは任意実行の比較シート4件。
  「459件すべて成功」ではなく、この内訳を区別する。Codexは全件を再実行していない。
- `sh Scripts/check-braiding-is-general.sh` は審査時にも成功。
  カードと縁の比較シート、作者のiPad画像を確認した。指定外のQAを追加していない。

### PR前に直す記述（形の再実装は不要）

1. **比0.54975 → 0.6113では、同じカード高なら長手1反復は約11.2%長くなる。**
   高さ112ptで61.572pt → 約68.47pt。報告4節の「11%短く」は逆である。
   `Flat16SurfaceMesh`、`Flat16SurfacePattern`、`FlatThumbnailRoundTheBraidTests` の同趣旨のコメントも訂正する。
   計算と試験の値はこの点で整合しており、変更しない。
2. **2.05倍は丸源氏と平源氏のカード同士の比**（1.25 / 0.6113）。
   報告8節の「一覧と立体の密度差」ではない。異なる組み方の密度を一致させる必要はなく、
   この数値だけを未解決の不具合として数えない。
3. `Flat16BundleShape.standard` 直前の「円弧／半楕円」「2値だけを目視で決めた」など、
   途中案のコメントを最終の持ち上げた余弦・レンズ・調整値に合わせる。
   幅×0.62は約1.050であり、1.11ではない。これは説明の訂正で、採用した形を動かす指示ではない。
4. 旧007Eの較正式 `sigma = 9.55 × crest` は旧形状での記録として扱う。
   `theCrestIsWhatTheFinishedBraidsEdgeMeasures` が通っても、新形状の輪郭を測り直した証拠にはならない。
   報告の「測り直し未了」とコード／試験コメントの説明を一致させる。今回の再測定は求めない。

### 実行記録の補足

保存された全件実行の経過は下表。最終版が300秒以内で通ったことと、途中の実行が複数あったことを分ける。
これは結果バンドルの開始〜終了であり、ツール呼び出し全体の時間や設定されたコマンド上限とは別である。

| 結果バンドル（`.build/test-results/`） | 記録時間 | 予期しない失敗数 |
| --- | ---: | ---: |
| `task050-all.xcresult` | 322.91秒 | 7 |
| `task050-all2.xcresult` | 350.40秒 | 2 |
| `task050-all3.xcresult` | 337.48秒 | 0 |
| `task050-final.xcresult` | 309.50秒 | 0 |
| `task050-final2.xcresult` | 294.44秒 | 1 |
| `task050-run.xcresult` | 278.96秒 | 0 |

途中の失敗には旧形状のハッシュ・旧格子の条件との不一致が含まれる。
`final2` は裏向きの最前面を10本までとした条件に対し13〜37本あり、最終版で1%以内の条件になった。
**すべての従来条件をそのまま満たしたとは書かない。** この緩和と背景へ抜ける視線0は別の確認事項である。
コマンド上限への到達や手動停止の有無は、この表だけからは確定しない。
Claude Codeは分かる実行履歴を補足し、「上限到達なし」を最終実行以外へ無条件に広げないこと。

### 今回は残す事項

- 中央の粒の並びと写真との差、新形状のシルエットの再測定。外観を受領したため、PRの前提にはしない。
- 縁に接する本体の外側で横糸が覗く報告値0.5%。今回の描画の近似として残す。
  重なりを作れば必ず起きる物理的事実とは扱わない。中央4列の全点で露出0という当初条件を完全達成したとは書かない。
- 裏向きの最前面が1%未満残ること。一致試験256点は各レーン中央の標本で、三角形を両面から交差判定している。
  実際の背面除去を含む縁・重なり境界の全点一致までは確認していない。
- 将来この描画やローダーを変更する際は、境界付近の可視面検査と、完成順を制御した画像更新試験を補う。
  今回のPR作成を止める追加作業にはしない。
- iPhone・ダーク・大きな文字・実機・VoiceOver・回転／拡大／リセットの再確認は指定どおり対象外。

### Claude CodeへのPR作成指示

上の記述訂正と作者の受領を反映し、今回の枝でTask 050のPRを作成する。
作業ツリーに残るCodexの `docs/README.md`・`docs/architecture.md` の変更も内容を確認して含める。
未追跡の `Claude outputs/`・`prototypes/` は今回のPRへ混ぜない。
PR本文に、中央・縁・カードの変更、最終検証、残す事項、対象外QAを簡潔に記す。
形状の追加調整と全件試験の再実行は、記述訂正だけなら不要。コメント以外のソースを変更した場合は、影響する検証を行う。
PR作成後にURLと差分の範囲を報告する。**マージは今回まだ依頼されていない。**

## 10. 追補: 一覧カードの速さと切れ目（作者の指示、2026-09-21）

- **遅さ**: 時間切れではなく、1枚の描画が Debug で約5.5秒かかっていた（最適化ありで約0.6秒）。
  約410万回の可視判定、判定ごとの小さな配列、取り消しの無い描画が原因。
  描く形は変えずに、配列を作らない判定・届く範囲だけを調べる絞り込み（長手と周の両方）・
  1レーン24→16画素・取り消しの伝達（古い配色の計算を途中で止める）にした。
  **Debug で約0.3秒**。絞り込みと配列の除去は、同じ解像度で出力が画素単位で一致することを確かめた。
- **切れ目**: 作者「対称に見えず気持ちが悪い」。カードの切れ目を右縁の真ん中へ1レーン動かし、
  上から矢羽根2・平面4・矢羽根4・平面4・矢羽根2と上下対称にした。
  **029-2（右縁を1本の帯として上端に置く）を更新する。** 16場所を一周1回ずつ、周の順に見せることは変わらない。
  立体は変わらない。
- 検証: `FlatThumbnailRoundTheBraidTests`・`Flat16CardAgreesWithSolidTests`・`Flat16VisibleThreadTests` が通る
  （取り消しの試験を追加）。iPad・ライトで表示を確認。全件は回していない。
