# Task 045 追補: 一覧と立体で、見えている糸と向きを揃える

- 状態: **実装指示・未着手**（2026-09-19、Codex）。
- 実装担当: Claude Code。Codexはソースを変更せず、提出された差分・画像・報告を審査する。
- 親タスク: [045-yatsu-kongo-appearance.md](045-yatsu-kongo-appearance.md)。その末尾の審査を読む。
- 審査した提出: `af78af8`。次の作業の開始コミットと既存変更を別途記録する。

## 目的と範囲

現状は、同じ八つ金剛の一覧と立体で、重なり部分に見える糸が違い、傾きも上下反転している。
**同じ紐を外側から見た展開図と立体として、色の帰属と向きを揃える。**
カードは一周を開いた平面図のままとする。立体のスクリーンショットへ置き換える指示ではない。

親タスクの制約は維持する。手順表、糸ID、占有履歴、到着位相、ピッチ0.403、S/Zの定義、
丸源氏・平源氏、共通照明とカメラ、保存形式・操作方法を変えない。物理研究を再開しない。
今回の主眼は表示の整合であり、まず現在の傾き・長さ・肩の3値を保って着手する。
新しい形の調整が必要なら理由と変更前後を示し、`.declared` と記録する。

読む順は `AGENTS.md` のとおり product → architecture → 対象仕様 → 親タスク → 本書。
関係する箇所は `RoundTube8ThumbnailView`、`RoundTube8SurfaceMesh`、`RoundTube8Bundle` とその試験。

## 1. 実際に見える糸を一致させる

現行カードの到着順で全輪郭を塗り重ねる方法は、立体の高さによる隠れ方と一致しない。
先に実際の生成メッシュでこの不一致を再現する小さな回帰試験を置く。

審査側の独立計算による再現候補（詳細・計算の限界は親タスク末尾）:

| 項目 | 条件 |
| --- | --- |
| 組み方 | S、8色識別 |
| 区間 | 同じ場所に1サイクル差で着く先着・後着の2区間 |
| 長手位置 | 先着の開始から1.10サイクル（後着の開始から0.10） |
| 周方向 | その場所の中心から−0.18列 |
| 実装の分割を再構成した半径 | 先着 約0.41103、後着 約0.38977（頂0.48） |
| 不一致 | 先着の面が外側にあるが、カードは後着の色を上塗りする |

先着の開始から1.30サイクル・周方向−0.42列でも同じ不一致を得た。
例えばSの列0は、現行の到着位相では先着の開始が−0.75、後着が0.25サイクルとなる。
端の切り取りを避け、中央のリピートへ移して確かめる。報告中の近似値をそのまま期待値に固定せず、実メッシュで糸IDと前後関係を判定する。

実装方法は次の性質を満たす小さなものを選ぶ:

- 各表面位置で外側に見える区間を決める規則を、カードと立体で揃える。
  立体の可視面を展開図へ反映する方法、または両者に共通の可視領域を与える方法などが考えられる。
  到着順を維持するため形を変えるなら、その順序が重なる領域全体で成り立つことと、形の退行がないことを示す。
- 色ではなく糸ID・区間で判定する。同色の糸が隣接する場合でも処理を変えない。
- 新しい検査は、セルの色メタデータや畝の頂だけの比較で済ませない。肩の前後と両脇を含め、S/Z、長手周期と周方向の継ぎ目を対象にする。
- 立体の透視画像と展開図の画素を直接一致させるのではなく、同じ長手位置・周方向へ対応づけて、見える糸の所属を検証する。
  実際の三角形の表面とカードが使う可視領域を比べ、境界上の丸め誤差は限定して扱う。
- 外側の面を途中で覆う別の区間も含めて判定する。比較する2区間だけを見て可視と断定しない。

「後着がどこでも上」とする現行コメント・報告を、採用した実際の規則に合わせる。
幾何の交差線を描画上の近似として残すことと、実物の組みの先後を確定したことを混同しない。

## 2. カードの向きを外側から見た立体に揃える

現行の共通レイアウトは周方向が画面の下へ増える。立体の正面とは上下が反転するため、8本側の座標変換で揃える。
共通レイアウトの大きさ・リピート配置は利用してよいが、16本の見えを変えない。
S/Zの名称やレシピを交換する、手順表を変える、詳細のカメラを反転する方法で解消しない。

検証画像では、長手の正方向と周方向の増える向き、立体正面に対応する展開図の領域を注記する。
同じ側の端を揃えて、S/Zそれぞれの糸束の傾きと8色の並びを確かめる。
これはアプリ内の座標の対応を確認するための注記であり、写真のどちらの端が組み点かを確定するものではない。
注記や比較用の座標は本番UIに出さない。

## 3. 比較資料と、形に残る差

現在のCを変更前D0、修正後をD1として、`.build/task045/review-fixes/` に保存する。
既存のA/B/Cを上書きしない。親タスクの撮影条件を保ち、コミット・配色・回転・倍率を記録する。

- S/Zの生成り単色・本の配色・8色識別について、実際の一覧と詳細を残す。
- 不一致を直した場所は拡大画像に印を付け、どの糸が見えるようになったかを示す。比較用の注記だけで画像の形や色を修整しない。
- 立体の正面・30°・60°・90°・180°・270°を確認し、継ぎ目・穴・ちらつきや形の退行を調べる。
  立体が不変なら、その根拠を示し、同じ状態の画像を不必要に作り直さなくてよい。
- 実物写真との同じ幅での並置も残す。隙間に見える平らな色面、糸束の細長さ、隣の束への差し込み、輪郭の起伏が改善・不変・悪化のどれかを記す。

今回は隙間を暗く塗る調整や絹の艶の追加で、残る形の差を解消した扱いにしない。
一致の修正に伴って露出する下地が変わる場合は、それも形の差として報告する。
下地の平面や糸束の細長さを改めて直す作業は、整合したD1を見て次の範囲を決める。
未計測の「写真では1.5〜2サイクル」という見積もりだけでピッチや升を変更しない。

## 4. 検証と提出

親タスクと現在の `AGENTS.md` の実行条件を守り、関係する試験を先に、全ユニットを最後に1回だけ実行する。
コマンド上限300000ms、試験ごとの既定60秒、並列実行なし、起動済みの同じUDID、結果バンドル保存、UIテスト一式は除外する。
打ち切り・失敗・スキップ・既存のexpected failureを区別し、名前の番人と16本の不変性も確認する。
カードの計算量が増えた場合は、一覧を表示・スクロールしたときの応答と、生成・描画コストを修正前後で確認する。

手動では実際の一覧→詳細、回転・拡大・リセットを確かめる。
iPadとiPhone、ライト／ダーク、標準／大きな文字の範囲を記録する。前回未確認だったiPhoneのダーク・大きな文字も確認する。
実機、VoiceOver、横向き等を確認していなければ、未確認のまま明記する。

提出には以下を含める:

1. 開始・提出コミット、主要変更箇所、維持した値と変更理由。
2. 不一致の実メッシュによる再現、修正後に同じ場所で見える糸ID、回帰試験の結果。
3. カードと立体の向きの対応を示す画像と、S/Z・配色・回転の比較。
4. テスト結果と結果バンドル、所要時間、打ち切りの有無、手動確認と未確認範囲、性能への影響。
5. 写真との差として残る形の問題。Task 035は引き続き未完了とする。

完了報告は親タスクの過去の記録を消さず、本書に追記する。
状態は**「修正実装済み・再審査待ち」**とする。表示の整合が取れたことだけで、実物らしさまで合格とはしない。
