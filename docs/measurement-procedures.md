# 実物の写真とレンダーを突き合わせる測り方

組紐の見た目を実物へ寄せる作業では、同じ量を何度も測り直す。手順が揺れると、前のタスクの
数値と今のタスクの数値が比べられなくなる。**ここを測り方の正本とする。**

各手順の出どころと生データは `.build/task007e-screenshots/`（平源氏）と
`.build/task005j-screenshots/`（丸源氏）にある。**`.build/` は git の管理外なので、
判断に効く事実はこちらへ写すこと。**

## 共通の原則

**較正式は組ごとに取り直すこと。引き写さないこと。** 断面も1手のピッチも組ごとに違う。
平源氏の畝の較正は直線 σ(%) = 9.55 × 頂 だったが、丸源氏は直線ですらなかった
（σ(%) = 17.31 × 頂^0.752）。引き写していれば大きく外していた。

**資料の数値へ ±15% で合わせにいかないこと。** 資料の値には糸の繊維のざらつき、カメラの
周辺減光、辺の取り方の振れが混じっている。**下限と目視で判断する。**

**手順が要求する条件は、フィクスチャを替えるたびに満たされているか確かめること。**
平源氏では主値の fixture B がたまたま単色だったため、「手元のフィクスチャで測る」ことと
「単色の行で測る」ことが一致していた。丸源氏へ数字を1つ並べたとき、この一致が偶然だった
ことに気づかず、2色フィクスチャの値を参考値として1年分の判断の隣に置いてしまった。

---

## 1. 長手方向の輝度変調

紐の谷の陰がどれだけ深いかを表す。

### 手順

1. スクリーンショットは**原寸**で測る（比較画像は縮小がかかって値が下がる）。資料写真は
   元の解像度で、傾きだけ補正する。
2. **単色の行**を選ぶ。行の中で色が替わると、形と無関係に値が上がる。
   - レンダー: 全糸を1色にしたフィクスチャを使う
     （`--ui-testing-hira-surface-plain` / `--ui-testing-maru-surface-plain`）。
   - 資料: 長手方向に1色で続く帯を目視で選び、その帯の内側だけを使う。
3. 各行について、紐の直線部の画素を取り、**RGB の単純平均**を輝度とする（重み付けなし）。
4. **平滑化はしない。**
5. その1次元配列の p95 と p5 から `(p95 − p5) / p95` を求める。
6. **行ごとの値の中央値**を代表値とする（平均ではない）。min と max も併記する。

### この指標が掛けられない帯

p95 で割るので、**p95 が小さい帯では発散する。** 黒や紺のように極端に暗い帯は分母が
小さいぶんだけ値が跳ね、明るい帯と並べて比べられない。単色で続くのがそういう帯しかない
資料は、この手順では測れない。除外して理由を書くこと。

Task 005J でこれにより bookA p94（丸源氏の完成写真）を外した。長手方向に1色で続くのは
黒の縁取りだけで、山形の側は白・橙・桃が交互に来るため単色の帯が取れない。

### これまでの値

  平源氏  baseColor のみ 0.463 → 両スロット 0.618（Task 007E 段階4a）
  丸源氏  baseColor のみ 0.489 → 両スロット 0.617（Task 005J 段階1）
  資料 平源氏  bookA p96 0.670 / bookB p23 #23 0.884
  資料 丸源氏  作者の真上から 0.738 / bookB p23 #24b 0.772・0.814

---

## 2. シルエットのさざ波から畝の高さを読む

**この手順は平たい紐にしか使えない。** 適用範囲を先に読むこと。

### 手順

1. 写真・レンダーの傾きを補正する（残差の高域抽出は緩やかな傾きを自動で落とすので、
   1度以下なら省いてよい）。
2. 列ごとに紐の上辺を求める。**輝度が「背景と紐の中間値」を下向きに横切る点**を線形補間で
   亜画素まで求める。
3. 1手のピッチ2つぶんの移動平均を引いて高域を取り出す。
4. その残差の σ と p95 − p5 を、1手のピッチに対する比で表す。
5. **残差の自己相関でさざ波の周期を求め、紐の構造の周期と一致することを必ず確認する。**

### 周期の確認は形式ではない。これが唯一の歯止めである

一致しなければ、測っているのは**糸のけばと背景のざらつき**であって形ではない。
これを飛ばすと資料から誤った値を読む。**2度助かっている。**

- Task 007E 段階5: bookB p23 の白い縁糸は σ 12.59%、周期は出るが r=0.16。頂 1.32 に
  相当する — 糸が自分の直径の1.3倍盛り上がることはない。周期の確認で失格にした。
- Task 005J 段階2: 丸源氏の資料4枚はいずれも周期を持たない（15px 以上のラグでの自己相関
  0.12〜0.41、レンダーは 0.98）。**にもかかわらず、その σ を較正へ通すと頂 0.045〜0.193 が
  返り、現在の値 0.12 はそのまん中に落ちる。** 周期を確認しなければ、4枚とも「0.12 で
  合っている」と答えていた。

短いラグ（15px 未満）の相関は隣の画素どうしの滑らかさであって構造ではない。**構造として
ありうるラグの範囲で見ること。**

### 較正

現在の値を含む3つ以上の頂でレンダーし、σ と頂の関係を実測して引き直す。**頂 0 でも
レンダーして、σ がほぼ 0 になること（測定の床が無いこと）を確かめる。** 床があるなら、
式の切片が実体なのか測定の癖なのか区別できない。

  平源氏  σ(%) = 9.55 × 頂        直線。頂 0.12 / 0.24 / 0.48 で確認。
  丸源氏  σ(%) = 17.31 × 頂^0.752  直線ではない。頂 0.00〜0.48 の8点、残差 0.044、
                                   頂 0 で σ = 0.02%。

### 適用範囲: 丸い紐には使えない（Task 005J 段階2、2026-09-06）

丸源氏で試して、**手順そのものが丸い紐に向かない**ことが分かった。良い写真が無かったので
はない。根拠が3つある。

1. **実物の外周が滑らかで、畝の列に見えない。** 拡大しても縁は1本の滑らかな紐である。
   丸い紐には糸が折り返す縁が無く、糸は斜めにシルエットを横切るだけなので、**滑らかなのが
   正しい可能性が高い。**
2. **シルエットの特徴が目と1対1に対応しない。** レンダーの端の波形の周期は1手ではなく
   **1手の 4/9**（141px に対し 62px）。平源氏では1手そのものだった。何を数えているのかが
   組ごとに変わる。
3. **較正が飽和する。** 丸い紐のシルエットは接線の包絡なので、畝が高くなるほど隣の畝の谷が
   隠れる。頂を2倍にしても σ は2倍にならない。逆に読むとき、上の方ほど分解能が落ちる。

**平源氏のシルエット法は、縁で糸が折り返す平たい紐だから成立した道具である。**
Task 008 以降で丸い組み方（八つ金剛、江戸八つ、八つ瀬、唐八つ、江戸源氏、方丈）の畝を
決めるときは、**この手順を最初から選択肢に入れないこと。** 別の観測量を先に探すこと。

---

## 2.5 レンダーのスクリーンショットから幾何量を読むときの遠近法の補正

**レンダーのスクリーンショットで「紐幅あたりの何か」を数えるときは、模様が手前の面にあって
カメラに近いぶん拡大されている。** 紐幅は最も広いところ、すなわち軸と同じ奥行きで測るのに
対し、模様は手前の面で測るためである。

  倍率 = カメラ距離 ÷ (カメラ距離 − 外半径)

丸源氏では 4.4 ÷ (4.4 − 0.52) = 1.135、つまり**見かけの密度が 14% 低く出る。**

**補正せずに写真と比べると模様が過密に見え、縦横比を下げて合わせたくなる。それは Task 005H
の失敗の再演である。**

Task 005J 段階2b は当初この補正を欠き、1.87 / 2.06 / 2.21 と報告した。補正すると
2.12 / 2.37 / 2.58 で、**幾何から直接出した 2.14 / 2.37 / 2.59 と 0.6% で一致する**
（どちらも紐幅 = 畝の稜線での外径）。**スクリーンショットから幾何量を読んだら、必ず幾何と
突き合わせて裏を取ること。**

### 「紐幅あたり」の数字には、何を紐幅としたかを必ず添えること

これを書かなかったことが Task 005J で文書間に3通りの値を生んだ原因である。丸源氏では
候補が3つあり、同じ頂 0.12 で 1.96（公称・平均面）／**2.14（畝の稜線での外径）**／
2.17（メッシュの最大半径）と割れる。

**写真と比べるなら畝の稜線での外径を使う。写真が測っているのは紐の外形の幅だからである。**
レンダーの輪郭は稜線の予測に対して −1.2〜−1.6% で一定して追随する（差は亜画素の辺の
取り方）。メッシュの最大半径は交差で糸が乗り上げる局所の瘤を含むので −3.3〜−5.4% とずれ、
しかも**ずれが頂とともに広がるため、一定の係数では補正できない。**

平たい紐を正面から見る場合は、面も縁も同じ奥行きにあるのでこの差は出ない。**丸い紐だけの
問題である。**

---

## 3. 正規化した並置による目視の計数（Task 005I）

山形の密度のように「1紐幅あたりいくつ」で表せる量は、**紐幅を揃えて並べて数える**のが
いちばん確かである。

1. レンダーと資料を、**紐幅が同じ画素数になるよう**それぞれ拡大縮小する。
2. **同じ紐幅ぶんの長さ**を切り出す（Task 005I は 4.5 紐幅ぶん）。
3. 数える。

**色の周期と形の周期を取り違えないこと。** 自己相関だけに頼ると、配色が2手ごと・3手ごとに
繰り返す資料では、形の周期ではなく配色の周期を拾う。Task 005J 段階2 で bookA p94 の山形を
自己相関で読むと 140px（配色の周期）に見えるが、拡大して数えると 35px（形の周期）だった。
**必ず拡大した画像で数えて裏を取ること。**

平源氏で単色フィクスチャを足したのも同じ理由である。縁の色が目ごとに変わると、形と無関係に
シルエットの測定点が動き、周期が1手ではなく配色の2手周期になる。

---

## 4. 比較画像の作り方

段階ごとに、判断の根拠を1枚で見られるようにする。

- 紐幅を揃え、同じ紐幅ぶんの長さを並べる。
- 各段に**測った値を併記**する。
- レンダーの前後と、資料を**同じ画像の中**に置く。別々の画像だと目視で比べられない。
- 資料を外した場合は、外した理由を画像の中に書く。

---

## 5. 構成した紐の読み方（Task 024、2026-09-08）

**構成した紐を正本と突き合わせるときは、絵に描いてから読む。** 玉の座標を直接見に行くと、
「どちらが手前か」を自分で推論することになり、その推論が正本との食い違いに化ける。
**平行投影で描き、z バッファが手前に選んだ色を読むこと。** 1本1色、陰影なし。
実装は `Scripts/task024/render.py`。

**読み方の誤りは幾何の誤りに見える。** Task 024 では4回食い違い、4回とも原因は読み方だった
（`tasks/020-general-braid-simulator.md`「これまでの誤り」19〜22）。**絵と正本が合わないとき、
まず読み方を疑うこと。**

### 手順

1. **本体は幅 1〜4 である**（平源氏）。幅 0・幅 5・縁の折り返しは**縁**であり、縁は色が
   付いていてよい（`architecture.md`「平源氏の幅の読みが変わりうる」）。
   **本体の定義は占有履歴に合わせること。絵から決めないこと。**
2. **列はその絵自身の u 軸へ射影して読む。** 表と裏では u が +x と −x を向くので、
   同じ world x を同じ列として読むと片方は紐の外側を読む（`render.box_for`）。
3. **視線は着地する場所の角度に取る**（管の紐）。占有履歴は16の場所を閉じの対で8列に畳み、
   場所は 22.5° ごとに立つ。45° 刻みの視線は**対の継ぎ目**を向き、継ぎ目は2本の糸のものだから
   **同じ糸が1行に2度出る**。対の2場所のうち**手が着地するほう**を取る
   （`occupancy.closing_columns`、`render.maru_columns`）。
4. **列ごとに、その列自身の留まりの高さで読む。** 場所の留まりが替わるのは手が着いたときで、
   1サイクルの手は同時に着かない。**横一線はサイクルの別々の時点を並べたものであって、
   1行ではない**（`render.maru_rows`）。
5. **読み始めは、すべての列で留まりの間隔が k になる最初の留まりである。** はじめの数個は
   種を取り込んでいる途中で、間隔が k になっていない。**この数は高さから読み取るもので、
   合わせ込むものではない。**

**どこを見るかだけが構成から来る。何が見えるかは絵が決める。**

### 一致とみなす自由度

**筒には原点も向きも無い**ので、列の回転・鏡像・上下の向き・行のずらしは自由である
（丸源氏で 8 × 2 × 2 × 4 = 128 通り）。**それ以外は自由ではない。** 糸の番号の付け替えも、
列ごとのずらしも、してはならない。**E/W の列順は未決のままである。**

## 6. 筒の紐の1サイクルの伸びを写真から読む（Task 031、2026-09-10）

**2026-09-20、Task 048差し戻しに伴う注意:** 下記の画像から読んだ「色の周期／紐幅」と、
それを「何サイクルか」で割る解釈を区別する。0.403はSの1.614を4で割った値で、
見える束1個の長さを直接測ったものではない。台の場所と完成した紐の位置の対応を見直す際、
固定した台の場所の色周期を、完成表面の固定角度の色周期へ無条件に適用しない。
同じ写真の色周期を保ち、描画上の周期を確かめてから長手尺度へ換算する。
旧測定値は履歴として残す。具体案と条件付きの換算は [048審査・再指示](tasks/048-review-and-spiral-rework.md) 4節。

**丸い紐の「1サイクルでどれだけ伸びるか」を、実物の写真から測る。** 山の高さ（測り方2）が
丸い紐に使えないと分かったあと、丸い紐について写真から読める寸法はこれが最初である。

**測るのは拡大した写真だけである。** 見開き（紐1本が 60〜78 px）では**同じ手順で縞の向きの
符号が逆に出た**。拡大（紐1本が 230〜260 px）を使うこと。

### 手順

1. **紐を背景から切る。** ぼかしてから、「色がある」（彩度の絶対量が大きい）**または**
   「明るい」（白い糸には色が無い）で拾う。
   **彩度の比 (max−min)/max を使ってはならない**——暗い影が高い値を返し、紐の脇の影を
   紐として拾う。
2. **本体の行を選ぶ。** 行ごとの幅を測り、**中央値の ±12% に収まる最長の連続**を本体とする。
   房・留め・ページの外がこれで落ちる。**紐の幅は、その範囲の幅の中央値。**
   これは**輪郭の幅**であり、山のぶんだけ名目の直径より太い（平源氏の 1.62 対 1.44 と同じ
   事情。`unsettled` に書くこと）。
3. **中心の帯だけを取る**（幅の ±22%）。**遠近補正である**（測り方2.5）——縁では周方向が
   縮んで見える。行ごとに紐の中心を追い、帯をまっすぐに直す。
4. **先に縞の傾きを求め、帯をその傾きで剪断する。** 帯の2次元自己相関の、原点から最も近い
   **極大**（原点そのものではない。相関は原点から単調に落ちるので「最大値」を探すと
   原点の隣が出る）が、面が自分自身へ戻る最短の変位で、縞に沿っている。
   **剪断せずに読むと周期が2割ふらつく。**
5. **色の縦周期**は、剪断した帯の色の信号（橙と生成りを分けるのは**明るさではなく色相**、
   たとえば (R−B)/(R+B)）から、長手方向の傾きを移動平均で取り、
   **自己相関の最も高い峰**を取る。峰の頂は3点の放物線で 1/5 px まで読む。
6. **サイクル数への読み替えは配色から導く。** 位置ごとの色の並びの周期 c と、手順表が
   1サイクルに送るコマ数 s から、**1つの場所が自分の色へ戻るのは c/gcd(c, s) サイクル**。
   八つ金剛の配色 a・S は c=4、s=3 なので **4サイクル**。配色 b は c=8 なので 8サイクルで、
   しかも白と橙が半周期で入れ替わるためどちらの読みでも澄んだ峰にならない。
   **読み替えられない配色は使わないこと。**

**角度から伸びを出す手は採らない。** 角度は締め具合で変わる。自己相関のほうを採る。

### これまでの値（Task 031、`Scripts/task031/measure_photographs.py`）

bookA p.8–9 の拡大（3584×2058）。角は紐を横切る向きからの符号つき角度。

| 紐 | 本体の行 | 幅 | 縞の向き | 色の縦周期 | 周期÷幅 | 1サイクル÷幅 |
| --- | --- | --- | --- | --- | --- | --- |
| Z-b | 454–1909 | 229 px | −56.7° | 251.3 px | 1.098 | （読み替え不可） |
| Z-a | 217–1902 | 241 px | −61.2° | 487.6 px | 2.023 | **0.506** |
| S | 918–1908 | 235 px | +54.0° | 379.3 px | 1.614 | **0.403** |

**縞の向きには2つ別のものがある。混同しないこと。**

- 上の表の「縞の向き」は、**面が自分自身へ戻る最短の変位**（2次元自己相関の、原点に最も近い
  極大）である。これは**升の格子**の向きであって、色の帯の向きとは限らない
- **色の帯の向き**を測るには、**先に升の肌理をぼかして消してから**、帯が水平になる剪断を探す
  （測り方6の手順5と同じ剪断を、角度のほうを未知として解く）。**ぼかしの強さを変えて読みが
  動くなら、それは升を読んでいる**——色を読めていれば動かない

### 色の帯の向き（ぼかしの強さ別、`measure_photographs.py`）

| 紐 | ぼかし1 | 31 | 61 | 91 | 読み |
| --- | --- | --- | --- | --- | --- |
| Z-b | +63.5° | +61.0° | +54.0° | +37.0° | **読めない**（動きすぎる） |
| Z-a | −54.0° | −53.0° | −51.0° | −49.0° | **約 −51°** |
| S | +55.0° | +55.0° | +54.5° | +53.5° | **約 +54.5°**（一致度 0.94） |

**S と Z-a は符号が逆で、大きさもほぼ揃う。** これが鏡であることの、写真からの裏付けである。

### 描いたものは、写真と同じ手順で測ってから比べる（Task 032、2026-09-11）

**写真から測った数を、描いたものの「手で出した値」と比べないこと。**Task 032 段階1 は写真の最短の
歩幅 +36° を、描いたものの通路の幾何から出した 0° と比べて「36° 違う」と書いた。**同じ手順で描いた
面を測ると、升を1つも傾けていないのに 27° を読んだ**（色の格子が最短の歩幅を斜めにする）。比べ方が
揃っていなかった。

- 描いた面は `BraidComparisonSheetTests.theYatsuKongoFacesForMeasuring`（`DRAW_SHEETS` のときだけ）で、
  **写真と同じ紐幅の画素数で、正面から、立てて**描く。測るのは `measure_units.py --drawing` と、
  `measure_photographs.colour_angle` を**手を加えずに**掛けたもの
- **符号も比べること。**傾きの向きは画面の中の回転では変わらない（紐を上下逆さに撮っても同じ）。
  鏡でだけ変わる。**大きさだけ比べると、左右の取り違えを見逃す**——Task 031 の検算は大きさしか見て
  おらず、描いた S が写真の Z の向きに傾いていることを Task 032 まで見逃した
- **描いた面が鏡像でないことを先に確かめる。**`BraidPicture.frame` は `u × v = n`（右 × 下 ＝ 視線）で、
  ふつうのカメラと同じ向きである

### 角の符号の取り決め（**この文書で一度だけ書く**、2026-09-11）

**写真と描いた面の角は、紐を立てて置いたときに右下がり（＼）を正とする。**紐の上下を入れ替えても符号は
変わらない（画面の中で回しても向きは変わらない）。**鏡でだけ変わる。**

- `measure_units.py` の最短の歩幅（下へ進むと右へずれるのが正）と、`measure_photographs.colour_angle` の
  色の帯（横切る向きから測る）は、どちらもこの取り決めである
- 写真の S は歩幅 +36.0°、色の帯 +54.5°。Z はどちらも負
- アプリの画面のように紐を寝かせて見たものも、**立てたときの符号に直して書く。**Task 032 段階3 で
  「寝かせて右下がり」と書いた撚りの筋は、この取り決めでは負である

### 描いた面は、紐の軸の周りの全ての向きで測る（Task 032、2026-09-11）

測る中央の帯（紐幅の 0.44）はほぼ1列ぶんしか無く、描いた列はまっすぐなので、**正面に来る2つの場所しか
見ていない。**描いたものの読みは紐をどちらへ向けたかで大きく変わる（八つ金剛 S の色の帯で +17.5〜+63.0°）。
写真は向きの分からない1枚なので、**描いたものは軸の周りに半列ずつ回した全ての向きで測り、写真の値が
その範囲に入るかを見る。**1つの向きで測った値どうしを比べないこと
（`BraidComparisonSheetTests.theYatsuKongoFacesForMeasuring` が 16 通りを描く）。

**8列の紐は中心帯が1列しかなく、この読み方は向きで量子化される。次は中心帯ではなく、円筒の縮みを戻して
全幅で測る。**
**Z-b はここでも読めない**——配色 b は3色で位置ごとの周期が8であり、色の帯が2色の帯になって
いない。

- **S と Z-a は一致しない**（0.403 対 0.506）。**Z-a は3本のうち最も太く**（241 px）、
  ゆるく組まれた紐は太くなり伸びも長くなるので、向きとしては辻褄が合う。
  **どちらかが誤りとは言えていない。** 出荷したのは S の値である（信号が最も澄んでおり、
  描いている紐がそれである）

### 色の周期が無い配色では、組み目の周期を読む（Task 054、2026-09-22）

**丸四つ組（bookA p.10）では手順5の色の周期が存在しない。** 配色 b は台のまわりで白・藤・白・藤（c = 2）、
手順表は1サイクルに2場所送る（s = 2）ので、1つの場所は**いつまでも同じ色**である（c/gcd(c, s) = 1）。
色は紐に沿ってまっすぐ走る。a も同じ並びの淡い2色、c は1色。

- **代わりに読むのは面そのものの周期**（中心帯の明るさ、列ごとの水準を除く）。この紐はどの場所も
  毎サイクル新しい糸を受け取るので、正面の列に沿った束1つが1サイクルである。**最初の極大**を取る
  （a は2サイクル目の峰のほうが高い）。
- **頁は見開きの倍率**（紐1本が約 59 px。手順6が求める拡大の 230〜260 px に届かない）。Task 031 の
  切り出し（`braidness`、ぼかし 9 px）はこの大きさの紐を拾えない（幅 30〜37 px と出る）ので、
  **明るさの行方向のばらつき（60行の塊で 20 を超える列）で紐を切った**。縞の向きは読まない。
- スクリプトは `Scripts/task054/measure_photograph.py`。

| 紐 | 幅 | 周期 | 1サイクル÷幅 |
| --- | --- | --- | --- |
| a | 60.0 px | 46.0 px | 0.767 |
| **b** | **59.0 px** | **47.3 px** | **0.801**（出荷した値） |
| c | 51.0 px（p10–p90 が 46–78 で、幅が澄んで読めない） | 50.2 px | 0.985 |

**描いたものを同じやり方で測ると 0.97 である**（iPad の画面、`.build/task054/sheets/v2.png`。周期 183 px、
輪郭の幅 189 px。束を盛り上げた後の `v4.png` では 0.99）。描いた紐の輪郭は山の直径の約 0.83 しかなく、**「紐の直径」を山の直径とした描き手の
0.80 は、輪郭で測ると長く出る。**差は残してある（寄せていない）。八つ金剛の 0.807 も同じ読み方で入って
いるので、同じ差がそちらにもあるかもしれない（測っていない）。

### 江戸八つ組（bookA p.4 の 3、Task 009、2026-09-22）

**同じ2つの読みを、手を加えずに掛けた**（`Scripts/task009/measure_colour_angle.py`）。この紐も c = 2（2色交互）、
場所ごとの運びは ±2 で偶奇が保たれるので、**1つの場所はいつまでも同じ色**であり、色の周期は無い。

- **色の帯の向き（`colour_angle`）は、この頁では読めない。** 紐1本が約 50 px（見開きの倍率）で、中心帯が 20 px。
  ぼかし 31 以上が帯より広く、原寸では −2.5° に縮退する。4倍に拡大すると +33.5°（31）→ +21.5°（61）→ −0.5°（91）と
  ぼかしで動く（升を読んでいる）。**拡大の写真が無い限り、帯の向きを写真と比べないこと。**
- 描いた面は 16 向きで、帯の読みは探索の上限（±75°、色が紐に沿って走る）か、列の境目のジグザグ（約 −43°）のどちらか。
- 1周期÷幅（Task 054 の読み）: 写真 **0.649**（幅の p10–p90 が 41–91 px と澄まない）、描いた面 **0.860**。

### 粒を数えて1周期を読む（江戸八つ組、教科書 p.64、Task 059、2026-09-24）

**写真の格子が描いた格子と違うとき、周期は1周期にならない。粒の数を数える。**

教科書 p.64 の頁頭の写真（600 dpi、紐の輪郭の幅 94 px）では、**紐の長さの向きに黄緑の隣が桃**（対の反対側の糸、47 px）で、
同じ色は 95 px ごとに戻り、同じ場所の糸の並び（黄緑と生成り）は斜めに走る。描いた格子（占有履歴、Task 053）では列はまっすぐで、
長さの向きの隣は同じ列の次の糸である。**だから長さの向きの周期は「1つの場所の1周期」ではない**:

- Task 054 の読み（中央の帯の明るさの最初の峰）は 95 px（幅の 1.01）を読む。**違う場所の粒2つぶんである。**
- 審査側の読み（色の周期を「1つの場所の色は2周期で戻る」で半分にする）は、桃の周期 95.6 px から 0.51。

**粒の数えかた**（`Scripts/task059/measure_grains.py`）:

1. 紐をまっすぐに寝かせ（列ごとに輪郭の中心を揃える）、**円筒の縮みを戻す**: 正面から弧長 *s* の行は写真の `R sin(s/R)` の行
   （*R* は輪郭の幅の半分。0.95 R まで）。
2. 4色を k-means（6群、2群は溝と影）で分け、**色ごとのつながった塊を1粒とする**（3×3 の開きのあと 60 px 以上）。
   この配色では同じ色の粒は隣り合わない。
3. 正面から弧長 ±*b* の帯に中心がある粒を数え、**1粒の面積 = 帯の面積 ÷ 粒の数**。
4. **描き手の前提は「1周期に1本の糸が1粒」**（占有履歴）なので、1周期は周に8粒: `8 A / (2πR)`。格子がどちらへ走っていても数は変わらない。

| 帯 ±*b*（px） | 15 | 20 | 25 | 30 | 35 |
| --- | --- | --- | --- | --- | --- |
| 粒の数 | 64 | 77 | 97 | 123 | 155 |
| 1粒 ÷ 幅² | 0.143 | 0.157 | 0.155 | 0.146 | 0.135 |
| 1周期 ÷ 幅 | 0.363 | 0.399 | 0.394 | 0.372 | 0.343 |

**中央値 0.372 → 0.37**（当時の `pitchOverDiameterTurningBothWays`、`.observed`、幅は 0.343–0.399。**Task 059 追補6で置き換えた**:
1周期は「隣り合う目の色の組と、周の行の間隔を読む」の同じ行の隣 **0.50 D**。0.37・0.74 は使わない）。**両方向の8本の筒の族の値**で、
八つ金剛の 0.807 は動かさない。4色の相関に格子を当てはめる読みも試したが、窓で 0.17〜0.45 と振れたので使わない。

**Task 059 追補2 で、数えた 0.37 は「目1つ」の長さと読み直した。** 完成した紐が回る両方向の族では、1本の糸は1周期に目2つで見える
（升が半周期ごとに周1列ずれる）ので、1周期は目2つ＝**0.74**（上の手順4の「周に8粒」は「周に16粒」になる）。**測った数は同じ**で、
割り方だけが変わった。0.74 はレシピ本 p.4 の拡大の 0.72（下）と揃う。写真の長さの向きの色の周期（95 px、1.01）とはまだ合わない。
**→ 追補6で置き換えた**（上）。レシピ本 p.4 の 0.72 は別の紐（糸も太さも違う）の値なので、この紐の値には使わない。

**レシピ本 p.4 の拡大（同じ紐、紐の幅 189 px）**に Task 054 の読みを当てると **0.719**（136 px）。**「2色の列がまっすぐで、回す前の描いた
格子そのもの」とここに書いていたのは誤読だった**（Task 059 追補4、審査側の訂正）: 長さの向きには、どの高さでも白とサーモンが交互に並ぶ。
136 px は長さの向きの色の周期として読んだ値で、目ごとに2色が交互なら1周期にあたる（測り直してはいない）。**教科書の紐の粒は、粒の面積でレシピ本のちょうど半分**（0.14 と 0.28 幅²）。
作者が比べたのは教科書なので、教科書の値を採った。

**描いた面**（`theEdoYatsuFacesForMeasuring`、`DRAW_SHEETS` のときだけ、16向き。Task 059 から束の2リピートと同じ長さで描く）も
同じスクリプトに渡せる。**描いた面では列がまっすぐなので、色の周期 ÷ 2 が描いた1周期を読む**（束 0.840–0.859、粒 0.376–0.386。
紐の直径で入れた 0.807・0.37 を、それより 5% ほど細い輪郭で割ったもの）。**粒の数は描いた面では読まない**——粒の中心が数本の列に
並ぶので、帯の数が列ごとに跳ぶ（粒で 0.26–0.56）。束は重なって塊が割れるので、なおさら数えられない。

### 目の傾きを読む（江戸八つ組、教科書 p.64、Task 059 追補3、2026-09-25）

**目1つずつの長い軸の向きを、形の2次モーメントで読む**（`Scripts/task059/measure_tilt.py`）。**Task 059 追補6: この値（±22〜29°）は
使わない**——標本が少なく（マゼンタ 9、生成り 2）、審査側が 1200 dpi で測り直すと色ごとの傾きはそろわなかった。縞の向きは手順表の運び
から出す（`RoundTube8SurfaceMesh.bothWaysFibreStripeAngleDegrees`）。手順は記録として残す。

1. 「粒を数えて1周期を読む」と同じく、紐を寝かせて円筒の縮みを戻す（長さの向きと周の向きが同じ px になる）。
2. 4色を分け、色ごとのつながった塊（150〜2500 px）を目1つとする。正面から弧長 30 px 以内の目だけを使う。
3. **目の長い軸** = 塊の2次モーメントの大きいほうの固有ベクトル。細長さ（固有値の比の平方根）1.3 以上の目だけを使う。
   角は紐の軸から、**長さの向きに進むと弧長が増える向きを正**とする（写真の中の向き。描いた紐のどちら回りかは分からない）。

| | 目の数 | 中央値 | 四分位 |
| --- | --- | --- | --- |
| シアン | 14 | −21.7° | −26.8〜−13.2 |
| マゼンタ | 9 | −31.2° | −33.6〜−20.4 |
| 黄緑 | 37 | +29.3° | +10.0〜+53.8 |
| 生成り | 2 | +36.6° | — |
| **場所 1・3・5・7（シアン・マゼンタ）** | 23 | **−22.4°** | −30.3〜−14.3 |
| **場所 2・4・6・8（黄緑・生成り）** | 39 | **+29.3°** | −0.8〜+54.3 |

- **傾きは組ごとに逆**（杉綾）。紐の長さの向きの1行には両方の組の目が交互に並ぶので、**行ごとの平均はほぼ打ち消す**
  （正面の弧長 ±30 px を 15 px の帯に分けても、帯の中で組ごとに符号が分かれる）。杉綾は「行ごと」ではなく「組ごと」である。
- 場所 2・4・6・8 のほうが散らばる（黄緑の塊が隣とつながりやすい）。
- **輝度の構造テンソル**（勾配の積をぼかし、変化の小さい向き）も試したが、**使えない**: ぼかしの強さを変えると中央値の符号が
  変わる（場所 1・3・5・7 で σ 2 px: +3.9°、σ 4 px: +31.2°、σ 6 px: +34.9°）。この倍率では網点と糸の筋が勾配を支配する。
- **写真の向きと描いた紐の向き（どちらが回りの向きか）は分からない**（八つ金剛の 049 2.3 と同じ）。符号は「2つの組で逆」だけを使う。


### 隣り合う目の色の組と、周の行の間隔を読む（江戸八つ組、教科書 p.64、Task 059 追補4、2026-09-25）

**目どうしの隣を、目が作る格子そのものから読む**（`Scripts/task059/count_neighbours.py`）。格子を仮定しない。

1. 「目の傾きを読む」と同じく、紐を寝かせて円筒の縮みを戻し、4色を分け、色ごとのつながった塊（150〜2500 px）を目1つとし、塊の重心を
   目の位置とする。正面から弧長 40 px 以内の目だけを使う（159個）。
2. 70 px 以内の目の組ごとに差のベクトルを取ると、群に分かれる: **縦（長さ 47.6 px、周 3 px）**、**斜め（長さ 23.9 px、周 26.5 px、
   周の向きは両方）**、**真周（長さ 3 px、周 50 px）**。組を群に割り振り、各組を1度だけ数える。
3. **周の行の間** = 斜めの隣の周の成分（26.5 px）。縦の隣（47.6 px）の約半分。**周に何行あるかは、この手順では読めない**:
   輪郭の幅で割った値は行の数として確定扱いしない（輪郭は目の面ではない。作者、2026-09-25）。
4. **目でない画素**: 正面から弧長 40 px 以内の画素のうち、k-means で4色のどれでもない2群（溝と影）に入る割合 = **15.3%**。

| 隣 | 組の数 | 色の組 |
| --- | --- | --- |
| 縦 | 122 | マゼンタ–黄緑 74、シアン–生成り 47、黄緑–生成り 1 |
| 斜め | 180 | シアン–黄緑 59、マゼンタ–シアン 55、マゼンタ–生成り 32、黄緑–生成り 32、シアン–生成り 1、黄緑–黄緑 1 |
| 真周（2行先） | 36 | マゼンタ–黄緑 28、シアン–生成り 7、黄緑–生成り 1 |

- **縦の隣はいつも違う組**（場所 1・3・5・7 の糸と 2・4・6・8 の糸）、**周の行は1行ずつ交互**（斜めの隣はいつも別の種類の行どうし、
  2行先は同じ種類）。審査側が数えた縦 35/35 と合う。
- **1周期 0.50 D はここから読む**（Task 059 追補6、`pitchOverDiameterTurningBothWays`、`.observed`）: 同じ行の隣は1周期（どの場所も
  1周期に1度上を渡られる）で、**47.6 px ÷ 94 px = 0.506**。審査側の 1200 dpi では 95 px ÷ 189 px = 0.503。長さの向きの色の周期（1.01 D）は
  ちょうど2周期にあたる。
- **正面の行の見かけ**: 隣の行の目は周へ 26.5 px ＝ **0.282 D**（審査側の 1200 dpi で 49〜51 px ＝ 0.27 D）。これは正面で広げた見かけで、
  周に何行あるかではない。
- **写真の隣の組は、図のシアンと黄緑を入れ替えた配色の「上を渡った糸」の行と一致する**（Task 059 追補6、審査側の読み。
  `EdoYatsuTurnTests.theSwappedColouringHasThePhotographsPairs`）。
- **描いた紐の隣の組は画素ではなく手順表と格子から数える**（`EdoYatsuTurnTests.neighbourPairs`）。カードの床は `RoundTube8CardImage
  .shownMap` の `.beneath` の画素、溝は「糸の4色でない画素」の割合（`EdoYatsuTurnTests.shareNotAThreadColour`）で数える。

**描いた正面にも回せる**（Task 059 追補6）: `python3 Scripts/task059/count_neighbours.py FACE.png`。平らに塗った正面の面（`BraidComparisonSheet
.paint`、`theEdoYatsuFacesForMeasuring` と同じ描き方）を `measure_grains.face` で寝かせ、4色は描いた色の最も近いもの（離れすぎは目でない）で
分ける。**窓はどれも紐の太さに対する比**（写真の px ÷ 94）なので、写真の数は変わらない。2026-09-25 の描いた正面（教科書の図の配色、
紐の太さ 230 px）: 同じ行の隣 **0.509 D**、隣の行の目への周の見かけ **0.365 D**。8行が輪郭とほぼ同じ半径に 45° おきに並ぶと、
正面で広げて 0.36〜0.39 D になる。
## 7. アプリの描画を、決まった配色と回転角で撮って写真と並べる（Task 045、2026-09-19）

**測る手順ではなく、見比べる画像を作り直せるようにするための手順である。**数は読まない（読んだのは頂点数と生成時間だけ）。

1. デバッグビルドの起動引数で、八つ金剛の立体（またはカード）を直接開く
   （`Kumihimo/Features/BraidSimulation/YatsuKongoComparisonPreview.swift`。本番UIには何も足していない）:
   `--ui-testing-yatsu-kongo-solid`／`--ui-testing-yatsu-kongo-card`、`--yatsu-kongo-recipe=s|z`、
   `--yatsu-kongo-colouring=plain|book|eight`（生成り1色・bookA p.54 の配色・8色すべて別）、`--yatsu-kongo-roll=<度>`（長軸まわり）。
   描くのはエディタと同じ `BraidPreviewForFamily`／`BraidThumbnailForFamily` で、素材・照明・カメラは同じ。
2. `sh Scripts/task045/render.sh <UDID> <app> .build/task045/<状態> <状態>` が、S/Z × 3配色 × 回転 0・30・60・90・180・270° と
   カードを撮る（`xcrun simctl io … screenshot`）。起動から撮るまで4秒待つ。
3. `python3 Scripts/task045/compose.py A B C` が `.build/task045/sheets/` に並べる。**紐幅を 200 px に揃える一様な拡大縮小だけ**で、
   長手だけの伸縮はしない。写真の紐幅は測り方6の `body` で読み（S 235 px、Z-a 241 px）、画面の紐幅はキャンバスの地色と違う行の数で読む
   （iPad (10th generation) の画面で 208 px）。**写真は右へ4分の1回して上を右へ置く**——写真の豆の尖った端は上を向き、描いた糸束の尖った端は
   組み点（画面の右）を向くからである。**写真の上が組み点側だというのは、この読みであって写真が示していることではない。**
   切り出した範囲と倍率は `sheets/record.txt` に書き出される。
4. 状態ごとのアプリは `.build/task045/Kumihimo-{A,B,C}.app` に写して取っておいた（`.build/` は git 管理外。作り直すには該当コミットをビルドする）。
5. **追補（2026-09-19）**: `python3 Scripts/task045/review_fixes.py` が `.build/task045/review-fixes/sheets/` に、立体の正面と修正前後のカードを縦に並べ、
   カードに立体の正面（turns 0）と正面から見える半周、長手・周方向の向きを注記した画像を作る。カードの座標は `UnrolledPatternThumbnailLayout` と同じ計算で
   画面から読み（`record.txt`）、注記は画像を加工しない。**一覧と立体の一致そのものは画像ではなく試験で確かめる**（`RoundTube8CardAgreesWithSolidTests`）。
