# 実物の写真とレンダーを突き合わせる測り方

組紐の見た目を実物へ寄せる作業では、同じ量を何度も測り直す。手順が揺れると、前のタスクの
数値と今のタスクの数値が比べられなくなる。**ここを測り方の正本とする。**

各手順の出どころと生データは `.build/task007e-screenshots/`（平源氏）と
`.build/task005j-screenshots/`（丸源氏）にある。**`.build/` は git の管理外なので、
判断に効く事実はこちらへ写すこと。**

## 共通の原則

**較正式は組ごとに取り直すこと。引き写さないこと。** 断面も1手のピッチも組ごとに違う。
平源氏の畝の較正は直線 σ(%) = 9.55 × 頂 だったが、丸源氏は直線ですらなかった
（σ(%) = 17.31 × 頂^0.752）。引き写していれば大きく外していた。

**資料の数値へ ±15% で合わせにいかないこと。** 資料の値には糸の繊維のざらつき、カメラの
周辺減光、辺の取り方の振れが混じっている。**下限と目視で判断する。**

**手順が要求する条件は、フィクスチャを替えるたびに満たされているか確かめること。**
平源氏では主値の fixture B がたまたま単色だったため、「手元のフィクスチャで測る」ことと
「単色の行で測る」ことが一致していた。丸源氏へ数字を1つ並べたとき、この一致が偶然だった
ことに気づかず、2色フィクスチャの値を参考値として1年分の判断の隣に置いてしまった。

---

## 1. 長手方向の輝度変調

紐の谷の陰がどれだけ深いかを表す。

### 手順

1. スクリーンショットは**原寸**で測る（比較画像は縮小がかかって値が下がる）。資料写真は
   元の解像度で、傾きだけ補正する。
2. **単色の行**を選ぶ。行の中で色が替わると、形と無関係に値が上がる。
   - レンダー: 全糸を1色にしたフィクスチャを使う
     （`--ui-testing-hira-surface-plain` / `--ui-testing-maru-surface-plain`）。
   - 資料: 長手方向に1色で続く帯を目視で選び、その帯の内側だけを使う。
3. 各行について、紐の直線部の画素を取り、**RGB の単純平均**を輝度とする（重み付けなし）。
4. **平滑化はしない。**
5. その1次元配列の p95 と p5 から `(p95 − p5) / p95` を求める。
6. **行ごとの値の中央値**を代表値とする（平均ではない）。min と max も併記する。

### この指標が掛けられない帯

p95 で割るので、**p95 が小さい帯では発散する。** 黒や紺のように極端に暗い帯は分母が
小さいぶんだけ値が跳ね、明るい帯と並べて比べられない。単色で続くのがそういう帯しかない
資料は、この手順では測れない。除外して理由を書くこと。

Task 005J でこれにより bookA p94（丸源氏の完成写真）を外した。長手方向に1色で続くのは
黒の縁取りだけで、山形の側は白・橙・桃が交互に来るため単色の帯が取れない。

### これまでの値

  平源氏  baseColor のみ 0.463 → 両スロット 0.618（Task 007E 段階4a）
  丸源氏  baseColor のみ 0.489 → 両スロット 0.617（Task 005J 段階1）
  資料 平源氏  bookA p96 0.670 / bookB p23 #23 0.884
  資料 丸源氏  作者の真上から 0.738 / bookB p23 #24b 0.772・0.814

---

## 2. シルエットのさざ波から畝の高さを読む

**この手順は平たい紐にしか使えない。** 適用範囲を先に読むこと。

### 手順

1. 写真・レンダーの傾きを補正する（残差の高域抽出は緩やかな傾きを自動で落とすので、
   1度以下なら省いてよい）。
2. 列ごとに紐の上辺を求める。**輝度が「背景と紐の中間値」を下向きに横切る点**を線形補間で
   亜画素まで求める。
3. 1手のピッチ2つぶんの移動平均を引いて高域を取り出す。
4. その残差の σ と p95 − p5 を、1手のピッチに対する比で表す。
5. **残差の自己相関でさざ波の周期を求め、紐の構造の周期と一致することを必ず確認する。**

### 周期の確認は形式ではない。これが唯一の歯止めである

一致しなければ、測っているのは**糸のけばと背景のざらつき**であって形ではない。
これを飛ばすと資料から誤った値を読む。**2度助かっている。**

- Task 007E 段階5: bookB p23 の白い縁糸は σ 12.59%、周期は出るが r=0.16。頂 1.32 に
  相当する — 糸が自分の直径の1.3倍盛り上がることはない。周期の確認で失格にした。
- Task 005J 段階2: 丸源氏の資料4枚はいずれも周期を持たない（15px 以上のラグでの自己相関
  0.12〜0.41、レンダーは 0.98）。**にもかかわらず、その σ を較正へ通すと頂 0.045〜0.193 が
  返り、現在の値 0.12 はそのまん中に落ちる。** 周期を確認しなければ、4枚とも「0.12 で
  合っている」と答えていた。

短いラグ（15px 未満）の相関は隣の画素どうしの滑らかさであって構造ではない。**構造として
ありうるラグの範囲で見ること。**

### 較正

現在の値を含む3つ以上の頂でレンダーし、σ と頂の関係を実測して引き直す。**頂 0 でも
レンダーして、σ がほぼ 0 になること（測定の床が無いこと）を確かめる。** 床があるなら、
式の切片が実体なのか測定の癖なのか区別できない。

  平源氏  σ(%) = 9.55 × 頂        直線。頂 0.12 / 0.24 / 0.48 で確認。
  丸源氏  σ(%) = 17.31 × 頂^0.752  直線ではない。頂 0.00〜0.48 の8点、残差 0.044、
                                   頂 0 で σ = 0.02%。

### 適用範囲: 丸い紐には使えない（Task 005J 段階2、2026-09-06）

丸源氏で試して、**手順そのものが丸い紐に向かない**ことが分かった。良い写真が無かったので
はない。根拠が3つある。

1. **実物の外周が滑らかで、畝の列に見えない。** 拡大しても縁は1本の滑らかな紐である。
   丸い紐には糸が折り返す縁が無く、糸は斜めにシルエットを横切るだけなので、**滑らかなのが
   正しい可能性が高い。**
2. **シルエットの特徴が目と1対1に対応しない。** レンダーの端の波形の周期は1手ではなく
   **1手の 4/9**（141px に対し 62px）。平源氏では1手そのものだった。何を数えているのかが
   組ごとに変わる。
3. **較正が飽和する。** 丸い紐のシルエットは接線の包絡なので、畝が高くなるほど隣の畝の谷が
   隠れる。頂を2倍にしても σ は2倍にならない。逆に読むとき、上の方ほど分解能が落ちる。

**平源氏のシルエット法は、縁で糸が折り返す平たい紐だから成立した道具である。**
Task 008 以降で丸い組み方（八つ金剛、江戸八つ、八つ瀬、唐八つ、江戸源氏、方丈）の畝を
決めるときは、**この手順を最初から選択肢に入れないこと。** 別の観測量を先に探すこと。

---

## 2.5 レンダーのスクリーンショットから幾何量を読むときの遠近法の補正

**レンダーのスクリーンショットで「紐幅あたりの何か」を数えるときは、模様が手前の面にあって
カメラに近いぶん拡大されている。** 紐幅は最も広いところ、すなわち軸と同じ奥行きで測るのに
対し、模様は手前の面で測るためである。

  倍率 = カメラ距離 ÷ (カメラ距離 − 外半径)

丸源氏では 4.4 ÷ (4.4 − 0.52) = 1.135、つまり**見かけの密度が 14% 低く出る。**

**補正せずに写真と比べると模様が過密に見え、縦横比を下げて合わせたくなる。それは Task 005H
の失敗の再演である。**

Task 005J 段階2b は当初この補正を欠き、1.87 / 2.06 / 2.21 と報告した。補正すると
2.12 / 2.37 / 2.58 で、**幾何から直接出した 2.14 / 2.37 / 2.59 と 0.6% で一致する**
（どちらも紐幅 = 畝の稜線での外径）。**スクリーンショットから幾何量を読んだら、必ず幾何と
突き合わせて裏を取ること。**

### 「紐幅あたり」の数字には、何を紐幅としたかを必ず添えること

これを書かなかったことが Task 005J で文書間に3通りの値を生んだ原因である。丸源氏では
候補が3つあり、同じ頂 0.12 で 1.96（公称・平均面）／**2.14（畝の稜線での外径）**／
2.17（メッシュの最大半径）と割れる。

**写真と比べるなら畝の稜線での外径を使う。写真が測っているのは紐の外形の幅だからである。**
レンダーの輪郭は稜線の予測に対して −1.2〜−1.6% で一定して追随する（差は亜画素の辺の
取り方）。メッシュの最大半径は交差で糸が乗り上げる局所の瘤を含むので −3.3〜−5.4% とずれ、
しかも**ずれが頂とともに広がるため、一定の係数では補正できない。**

平たい紐を正面から見る場合は、面も縁も同じ奥行きにあるのでこの差は出ない。**丸い紐だけの
問題である。**

---

## 3. 正規化した並置による目視の計数（Task 005I）

山形の密度のように「1紐幅あたりいくつ」で表せる量は、**紐幅を揃えて並べて数える**のが
いちばん確かである。

1. レンダーと資料を、**紐幅が同じ画素数になるよう**それぞれ拡大縮小する。
2. **同じ紐幅ぶんの長さ**を切り出す（Task 005I は 4.5 紐幅ぶん）。
3. 数える。

**色の周期と形の周期を取り違えないこと。** 自己相関だけに頼ると、配色が2手ごと・3手ごとに
繰り返す資料では、形の周期ではなく配色の周期を拾う。Task 005J 段階2 で bookA p94 の山形を
自己相関で読むと 140px（配色の周期）に見えるが、拡大して数えると 35px（形の周期）だった。
**必ず拡大した画像で数えて裏を取ること。**

平源氏で単色フィクスチャを足したのも同じ理由である。縁の色が目ごとに変わると、形と無関係に
シルエットの測定点が動き、周期が1手ではなく配色の2手周期になる。

---

## 4. 比較画像の作り方

段階ごとに、判断の根拠を1枚で見られるようにする。

- 紐幅を揃え、同じ紐幅ぶんの長さを並べる。
- 各段に**測った値を併記**する。
- レンダーの前後と、資料を**同じ画像の中**に置く。別々の画像だと目視で比べられない。
- 資料を外した場合は、外した理由を画像の中に書く。

---

## 5. 構成した紐の読み方（Task 024、2026-09-08）

**構成した紐を正本と突き合わせるときは、絵に描いてから読む。** 玉の座標を直接見に行くと、
「どちらが手前か」を自分で推論することになり、その推論が正本との食い違いに化ける。
**平行投影で描き、z バッファが手前に選んだ色を読むこと。** 1本1色、陰影なし。
実装は `Scripts/task024/render.py`。

**読み方の誤りは幾何の誤りに見える。** Task 024 では4回食い違い、4回とも原因は読み方だった
（`tasks/020-general-braid-simulator.md`「これまでの誤り」19〜22）。**絵と正本が合わないとき、
まず読み方を疑うこと。**

### 手順

1. **本体は幅 1〜4 である**（平源氏）。幅 0・幅 5・縁の折り返しは**縁**であり、縁は色が
   付いていてよい（`architecture.md`「平源氏の幅の読みが変わりうる」）。
   **本体の定義は占有履歴に合わせること。絵から決めないこと。**
2. **列はその絵自身の u 軸へ射影して読む。** 表と裏では u が +x と −x を向くので、
   同じ world x を同じ列として読むと片方は紐の外側を読む（`render.box_for`）。
3. **視線は着地する場所の角度に取る**（管の紐）。占有履歴は16の場所を閉じの対で8列に畳み、
   場所は 22.5° ごとに立つ。45° 刻みの視線は**対の継ぎ目**を向き、継ぎ目は2本の糸のものだから
   **同じ糸が1行に2度出る**。対の2場所のうち**手が着地するほう**を取る
   （`occupancy.closing_columns`、`render.maru_columns`）。
4. **列ごとに、その列自身の留まりの高さで読む。** 場所の留まりが替わるのは手が着いたときで、
   1サイクルの手は同時に着かない。**横一線はサイクルの別々の時点を並べたものであって、
   1行ではない**（`render.maru_rows`）。
5. **読み始めは、すべての列で留まりの間隔が k になる最初の留まりである。** はじめの数個は
   種を取り込んでいる途中で、間隔が k になっていない。**この数は高さから読み取るもので、
   合わせ込むものではない。**

**どこを見るかだけが構成から来る。何が見えるかは絵が決める。**

### 一致とみなす自由度

**筒には原点も向きも無い**ので、列の回転・鏡像・上下の向き・行のずらしは自由である
（丸源氏で 8 × 2 × 2 × 4 = 128 通り）。**それ以外は自由ではない。** 糸の番号の付け替えも、
列ごとのずらしも、してはならない。**E/W の列順は未決のままである。**

## 6. 筒の紐の1サイクルの伸びを写真から読む（Task 031、2026-09-10）

**丸い紐の「1サイクルでどれだけ伸びるか」を、実物の写真から測る。** 山の高さ（測り方2）が
丸い紐に使えないと分かったあと、丸い紐について写真から読める寸法はこれが最初である。

**測るのは拡大した写真だけである。** 見開き（紐1本が 60〜78 px）では**同じ手順で縞の向きの
符号が逆に出た**。拡大（紐1本が 230〜260 px）を使うこと。

### 手順

1. **紐を背景から切る。** ぼかしてから、「色がある」（彩度の絶対量が大きい）**または**
   「明るい」（白い糸には色が無い）で拾う。
   **彩度の比 (max−min)/max を使ってはならない**——暗い影が高い値を返し、紐の脇の影を
   紐として拾う。
2. **本体の行を選ぶ。** 行ごとの幅を測り、**中央値の ±12% に収まる最長の連続**を本体とする。
   房・留め・ページの外がこれで落ちる。**紐の幅は、その範囲の幅の中央値。**
   これは**輪郭の幅**であり、山のぶんだけ名目の直径より太い（平源氏の 1.62 対 1.44 と同じ
   事情。`unsettled` に書くこと）。
3. **中心の帯だけを取る**（幅の ±22%）。**遠近補正である**（測り方2.5）——縁では周方向が
   縮んで見える。行ごとに紐の中心を追い、帯をまっすぐに直す。
4. **先に縞の傾きを求め、帯をその傾きで剪断する。** 帯の2次元自己相関の、原点から最も近い
   **極大**（原点そのものではない。相関は原点から単調に落ちるので「最大値」を探すと
   原点の隣が出る）が、面が自分自身へ戻る最短の変位で、縞に沿っている。
   **剪断せずに読むと周期が2割ふらつく。**
5. **色の縦周期**は、剪断した帯の色の信号（橙と生成りを分けるのは**明るさではなく色相**、
   たとえば (R−B)/(R+B)）から、長手方向の傾きを移動平均で取り、
   **自己相関の最も高い峰**を取る。峰の頂は3点の放物線で 1/5 px まで読む。
6. **サイクル数への読み替えは配色から導く。** 位置ごとの色の並びの周期 c と、手順表が
   1サイクルに送るコマ数 s から、**1つの場所が自分の色へ戻るのは c/gcd(c, s) サイクル**。
   八つ金剛の配色 a・S は c=4、s=3 なので **4サイクル**。配色 b は c=8 なので 8サイクルで、
   しかも白と橙が半周期で入れ替わるためどちらの読みでも澄んだ峰にならない。
   **読み替えられない配色は使わないこと。**

**角度から伸びを出す手は採らない。** 角度は締め具合で変わる。自己相関のほうを採る。

### これまでの値（Task 031、`Scripts/task031/measure_photographs.py`）

bookA p.8–9 の拡大（3584×2058）。角は紐を横切る向きからの符号つき角度。

| 紐 | 本体の行 | 幅 | 縞の向き | 色の縦周期 | 周期÷幅 | 1サイクル÷幅 |
| --- | --- | --- | --- | --- | --- | --- |
| Z-b | 454–1909 | 229 px | −56.7° | 251.3 px | 1.098 | （読み替え不可） |
| Z-a | 217–1902 | 241 px | −61.2° | 487.6 px | 2.023 | **0.506** |
| S | 918–1908 | 235 px | +54.0° | 379.3 px | 1.614 | **0.403** |

**縞の向きには2つ別のものがある。混同しないこと。**

- 上の表の「縞の向き」は、**面が自分自身へ戻る最短の変位**（2次元自己相関の、原点に最も近い
  極大）である。これは**升の格子**の向きであって、色の帯の向きとは限らない
- **色の帯の向き**を測るには、**先に升の肌理をぼかして消してから**、帯が水平になる剪断を探す
  （測り方6の手順5と同じ剪断を、角度のほうを未知として解く）。**ぼかしの強さを変えて読みが
  動くなら、それは升を読んでいる**——色を読めていれば動かない

### 色の帯の向き（ぼかしの強さ別、`measure_photographs.py`）

| 紐 | ぼかし1 | 31 | 61 | 91 | 読み |
| --- | --- | --- | --- | --- | --- |
| Z-b | +63.5° | +61.0° | +54.0° | +37.0° | **読めない**（動きすぎる） |
| Z-a | −54.0° | −53.0° | −51.0° | −49.0° | **約 −51°** |
| S | +55.0° | +55.0° | +54.5° | +53.5° | **約 +54.5°**（一致度 0.94） |

**S と Z-a は符号が逆で、大きさもほぼ揃う。** これが鏡であることの、写真からの裏付けである。

### 描いたものは、写真と同じ手順で測ってから比べる（Task 032、2026-09-11）

**写真から測った数を、描いたものの「手で出した値」と比べないこと。**Task 032 段階1 は写真の最短の
歩幅 +36° を、描いたものの通路の幾何から出した 0° と比べて「36° 違う」と書いた。**同じ手順で描いた
面を測ると、升を1つも傾けていないのに 27° を読んだ**（色の格子が最短の歩幅を斜めにする）。比べ方が
揃っていなかった。

- 描いた面は `BraidComparisonSheetTests.theYatsuKongoFacesForMeasuring`（`DRAW_SHEETS` のときだけ）で、
  **写真と同じ紐幅の画素数で、正面から、立てて**描く。測るのは `measure_units.py --drawing` と、
  `measure_photographs.colour_angle` を**手を加えずに**掛けたもの
- **符号も比べること。**傾きの向きは画面の中の回転では変わらない（紐を上下逆さに撮っても同じ）。
  鏡でだけ変わる。**大きさだけ比べると、左右の取り違えを見逃す**——Task 031 の検算は大きさしか見て
  おらず、描いた S が写真の Z の向きに傾いていることを Task 032 まで見逃した
- **描いた面が鏡像でないことを先に確かめる。**`BraidPicture.frame` は `u × v = n`（右 × 下 ＝ 視線）で、
  ふつうのカメラと同じ向きである
**Z-b はここでも読めない**——配色 b は3色で位置ごとの周期が8であり、色の帯が2色の帯になって
いない。

- **S と Z-a は一致しない**（0.403 対 0.506）。**Z-a は3本のうち最も太く**（241 px）、
  ゆるく組まれた紐は太くなり伸びも長くなるので、向きとしては辻褄が合う。
  **どちらかが誤りとは言えていない。** 出荷したのは S の値である（信号が最も澄んでおり、
  描いている紐がそれである）
