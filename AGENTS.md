# Kumihimo 開発ガイド

このリポジトリで作業するCodex Workerは、変更前に次の文書を順に読むこと。

1. `docs/product.md`
2. `docs/architecture.md`
3. 対象機能の `docs/specifications/*.md`
4. 対象作業の `docs/tasks/*.md`

文書間で内容が矛盾する場合は、推測で実装せず、矛盾点と選択肢を報告すること。

## 基本方針

- 対象はiPhoneとiPad向けのネイティブiOSアプリとする。
- SwiftとSwiftUIを使用する。
- 最低対応OSはiOS 17とする。
- UIKitや外部依存パッケージは、標準機能では要件を満たせない場合に限り、追加前に理由を示す。
- 小さく理解しやすい実装を優先し、将来機能を先回りして作り込まない。
- UI文言は日本語を基準にし、表示文字列をビュー内に重複させない。
- ライトモード、ダークモード、Dynamic Type、VoiceOverを考慮する。

## 実装ルール

- 機能単位でファイルを整理し、1つの巨大なViewに処理を集中させない。
- Viewは表示とユーザー操作に集中させ、保存・並べ替え・削除などの状態管理を分離する。
- 強制アンラップと、理由のないクラッシュ処理を避ける。
- 日時や色は、画面表示用文字列ではなく意味のある型で保持する。
- Preview用データと本番データを混在させない。
- 仕様にない大規模なリファクタリングや機能追加を同時に行わない。

## 検証ルール

- 変更後は利用可能なビルドとテストを実行する。
- 並べ替え、空状態、削除など、表示以外の振る舞いは可能な限り自動テストする。
- シミュレータを利用できる場合は、標準文字サイズとアクセシビリティ文字サイズ、ライト／ダークモードを確認する。
- 検証できなかった項目は、完了報告で明示する。
- 導出（`Kumihimo/Domain/Braiding/`）に組み方の名前が入っていないことを
  `sh Scripts/check-braiding-is-general.sh` で確かめる。組み方ごとの手順表は
  `Kumihimo/Domain/BraidMethodCatalog.swift` に置くこと。

## テストの回し方

テストは3層で時間を制限する。**常に短く切るのではなく、詰まったものだけが落ちるようにする。**

### 1. コマンドに必ず時間の上限を渡す

Bash ツールの `timeout` 引数（ミリ秒、最大 600000）を、ビルドとテストの実行には**毎回明示的に
渡す**。既定値に任せない。目安はユニットのみ 300000、UIを含む場合 600000。

**2026-09-12 の実測**（iPhone 16、`-only-testing:KumihimoTests`、増分ビルド）:
**テスト段階 212 秒**、呼び出し全体で約 217 秒。**300000 で足りる。**

**同日いちど 600000 に上げ、同日 300000 に戻した。**上げたときの実測は 442 秒で、
**その 55% が1件（`everyRegionCarriesTheRidgeIncludingBothEdges` の 243 秒）だった。**
**遅さの正体はテストの作りで、直したら 23 秒になった**（Task 018）。
**暫定で上げた値は、直したら下げ直すところまでが1組である。**

これは例外や停止で固まったときに待ち続けないための最後の砦であり、通常の実行を切るための
ものではない。短すぎて正常な実行が切れた場合は、上限を上げるのではなく**下の2〜4を先に
見直す**こと。**上げるなら、この節のように実測を添えること。**

### 2. テスト単位の時間制限（全体を殺さない）

`xcodebuild` に次を渡す。詰まった1件だけが失敗として記録され、残りは続行する。

    -test-timeouts-enabled YES \
    -default-test-execution-time-allowance 60

Swift Testing 側は `@Suite(.timeLimit(.minutes(1)))` で同じことができる。**60秒を超えるテスト
は、そもそもテストの作りを疑うこと。**

**`-maximum-test-execution-time-allowance` は渡さない**（2026-09-12 に外した）。**これは
1件のために 300 へ上げてあった値で、その1件が直ったので書類のほうも消した。**渡さなければ
既定に戻り、どの試験も `.timeLimit` を宣言していない以上、**全件が上の 60 秒で頭打ちになる。**

**これは締めるための選択である。**上限を実測よりはるかに高くしておくと、**10倍に伸びても
上限に当たらないので誰にも見えない。**60 秒で落ちるほうが、見えないまま遅くなるよりよい。
上げたくなったら、**上げる前に作りを疑うこと**（下の閉じた記録がその実例である）。

### 上限を上げてあった1件——閉じた記録（2026-09-09 に開き、2026-09-12 に閉じた。Task 018）

**いまは例外が1件も無い。**この節は消さずに残してある。**同じことをもう一度やらないための
記録であって、いま守るべき決まりではない。**

**旧状態**（2026-09-09〜2026-09-12）: `KumihimoTests/Flat16SurfaceMeshTests.swift` の
`everyRegionCarriesTheRidgeIncludingBothEdges` に **`@Test(.timeLimit(.minutes(5)))`** を付け、
`-maximum-test-execution-time-allowance` を **300** にしてあった。

**なぜ上げていたか**: 既定の60秒では**毎回打ち切られ、打ち切りが平常になって他の異常を
隠していた**。**遅いだけで壊れてはいなかった**ので、まず通るようにした。

**なぜ閉じたか**: **遅さの原因がテストの作りだったからである。243秒 → 23秒。**
`reliefFromThePlainOutline` が**1頂点ごとに 4領域 × 257 段 = 1028 点の断面外形を引き直していた。
この 1028 点は頂点に依存しない。**ループの外へ出しただけで直った（`plainOutline`）。
**主張は1つも弱めていない**（間引いていない）。23秒は既定の60秒に収まるので、
**上限もこの試験の `.timeLimit` も外した。**

**23秒に5分の上限を残さなかったのは、締めるためである。**次に10倍伸びても上限に当たらず、
**また見えなくなる。**

**書き留めておく値打ちがあるのは、遅さそのものではない。**

- **費用を間違った形で書くと、伸びても誰にも見えない。** doc comment は「メッシュ全頂点を
  4回歩く」と書いていた。実際は「頂点数 × 1028 × 弧長表」である。**「頂点数 × 4」と書いてあれば、
  メッシュが大きくなって遅くなるのは当たり前に見えるので、10倍の無駄が平常に見えてしまう。**
  **費用を書くなら、実際に回る回数の形で書くこと。**
- **記録は腐る。**「2分ほど」と書いてあったが、測ったら **4分3秒**だった。倍になっていた。
- **型名とファイル名を取り違えないこと。**この試験の suite は `HiraGenjiSurfaceMeshTests` で、
  ファイル名（`Flat16SurfaceMeshTests.swift`）とは違う。**xcresult は suite 名で引く。**
- **暫定で上げた値は、下げ直すところまでが1組である**（上の 1 の 600000 → 300000）。

### 3. 既定はユニットのみ。UIは明示的に頼まれたときだけ

    -only-testing:KumihimoTests

UIテストは Task 015 の既知のクラッシュがあり、通しで走らせるたびにどれか1件が落ちる（毎回
違うテストで、単独実行では通る）。**既定の検証には含めない。** 含めた場合は、その結果を
Task 015 の既知の事象と区別して報告すること。

### 4. シミュレータをクローンさせない

`stuck in creation state` はクローンで起きる。

    -parallel-testing-enabled NO
    -destination 'platform=iOS Simulator,id=<UDID>'

`name=` ではなく **UDID 指定**にし、`xcrun simctl boot <UDID>` で先に起動しておく。同じ機体を
使い回すこと。

### 5. 結果を残す・出力を減らす

    -resultBundlePath .build/test-results/<タスク名>.xcresult
    -quiet

`-resultBundlePath` を付けておけば、途中で打ち切られても結果を読める。

### 6. 待ち受けを重ねない — 1 つの実行に 1 つの待ち

**走っているテストを待つバックグラウンドの待ち受けは、同時に 1 つだけにすること。**
待ちきれずに「今度こそ報告させる」待ち受けを足しても、**実行は速くならず、待ちが積み上がる
だけである**（2026-09-10、Task 008/031 で同じ実行を 3 つの待ち受けが並んで待ち、1 時間半のうち
大半がこれに消えた）。

**ポーリングをやめる判断は正しい。やめたあとに新しい待ち受けを足すのが誤りである。**
1 回走らせ、1 つ待ち、終わってから次を決めること。

**そもそも全件を何度も回さない。** 直したところに関わるテストを先に回し、**全件は最後に 1 回**。
全件はフルビルドを伴うので安いものではない（**2026-09-12 の実測でテスト段階 212 秒**。
Task 018 を直す前は 442 秒だった)。

### 7. 機体に置いた環境変数は、外すまで残る

**`DRAW_SHEETS` のような「頼まれたときだけ」の環境変数は、`xcodebuild` の環境からは
test プロセスに届かない**（`TEST_RUNNER_` を付けても届かない）。届く道は機体に置くことである。

    xcrun simctl spawn <UDID> launchctl setenv DRAW_SHEETS 1
    ...（そのとき限りの実行）...
    xcrun simctl spawn <UDID> launchctl unsetenv DRAW_SHEETS

**`unsetenv` まで書いて 1 組である。** 置いたままにすると**次の既定の実行でも重い処理が走り**、
関係のないテストが上限で打ち切られる（2026-09-10、Task 032 で比較シートの 2 件が 60 秒で
落ちた。コード側の「頼まれたときだけ」は守られていて、**破れたのは機体の側**だった）。

**打ち切りが出たら、まず機体に何か置いたままでないかを見ること。**

### 報告

**打ち切りが起きた場合は、その旨と上限値を完了報告に必ず書くこと。「テストは通った」と
書かないこと。**

## 完了報告

Workerは完了時に次を簡潔に報告する。

- 実装した内容
- 変更した主要ファイル
- 実行した検証と結果
- 未解決事項または仕様判断が必要な点
